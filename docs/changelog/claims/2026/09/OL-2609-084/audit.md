## Audit of OL-2609-084 - feat(products): one product per donation service, each connecting its service on the product page, in a band that is no longer a green wall

**Audited:** 2026-09-25
**Commit:** 08298f12238c502d01b1b9d85adf9ff95fcabafd (sole commit carrying the trailer)
**Verdict:** FINDINGS

### Claims
| Claim | Verdict | Evidence |
|-------|---------|----------|
| C1 | CONFIRMED | `app/Services/Recipes/RecipeCatalog.php:25-47 @08298f1` - `all()` globs `resources/recipes/*/manifest.json` and `ksort`s, `listed()` filters `listed`; @08298f1 the listed slugs are `bmac_alert, chat_checkin, chat_tower, follower_bowling, fourthwall_alert, kofi_alert, streamlabs_alert, throne_alert` (`coin_flip`, `dice` unlisted), and no `donation_alert` directory exists. @HEAD the slugs are hyphenated (OL-2609-114) and `twitch-chat-overlay` is a ninth listed product (OL-2609-089) |
| C2 | CONFIRMED | all five `resources/recipes/*_alert/manifest.json @08298f1` parsed: top-level keys have no `hero`, `ingredients` or `requires_integrations`; `installs` = one overlay `tip_alert` -> `tip_alert.md`, `integrations` = [own service], one `alert_triggers` entry {`tip_alert`, own service, `donation`}, no `alert_targets`; `max_instances_per_user` 1; each `tip_alert.md:3` is `type: alert`. @HEAD the folders and slugs are renamed (OL-2609-114) |
| C3 | CONFIRMED | each `resources/recipes/*_alert/tip_alert.md:38-65 @08298f1` has a `css` block opening `.donation-alert {` at line 43; `resources/recipes/donation_alert/stage.md` is deleted in the diff |
| C4 | CONFIRMED | `app/Http/Controllers/ProductController.php:346-352 @08298f1` - `collect($firing)->pluck('service')->unique()->filter(ServiceConnections::has)->map(ServiceConnections::for)`; `$firing` is filled only from `ExternalEventTemplateMapping` rows `where('enabled', true)` on alert overlays, in overlay then row order. Same @HEAD (`ProductController.php:711-717`) |
| C5 | CONFIRMED | `ProductController.php:381-414 @08298f1` - `$none` when any overlay `type !== 'alert'` (385) or no statics; `accessed_at >= now()->subDays(30)` on logs of the user's tokens (400); `$statics->take(5)` fallback (407); `total` = `$statics->count()`. Same @HEAD (`ProductController.php:746-778`) |
| C6 | CONFIRMED | `app/Support/ServiceConnections.php:26-32, 69-75 @08298f1` - `connected` = row && `enabled` && `isAuthenticated()`; `received` = `last_received_at !== null`; `webhook_url` null when kind `oauth` (streamlabs, fourthwall); `connect_url` adds `return_to` for oauth only; routes `kofi.save`, `bmac.save`, `throne.connect`. `ProductController.php:344 @08298f1` passes `route('products.show', $slug, false)`. File unchanged @HEAD |
| C7 | CONFIRMED | `app/Http/Controllers/Settings/DonationIntegrationController.php:211-241 @08298f1` - PHP literal `'#^/(?![/\\\\])\S*$#'` is the regex `^/(?![/\\])\S*$`; `put` on match (216), `forget` otherwise (221); `returnTo()` `session()->pull` (231) with fallback `settings.integrations.{service}.show`. Unchanged @HEAD |
| C8 | CONFIRMED | `StreamLabsIntegrationController.php @08298f1` - every `callback()` return (lines 73, 83, 103, 111, 126, 152) is `$this->returnTo()`; `FourthwallIntegrationController.php @08298f1` lines 83, 88, 97, 105, 160, 172 likewise; neither file contains `redirect()->route(`. Same @HEAD; StreamLabs later touched by OL-2609-097 (scope string only) |
| C9 | CONFIRMED | `resources/js/components/ProductServices.vue @08298f1` - oauth `<a>` while `!connected` (100); `none`/`secret` buttons while `!webhook_url` (101-118) calling `router.post`; token field posting `verification_token` while `!has_credential` (122-143, 51); link + Copy while `webhook_url && !received` (146-163); secret field posting `webhook_secret` while `webhook_url && !has_credential` (166-181), where `webhook_url` is non-null exactly when a row exists for a non-oauth kind (`ServiceConnections.php:71`). Unchanged @HEAD |
| C10 | CONFIRMED | `resources/js/pages/products/show.vue @08298f1` - beats in order: `v-if="services.length"` (382), `stages.length` (402), `!stages.length && alerts.length` (432), `v-for="guide in testGuides"` (488), `alerts.length` (523); own-overlays beat: `loadedNames` caps at three plus "and N more" (189-193), OBS-tab links per overlay plus "And N more" when `total` exceeds the list (452-468), "Make an overlay" when none (470-478). Same beats @HEAD (386, 436, 492) |
| C11 | CONFIRMED | `show.vue:196 @08298f1` `servicesTitle`; pill `show.vue:368 @08298f1` reads `${joinNames(connectedLabels)} connected`, falling back to the bare word "Connected" when no service is connected (e.g. Chat Tower); services section with steps left at 594, placed directly before the checklist section. Same @HEAD (200, 372, 598) |
| C12 | CONTRADICTED | Holds except for the clause "every other button a neutral outline pill": the "Make an overlay" link at `show.vue:473-478 @08298f1` goes to `route('templates.create')`, not an OBS tab, and carries the blue pill classes (`border-blue-500/50 bg-blue-500/8 ... text-blue-700`). Still true: section classes `product-ready mt-8 flex flex-col gap-6` (362); cards `border-sidebar-border bg-sidebar ... dark:bg-black/35` with no `rounded` (383, 403, 433, 490, 524); landed pill `product-glow` (534) with text unchanged; waiting dot `bg-blue-300` on `product-pulse` (545). Same @HEAD (`show.vue:481`) |
| C13 | CONFIRMED | `show.vue:717-722 @08298f1` - "Add the overlay to OBS" when `product.overlays.some(type !== 'alert')`, else "Have one of your overlays in OBS" when an alert exists. Same @HEAD (752-755) |
| C14 | CONFIRMED | `php artisan test --filter=ProductDonationServicesTest` in a worktree at 08298f1 (own `composer install` and `npm run build`): 31 passed, 416 assertions. Each asserted item is present: per-product page (55-72), manifest shape (74-83), install (85-109), refusal sentence against a switched-off trigger (111-131), side-by-side next to a Throne alert (133-153), installed view (155-182), five newest of seven then Scene 2 at 3 days with the 40-day log ignored (207-246), landed filtering (248-280), pre-install tip (282-299), switched-off trigger (301-312), banner `integration-kofi` (314-324), no duplicate row (326-335), uninstall (337-352) |
| C15 | CONFIRMED | `php artisan test --filter=ProductServiceConnectTest` at 08298f1: 14 passed, 174 assertions. Ko-fi before/after with wire `satisfied` (32-90), empty token (92-101), Throne delete + one POST (103-131), BMAC link then secret (133-158), Streamlabs success and cancel with `assertSessionMissing('integration_return_to.streamlabs')` (160-195), four `return_to` values off-site, protocol-relative, backslash and empty (197-210), unconfigured Fourthwall (212-221), Chat Tower `services == []` (223-230) |
| C16 | UNVERIFIABLE | tagged [unverified] |

### Surface
Complete. All 28 paths in `git show --stat` are listed, apart from the claim file and `docs/changelog/changelog-2026-09.md`, which are exempt. No phantom paths.

### Findings
- **F1** contradicted claim - C12 says every button other than the OBS links is a neutral outline pill, but the "Make an overlay" link at `resources/js/pages/products/show.vue:473-478 @08298f1` (`:481 @HEAD`) is a blue pill pointing at `templates.create`; a new claim should restate C12 to cover it, or the link should take the neutral pill classes.
- **F2** Surface description false - Surface says `tests/Feature/ProductDonationServicesTest.php` has "30 tests"; at 08298f1 it runs 31 (four `->with(donationProducts())` tests x 5, plus 11 plain tests); a new claim should restate the count.
- **F3** reverses an earlier claim without citing it - the diff deletes `resources/recipes/donation_alert/` and `tests/Feature/ProductDonationAlertTest.php`, which undoes OL-2609-077 (C1-C5, C9: "`donation_alert/manifest.json` declares `listed: true` ...") and OL-2609-082 (C1: "`installs.alert_triggers` in the manifest lists five entries ..."), but the claim cites neither ID and cites only OL-2609-083, under Unchanged; a new claim should record the reversal as "supersedes OL-2609-077 and OL-2609-082".
- **F4** narrows an earlier claim without citing it - OL-2609-080 C8 records the finished band's beats as "the OBS beat when a static overlay exists, the test-tip beat when `installed.test_guide` is set, and the 'Watch it land' beat". At `show.vue:382-523 @08298f1` there is a services beat, an own-overlays OBS beat, and one test-tip beat per `testGuides` entry, which is built from connected services' guides first (`show.vue:176-180`). C10 does not cite OL-2609-080; a new claim should cite "corrects OL-2609-080 C8".

### Notes
- The Pest runs were done in a scratch worktree at 08298f1. It needed its own `composer install`, because HEAD's vendor no longer has `stevebauman/location` (removed in OL-2609-097), and `npm run build`, because the first run failed 27 tests on a missing Vite manifest. After the build, all 78 tests across the five Product* files passed. The worktree has been removed.
- HEAD drift is disclosed in every case: slugs and folders by OL-2609-114, the ninth product by OL-2609-089, and the StreamLabs scope string by OL-2609-097. Commit 49eca947 touches `show.vue` with no claim, but only `.vue` files, so it is exempt, and it removes the page's own toast and ConfirmDialog mounts, not any claimed symbol.
- The Risk line "The save flashes ... now also show as toasts on the product page" describes wiring that 49eca947 later moved into `ProductsLayout`.
