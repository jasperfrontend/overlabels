## Audit of OL-2609-098 - feat(help): /viewers, a notice page written for the people whose data we hold

**Audited:** 2026-09-25
**Commit:** 1b641c7cb552afdcebf15189888a7c2e9a523dd2
**Verdict:** FINDINGS

### Claims
| Claim | Verdict | Evidence |
|-------|---------|----------|
| C1 | CONTRADICTED (half) | Redirect half CONFIRMED: `routes/web.php:110 @1b641c7` is `Route::redirect('/viewers', '/help/viewers', 301)`, no controller; `routes/web.php:111 @HEAD` unchanged. Position half false: the `/manifesto` redirect is at `routes/web.php:105 @1b641c7`, five lines above, not three (blank line plus three comment lines between) |
| C2 | CONFIRMED | No table, controller or component in the diff (`git show --stat`); `HelpPage::all()` walks `pages/` recursively (`app/Support/HelpPage.php:68 @1b641c7`); page routes and `.md` twin are generated by a loop (`routes/web.php:80,85 @1b641c7`); sitemap, search index and sidebar read `HelpCorpus` (`SitemapController.php:6`, `BuildHelpReferenceIndex.php:68`, `HelpNav.php:35-36 @1b641c7`) |
| C3 | CONFIRMED | `resources/help/pages/viewers.md:3 @1b641c7` is `section: Getting started`, a key of `HelpCorpus::SECTIONS` (`app/Support/HelpCorpus.php:82 @1b641c7`); frontmatter lines 1-9 carry no `context:`; the cap test skips pages with no declared context (`tests/Feature/HelpContextTest.php:34-36 @1b641c7`) |
| C4 | CONFIRMED (code half); UNVERIFIABLE (history half) | `viewers.md:5 @1b641c7` heading is `Why your name is on a stream`, no apostrophe; `tests/Feature/HelpPageTest.php:80-82 @1b641c7` asserts `->toContain($page['heading'])` on the response body. "The first draft did, and was changed" is a statement about a tree that does not exist and cannot be checked as [code] - see F2 |
| C5 | CONFIRMED | Ran `php artisan test --filter=Help` @HEAD: 74 tests, 73 passed, 1 skipped (`HelpPageOgImageTest` "gives two help pages two different cards" - resvg not installed). Covers `HelpPageTest.php:17` (slug), `:61` (whole rendered body), `:153` (reachable from index), `HelpTaxonomyTest.php:14,102` (sections). Help code and tests between @1b641c7 and @HEAD differ only in `HelpContextTest.php` (2 lines) and `viewers.md` |
| C6 | CONFIRMED | `viewers.md:61-68 @1b641c7` gives `privacy@overlabels.com` as the only route; `git grep -i forgetme 1b641c7 -- app routes resources/js config database` returns nothing. @HEAD `!forgetme` exists and the page names it first (OL-2609-099) |
| C7 | CONTRADICTED (half) | Check-ins: `routes/console.php:209 @1b641c7` `Checkin::where('checked_in_at', '<', now()->subDays(90))`; twitch/external events 90 days `:173`, `:176`; TTS 7 days `:304`. All match `viewers.md:33,36,37,59 @1b641c7`. Backups 30 days is not in `routes/console.php @1b641c7` (backup lines 332-360 schedule and ping only); `docs/deploy/database-backups.md:23 @1b641c7` says "Retention is **not** in the code" - see F3 |
| C8 | CONTRADICTED | Page half true: `viewers.md:19-25 @1b641c7`; `resources/js/composables/useTwitchChat.ts:25,201 @1b641c7` connects anonymously to `wss://irc-ws.chat.twitch.tv:443` as `justinfan…`. `StreamSessionService::applyChatStats()` does not exist @1b641c7 or @HEAD; the method writing `latest_chat_message` is `applyChatSummary()` (`app/Services/StreamSessionService.php:234,285 @1b641c7`) - see F4 |
| C9 | CONFIRMED | `app/Services/TwitchPayloadScrubber.php:34-36 @1b641c7` denies `top_predictors`; `:41-46,56-60` null `user_id`/`user_login`/`user_name`/`user_avatar` when `is_anonymous`; called at `TwitchEventSubController.php:524 @1b641c7`; file added in `8ad02ed3` (trailer OL-2609-097). Page: `viewers.md:51-54 @1b641c7` |

### Surface
Complete.

### Findings
- **F1** false detail in C1 - the claim puts the `/manifesto` redirect "three lines above" the new route, but it is at `routes/web.php:105 @1b641c7` against `:110`; the next claim should state the position correctly or leave it out.
- **F2** mistagged, checkable as [unverified] - C4's last sentence ("The first draft did, and was changed") describes a draft that never reached the tree, so it cannot be checked under [code]; it belongs in its own claim tagged [unverified].
- **F3** mistagged / false citation in C7 - "backups 30 days" is attributed to `routes/console.php`, which holds no backup retention @1b641c7; the figure is an R2 lifecycle rule outside the repo (`docs/deploy/database-backups.md:23 @1b641c7`), so that half should be a separate [unverified] claim or cite that doc.
- **F4** symbol not found in C8 - `StreamSessionService::applyChatStats()` does not exist at @1b641c7 or @HEAD; the method is `applyChatSummary()` at `app/Services/StreamSessionService.php:234 @1b641c7`. A later claim should name the right symbol.

### Notes
- C6 and the email-only removal text were superseded by OL-2609-099 (`!forgetme`, listed in its Surface) and the callout further changed by OL-2609-103; both disclose `viewers.md`.
- The comment at `routes/web.php:107 @1b641c7` says the page "is linked from Twitch chat by the bot"; at ship no bot change existed (claim's own Unchanged). OL-2609-099 later built the notice.
- `viewers.md:34 @1b641c7` says a tower player's name is kept "Until the tower falls", but `TowerService` rewrites the all-time `tower_record` List with record-holders' names (`app/Services/Tower/TowerService.php:21-30 @1b641c7`). No claim asserts the tower row, so not a finding; worth checking the page.
- C5's test run was done at HEAD; an exported tree of @1b641c7 could not boot because `vendor` autoloads the repo path.
