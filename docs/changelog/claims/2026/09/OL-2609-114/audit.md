## Audit of OL-2609-114 - feat(products): hyphenated product URLs, 301s from the old ones, and the SEO head they never had

**Audited:** 2026-09-25
**Commit:** 0212f7b3ae479d50e18edb606e612b8465bd559f
**Verdict:** FINDINGS

### Claims
| Claim | Verdict | Evidence |
|-------|---------|----------|
| C1 | CONFIRMED | `resources/recipes/*/manifest.json:3 @0212f7b` - the nine slugs are as listed, and each is its own `name` kebab-cased ("Buy Me a Coffee Alerts" -> `buy-me-a-coffee-alerts`, "Ko-fi Alerts" -> `ko-fi-alerts`, "Twitch Chat Overlay" -> `twitch-chat-overlay`, the other six likewise); same @HEAD |
| C2 | CONFIRMED | `resources/recipes/*/manifest.json:4 @0212f7b` - each of the nine has one alias, its former slug (`bmac_alert`, `chat_checkin`, `chat_tower`, `follower_bowling`, `fourthwall_alert`, `kofi_alert`, `streamlabs_alert`, `throne_alert`, `twitch_chat`), and no value repeats; same @HEAD |
| C3 | CONFIRMED | `resources/recipes/{coin_flip,dice}/manifest.json:3 @0212f7b` - slugs `coin_flip`, `dice`, and neither has `url_aliases`. Neither has a `listed` key at all; it resolves to false through `($manifest['listed'] ?? false)` (`app/Services/Recipes/RecipeCatalog.php:63 @0212f7b`, schema `"default": false`), and `ProductController::listedManifest()` aborts 404 on an unlisted manifest (`app/Http/Controllers/ProductController.php:570-571 @0212f7b`) |
| C4 | CONFIRMED | `app/Services/Recipes/RecipeCatalog.php:75-84 @0212f7b` - loops `all()`, returns the key slug when `in_array($legacy, $manifest['url_aliases'] ?? [], true)`, else null; unchanged @HEAD |
| C5 | CONFIRMED | `app/Http/Controllers/ProductController.php:213-215 @0212f7b` (`show()`) and `:445-447` (`design()`) - `movedPermanently()` is the first statement; it returns `redirect()->route($route, $canonical, 301)` (`:542-546`); unchanged @HEAD |
| C6 | CONFIRMED | `app/Http/Controllers/ProductController.php:355 @0212f7b` (`install()`), `:396` (`applyPreset()`), `:491` (`uninstall()`) - `$slug = $this->canonical($slug);` is the first line of each; `canonical()` is `canonicalSlugFor($slug) ?? $slug` (`:558-561`), and `instanceFor()` matches `Recipe::where('slug', $slug)` (`:602-608`); unchanged @HEAD |
| C7 | CONFIRMED | `app/Models/RecipeInstance.php:46-48 @0212f7b` - `str_replace('-', '_', $productSlug)`; `ProductController.php:376 @0212f7b` passes `RecipeInstance::instanceSlugFrom($slug)` to `$this->installer->install()`; unchanged @HEAD |
| C8 | CONFIRMED | `app/Models/RecipeInstance.php:33 @0212f7b` - `'/^[a-z][a-z0-9_]{0,49}$/'`, a context line in the diff, not a changed one; same @HEAD |
| C9 | CONFIRMED | `database/migrations/2026_09_19_120000_rename_product_slugs_to_url_form.php:33-43 @0212f7b` - `RENAMES` literal with the nine pairs; `up()` `:48` and `down()` `:55` use `DB::table('recipes')` only, `down()` swapping each pair back; no `App\Models` import |
| C10 | CONFIRMED | same migration `@0212f7b` - the only `DB::table` calls are `'recipes'` (`:48`, `:55`); `database/migrations/2026_05_13_140001_create_recipe_instances_table.php:14 @0212f7b` - `foreignId('recipe_id')->constrained()` |
| C11 | CONFIRMED | `app/Services/Recipes/RecipeManifestValidator.php:160-166 @0212f7b` - inside `semanticErrors()` (`:149`), `str_contains($slug, '-') && ($manifest['control_exports'] ?? []) !== []` adds `'pointer' => '/slug'`; unchanged @HEAD |
| C12 | CONFIRMED | `resources/recipes/recipe-manifest.schema.json:18 @0212f7b` - slug pattern `^[a-z][a-z0-9_-]{0,49}$`; `:83-89` `url_aliases` is `type: array`, `items.type: string`, `uniqueItems: true`. @HEAD only an unrelated `lists` description changed (OL-2609-129) |
| C13 | CONFIRMED | `routes/web.php:286,701,704,710,715 @0212f7b` - the five product `slug` constraints are `[a-z][a-z0-9_-]*`; `:715` keeps `'preset' => '[a-z][a-z0-9_]*'`. @HEAD a sixth product constraint exists (`routes/web.php:755 @HEAD`, `products.saved-presets.*`, OL-2609-121), also hyphenated |
| C14 | CONFIRMED | `resources/js/composables/useUiMode.ts:32 @0212f7b` - `const SLUG = /^[a-z][a-z0-9_-]{0,49}$/`, applied to the `product` param at `:43`; unchanged @HEAD. See F2 |
| C15 | CONTRADICTED (compound) | Half 1 (`/products` and one `<loc>` per listed slug are emitted) true: `app/Http/Controllers/SitemapController.php:40 @0212f7b` and the `foreach (array_keys($this->catalog->listed()) ...)` loop at `:85-91`. Half 2 ("neither hand-listed") false: `/products` is a literal `['path' => '/products', ...]` entry in the hand-written `STATIC_PATHS` const at `:40 @0212f7b`; only the per-product rows are derived. Half 3 (no `/products` URL before) true: `git show 0212f7b^:app/Http/Controllers/SitemapController.php \| grep products` returns nothing |
| C16 | CONFIRMED | `app/Http/Controllers/ProductController.php:248 @0212f7b` shares `canonical`; `:261-293` shares `jsonLd` with `@graph` holding exactly a `SoftwareApplication` node (`:265`) and a `BreadcrumbList` node (`:285`); unchanged @HEAD |
| C17 | CONFIRMED | `app/Http/Controllers/ProductController.php:110-115 @0212f7b` - `INSTALLED_FILTER` shares `robots` `noindex, follow` and `canonical` `route('products.index')`; otherwise `canonical` is the bare index or `products.index` with the same `category`; unchanged @HEAD |
| C18 | CONFIRMED | `resources/js/layouts/ProductsLayout.vue:119-120 @0212f7b` - `heading` rendered when set, else the `Products` link; `:124-127` `lead ??` the catalogue sentence; `resources/js/pages/products/index.vue:89 @0212f7b` passes neither prop. Still true @HEAD (`ProductsLayout.vue:109-117`, `index.vue:89`) after unclaimed `49eca947` |
| C19 | CONFIRMED | `resources/js/pages/products/show.vue @0212f7b` - no `<h1`; `product.description` appears only as the `<Head>` meta description (`:318`, pre-existing at `@0212f7b^`) and as the `:lead` prop (`:331`). Same @HEAD (`:271`, `:280`) |
| C20 | CONFIRMED | `tests/Feature/ProductSlugRenameTest.php @0212f7b` (identical @HEAD) - test 1 asserts 301 + `assertRedirect` for every alias of every listed product; test 2 that each listed product has non-empty `url_aliases`; test 3 that `/sitemap.xml` contains every current slug and no alias; test 4 installs `twitch-chat-overlay` through `RecipeInstaller::install()` with `instanceSlugFrom()` and asserts `instance_slug` matches `SLUG_PATTERN`; test 6 asserts the validator refuses `my-picker` with `control_exports`. `php artisan test --filter=ProductSlugRenameTest` @HEAD: 6 passed (69 assertions) |
| C21 | UNVERIFIABLE | tagged [unverified]; fail-first runs against trees that no longer exist |
| C22 | UNVERIFIABLE | tagged [unverified]; a past full-gate run |

### Surface
Complete.

### Findings
- **F1** compound claim, half false - C15 says the `/products` sitemap entry is not hand-listed, but it is a literal row in `SitemapController::STATIC_PATHS` at `app/Http/Controllers/SitemapController.php:40 @0212f7b`; only the per-product `<loc>` rows are derived from `RecipeCatalog::listed()`. Restate C15 in a new claim citing OL-2609-114 C15.
- **F2** record contradiction - `resources/js/composables/useUiMode.ts:32 @0212f7b` widens the `product` slug regex that OL-2609-050 C1 records as "only for a value matching `^[a-z][a-z0-9_]{0,49}$`" (and OL-2609-045 C1 the same). C14 does not cite either one inline. A new claim should record that it supersedes OL-2609-050 C1.
- **F3** record contradiction - OL-2609-088 C29 records "The page lead in `ProductsLayout.vue` ... [is] the same sentence, beginning 'Everything here is free, made by Overlabels'". On a product page that lead is now `product.description` (`resources/js/layouts/ProductsLayout.vue:124-127 @0212f7b`, `resources/js/pages/products/show.vue:331 @0212f7b`), so 088 C29 now holds only for the listing. C18 does not cite 088 C29. A new claim should record the narrowing.
- **F4** scope - `resources/js/layouts/ProductsLayout.vue:115 @0212f7b` changes the header `ProductBadge` accessible `label` to "An official Overlabels product" when `heading` is set, and adds `shrink-0` at `:116`. The Surface line says "optional `heading`/`lead` props" and no claim mentions this. Disclose it in a follow-up claim.
- **F5** scope - `resources/js/pages/products/show.vue:351-352 @0212f7b` changes the state band row from `items-start` to `items-center` and drops the `mt-0.5`/`mt-1` offsets on the badge and service logo. `:364` now renders the Installed / bot line column only under `v-if="installed || product.requires_bot"`. The Surface line covers only the heading/lead handoff and the removed `h1` and description. Disclose it in a follow-up claim.

### Notes
- `CLAUDE.md:438` (@0212f7b and @HEAD) still names `resources/recipes/chat_tower/tower.md`, a path this commit moved to `chat-tower/`. The Chat Tower section's "re-export from `docs/private/engine/chat-tower/`" instruction now points at a missing target path.
- `resources/recipes/twitch-chat-overlay/chat.md:273 @0212f7b` has the new URL but still reads "Installed by the Twitch Chat product", although the manifest `name` became "Twitch Chat Overlay".
- `ProductSlugRenameTest` was run at HEAD only; the test file is byte-identical at @0212f7b. `ProductController`, `SitemapController`, `routes/web.php` and the Twitch Chat manifest have drifted since then, through OL-2609-115/121/129 and unclaimed `49eca947` (a `.vue`-only chrome move). None of that drift touches a claimed symbol.
