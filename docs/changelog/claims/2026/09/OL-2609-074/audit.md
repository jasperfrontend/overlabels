## Audit of OL-2609-074 - fix(copy): spell the service Streamlabs, except where the spelling is a value

**Audited:** 2026-09-25
**Commit:** 64a7e3c62d483f6a975834ce5d68db1bd71b6c8f
**Verdict:** FINDINGS

### Claims
| Claim | Verdict | Evidence |
|-------|---------|----------|
| C1 | CONFIRMED | `app/Services/External/Drivers/StreamLabsServiceDriver.php:86 @64a7e3c` - `'event.source' => 'StreamLabs'`; the line is outside every hunk of the diff. @HEAD same value at line 87 (line shift from f8361f19, OL-2609-096) |
| C2 | CONFIRMED | `resources/help/pages/conditionals.md:1056 @64a7e3c` - `Always "StreamLabs"`; `resources/help/reference/eventsub-tags/streamlabs-donation-event-tags.md:10 @64a7e3c` - `Always "StreamLabs"`; both identical @HEAD |
| C3 | CONFIRMED | Every hunk in `git show 64a7e3c` changes only string literals, prose or Markdown headings; `routes/` is not in the diff; `href="/settings/integrations/streamlabs/redirect"` and `service="streamlabs"` in `streamlabs.vue` are context lines, and every `'streamlabs' =>` / `streamlabs:` key is unchanged |
| C4 | CONFIRMED | `StreamLabsServiceDriver.php:130,135 @64a7e3c` - labels `Streamlabs Donations Received`, `Total Streamlabs Amount (session)`; same @HEAD (lines 131,136) |
| C5 | CONFIRMED / UNVERIFIABLE (compound) | First half: `resources/help/reference/integration-controls/streamlabs.md:11,16 @64a7e3c` carry both labels, unchanged since @HEAD. Second half ("regenerated by `help:build-integration-controls` rather than hand-edited") is a provenance statement; the tree holds the output, not how it was produced, so no revision can decide it |
| C6 | CONFIRMED | `tests/Feature/IntegrationControlsReferenceTest.php:33-36` runs `help:build-integration-controls --check` and asserts success; `php artisan test --filter='IntegrationControlsReferenceTest\|IntegrationEventTagDocsTest'` @HEAD: 10/10 passed in this file |
| C7 | CONFIRMED | `tests/Feature/IntegrationEventTagDocsTest.php:50-66` asserts every `'event.x' =>` literal in each registered driver appears in `resources/help/reference/eventsub-tags/*.md`, datasets over `ExternalServiceRegistry::services()`; same run @HEAD: 10/10 passed |
| C8 | CONFIRMED | `resources/js/utils/services.ts:11 @64a7e3c^` - `streamlabs: 'Streamlabs'`, file not in the diff, same @HEAD; `'StreamLabs'` occurs 0 times @64a7e3c in `ExpressionBuilder.vue`, `EventsFeed.vue`, `events.vue`, `recents.vue`, `stream-sessions.vue` |

### Surface
Complete.

### Findings
- **F1** compound claim, half uncheckable under its tag - C5 is tagged [code] but joins a checkable fact (the two labels in `resources/help/reference/integration-controls/streamlabs.md @64a7e3c`) to a process claim (that the file was produced by `help:build-integration-controls`, not hand-edited) that no revision can confirm; a remedy claim should split it, keeping the labels as [code] and either dropping the provenance half or tagging it [unverified] (C6 already carries the checkable form: `--check` reports no drift).

### Notes
- C6 and C7 were run @HEAD, not @64a7e3c: a worktree of the shipped commit could not boot against the current `vendor/` (`Stevebauman\Location\LocationServiceProvider` not found; package removed in OL-2609-097). Both test files are unchanged since 64a7e3c, as are `integration-controls/streamlabs.md` and `streamlabs-donation-event-tags.md`; the driver changed only in f8361f19 (OL-2609-096), with its labels and `event.source` intact.
- Unchanged lines hold: `ExternalEventTemplateMapping::SERVICE_EVENT_TYPES` reads `'Streamlabs Donation'` at `app/Models/ExternalEventTemplateMapping.php:141 @64a7e3c`, not in the diff; no migration is in the diff; `routes/web.php`, `routes/api.php`, `SpeakableText`, `TwitchApiService`, `KofiServiceDriver` are not in the diff.
- OL-2609-077 cites the `StreamLabs` casing of `[[[event.source]]]` as a contract established here, which is consistent with C1/C2.
