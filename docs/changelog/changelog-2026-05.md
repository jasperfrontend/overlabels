# CHANGELOG MAY 2026

## May 24th, 2026 - docs(design): Lists data-bus (M8) design strawman

Cooked the full A-to-Z design for M8 (Lists realtime diff broadcasts) into a standalone strawman at `docs/design/lists-data-bus.md`, so the idea can be reviewed cold before any code is written. Grounded it in a sweep of the actual Lists subsystem (every mutator, the snapshot system, the current `ListUpdated` broadcast, and how `OverlayRenderer.vue` consumes it).

- Documents the wire format (a new `ListMutated` event: surgical `op` + size-guarded `after`, no `before`), the full op catalog mapped to the exact mutator that emits each delta, the new per-list channel `lists.{twitch_id}.{slug}`, and the external-consumer auth + REST-bootstrap story.
- Two deliberate deviations from the original memory note, both justified in-doc: dropped `before` from the wire (redundant for stateful consumers, and the main Reverb 10 KB payload risk), and reused the OverlayAccessToken model for external-consumer channel auth (the memory never addressed how a third party gets onto a private channel).
- Calls out five real decisions (D1-D5) with options, my pick, and the tradeoff, plus an honest scope note: the "~1 day" core is only the half that talks to our own renderer; the channel + auth + REST bootstrap + reference consumer is the part that actually makes it a bus.
- Linked from MILESTONES.md M8.

## May 24th, 2026 - docs(milestones): refresh the roadmap to match reality and capture outstanding work

`MILESTONES.md` had drifted badly out of date (last touched May 4th) - it still listed the Twitch bot as pending and didn't mention the donation integrations, the Stream State Machine, Controls, Lists, Recipes, or Alert Targeting at all. Rewrote it from a memory + code sweep so the doc reflects what actually shipped and what's genuinely outstanding.

- Brought the Completed section up to date: added collapsed entries for the StreamLabs/StreamElements/Fourthwall integrations, the Stream State Machine, the Controls/Alert-Targeting/Lists core, and the Recipes engine (steps 1-5); moved the Twitch Bot to Completed.
- Added a priority-ordered Active Roadmap: M8 Lists realtime data bus (top priority - the before/after/op diff broadcast), M9 Recipes producer layer + Bot Expression hardening, M10 Flows, M11 Patreon, M6 Community, M7 IRL/GPS.
- Added a Backlog section for loose bugs and polish, with a "Recently cleared" line recording the recent fixes: GDPR admin delete, RAF/60 FPS in every browser, idle Expression Controls no longer recalculating, and the stream-sessions aggregation discrepancy (a query bug multiplying output numbers per event).
- Converted em dashes to spaced hyphens per house style.

## May 23rd, 2026 - ops(deploy): flush the Redis app cache automatically on every deploy

The tag-picker bug below was only "fixed" in prod after a manual `php artisan cache:clear` - the code fix alone left stale, shape-incompatible entries serving from Redis until their TTL lapsed. That manual step is exactly the kind of thing that gets forgotten, so deploys now self-correct.

- `docker-entrypoint.sh` gained an `ENTRYPOINT_RUN_CACHE_CLEAR` flag block (same pattern as migrations / storage:link / help-index / og-generate). When set it runs `php artisan cache:clear`, tolerant of failure so a clear hiccup never blocks a deploy.
- The flag is set on the `web` role only in `config/deploy.yml`, so it fires once per deploy rather than on every role's container boot.
- Safe in this topology: `SESSION_DRIVER=database` means sessions live in Postgres, so clearing the cache never logs anyone out; and Redis already runs `--save "" --maxmemory-policy allkeys-lru`, so the cache is non-persistent and evictable - the app already tolerates a cold cache on any Redis restart. The only cost is a lazy, per-user re-fetch of the long-lived Twitch snapshot after a deploy, which already happened on restarts anyway.
- Replaces the short-lived `cache-clear` kamal alias idea, which was redundant - the clear belongs in the deploy lifecycle, not as a command someone has to remember to run.

## May 23rd, 2026 - fix(tags): TemplateTagsList showed no tags after the Laravel 13 upgrade

The tag picker in the template editor (Tags tab on `/templates/*/edit` and `/create`) rendered "No tags available" for everyone, on localhost and prod alike, even though the underlying tags were intact - the `/tags` generator page listed them fine. Root cause was the Laravel 13 upgrade setting `serializable_classes => false` in `config/cache.php` (a hardening default). `TemplateTagController::getAllTags()` caches the output of `getOrganizedTemplateTagsForUser()`, which contains Eloquent models (`'category' => $category`) and a `Collection` (`'tags' => ...->map()`). With object deserialization refused, the cache read those back as `__PHP_Incomplete_Class`, so the JSON response emitted `"tags": {"__PHP_Incomplete_Class_Name":"Illuminate\\Support\\Collection"}` - an object, not an array. The frontend's `Array.isArray(categoryData.tags)` guard in `processTags()` then dropped every category, leaving the list empty.

Why it hid so well: the `/tags` page calls the parser directly (no cache), so it was never affected, and the test suite runs `CACHE_STORE=array` (the array driver never serializes), so all 52 tag tests passed against the broken code. Only the serializing drivers used in dev/prod (`database`) tripped it.

- `getAllTags()` now normalizes the parser output with `json_decode(json_encode($organized), true)` before caching, so the cached payload is arrays and scalars only. This honors the `serializable_classes => false` lockdown, round-trips cleanly through any cache driver, and produces a byte-identical response shape to before the upgrade.
- Verified the other `Cache::remember`/`put` sites are object-free: the Twitch snapshot caches decoded-JSON arrays, the GPS GeoJSON builds a pure nested array (the `DB::select` stdClass rows are only read for scalars), and help/lockdown/slug caches store arrays or scalars. The tags cache was the only casualty.
- Existing poisoned cache entries persist until their 1-hour TTL expires. Dev caches were cleared during the fix; prod needs `php artisan cache:clear` after deploy (or it self-heals within the hour).

## May 21st, 2026 - fix(gamejam): bounded game log so the live broadcast can't exceed Reverb's limit

Chat Castle froze mid-stream in room 5 (round 49) when chat was hammering the door with `!a`. Root cause: `GameStateChanged` broadcasts the full game snapshot every round, and that snapshot embeds `Game::$log`. `GameLog::append()` was documented as a "rolling window for the live ticker" but never actually trimmed `log` - it grew append-only, identical to `recap`. By room 5's end the snapshot's wrapped Reverb events request was sitting at ~9.5 KB (95% of the default 10 KB `max_request_size`), so the busy door-attack round - with every active joiner's vote populated - tipped it over. Reverb returned 413, the `BroadcastException` ("Payload too large") propagated out of `ResolveGameRound` after the round's state had already committed, the job failed, and the overlay never got the frame. The authoritative state advanced but the on-stream board froze.

- `GameLog::append()` now trims `log` to the last `GameLog::LIVE_LOG_LIMIT` (30) entries, so the broadcast snapshot stays bounded regardless of game length. `recap` remains append-only for the end-of-game post-mortem. This is the behavior the docblock already promised; it just was never implemented.
- `ResolveGameRound::handle()` now wraps `GameStateChanged::dispatch()` in a try/catch. The round's state is committed before the broadcast, so a broadcast failure (oversized payload, transient Reverb hiccup) now costs at most one stale overlay frame instead of failing the job and freezing the game. Failures log as `gamejam.broadcast.failed`.
- The frontend live ticker (`gamejam/live.vue`) replaces its log from each snapshot and auto-scrolls to newest, so a 30-entry rolling window is exactly the intended behavior.
- Tests: the old `GameLogTest` case that asserted "log and recap both grow unbounded" (it encoded the bug) is replaced with one asserting `log` is capped while `recap` keeps the full history, plus a case verifying the newest entries are kept and the oldest roll off.

## May 21st, 2026 - UX: contextual breadcrumb on directly-opened templates

Opening a template with no recorded navigation - a fresh tab, a pasted `/templates/505` URL, or the redirect straight after creating one - collapsed the first breadcrumb crumb to the generic "My overlays". The list context that powers that crumb lives in `sessionStorage`, which is per-tab and dies when the tab closes, so any direct visit had nothing to read and fell back to the generic label. There was never a recoverable "My static overlays" history in those flows; it only ever existed if you arrived via the filtered index page.

Rather than lean on fragile session state, the crumb is now *derived* from the template itself when no navigation was recorded.

- New `deriveListContext()` in `useListContext.ts` builds a `{ title, href }` from the template's `type` + ownership, mirroring the index page's own filter labels (`ownerMap`/`typeMap`). A directly-opened static overlay you own now reads "My static overlays > name" and links to `/templates?filter=mine&type=static`; someone else's public alert reads "Public event alerts > name".
- `captureListContext()` gained an optional `derived` argument. Precedence is unchanged where it matters: a previously frozen origin wins, then the live list you navigated from (`GLOBAL_KEY`), then the derived crumb, then the generic fallback. So the "came from this list" semantic still holds when you actually arrived via the index - the derived crumb only fills the gap on a cold visit.
- `show.vue` passes `ownedByMe: props.canEdit` (which is exactly `auth id === owner_id`); `edit.vue` passes `ownedByMe: true` since the edit page is owner-only.

## May 20th, 2026 - UX: drop the screenshot gate on overlay creation

Creating an overlay no longer requires a screenshot first. The previous "Add a screenshot to enable Create" gate was backwards: you can't screenshot a finished overlay until you've saved it at least once, so it blocked the exact first save it depended on. Screenshots are now optional at create time and can be added any moment after, as they always could on the edit page.

- `create.vue`: removed the `screenshot_url` short-circuit in `submitForm`, the disabled-until-screenshot state on both Create buttons, and the "Add a screenshot to enable Create" hint. The Screenshot tab copy now reads "Optional" instead of "Required" and drops the asterisk.
- `OverlayTemplateController::store`: `screenshot_url` validation relaxed from `required` to `nullable`, and the Cloudinary `claim()` call is now guarded so it only runs when a URL is actually present.
- `TemplateCodeEditor.vue`: `template` prop made optional so the create page (which has no saved template yet) can mount the editor. Fixes a TS2345 error. The OBS tab - which generates a browser-source URL that can't exist before save - is now hidden when no template is present.

## May 20th, 2026 - UX: post-delete redirect always matches the breadcrumb

Follow-up to the April 4th contextual breadcrumbs work. Deleting a template from show/edit was supposed to return you to the filtered list you came from (e.g. "My static overlays"), but in some situations it dumped you on the unfiltered `/templates`. Root cause: the breadcrumb read the saved list context once at mount, while the delete redirect re-read `sessionStorage` live at click time, and that key is only ever (re)written by the index page. Any path that re-filtered the index after you landed (notably browser back -> change filter -> forward), or that entered show/edit from a non-index page (kit page, events list, dashboard), let the two reads drift apart, so the breadcrumb could say "My static overlays" while the redirect went to `/templates`.

- New `useListContext` composable owns the single sessionStorage key and the read/write/freeze logic (previously duplicated inline across `index.vue`, `show.vue`, and `edit.vue`).
- show/edit now FREEZE the list context into a per-template key (`template_origin:<id>`) on first mount. The frozen value is immune to later index re-filtering and to Inertia re-running `setup()` on browser back/forward.
- The breadcrumb and the post-delete redirect now read that same frozen value, so they can no longer disagree. The redirect target is passed from the page into `useTemplateActions` via `redirectAfterDelete` instead of being re-read from sessionStorage.
- The per-template key is cleared after a successful delete.

## May 20th, 2026 - npm security: patch `ws` (Dependabot) + `brace-expansion`

Dependabot flagged `ws` (GHSA-58qx-3vcg-4xpx, "uninitialized memory disclosure", medium) on the default branch. It's a transitive dep: `socket.io-client -> engine.io-client -> ws@8.18.3`, used server-side by the StreamLabs/StreamElements Node listeners (the browser uses native WebSocket, so the bundle is unaffected).

- `engine.io-client@6.6.4` pins `ws` at `~8.18.3` (< 8.19.0), so a plain `npm update` can't reach the patched 8.20.1. Forced it via an npm `overrides` entry: `"ws": "^8.20.1"`. `ws` 8.18 -> 8.20 is a same-major patch bump, API-compatible.
- While in the lockfile, `npm audit fix` also cleared `brace-expansion` (GHSA-f886-m6hf-6m8v / GHSA-jxxr-4gwj-5jf2, ReDoS/DoS), a dev-only transitive of eslint and vue-language-core - not shipped to production.
- `npm audit` now reports 0 vulnerabilities. `npm run build` green.

## May 20th, 2026 - Laravel 13 upgrade

Laravel 12 drops to security-only fixes after Aug 13, 2026 (EOL Feb 4, 2027), so moving to Laravel 13 ahead of that. The upgrade turned out unusually clean: every third-party package already ships Laravel 13 support at its current version, so only the framework itself and tinker needed constraint changes.

### Dependency changes (`composer.json`)

- `laravel/framework` `^12.0` -> `^13.0` (resolved to v13.11.2)
- `laravel/tinker` `^2.10.1` -> `^3.0` (resolved to v3.0.2)
- Transitive: Symfony 7.4 -> 8.0 across the stack, guzzle/flysystem/prompts minor bumps. Nothing else moved - sanctum, socialite, inertia, reverb, telescope, banhammer, stevebauman/location, ziggy, collision, ide-helper, pest, and phpunit were all already L13-compatible.

### Code/config changes

The upgrade guide's breaking changes almost entirely missed our surface area (no `upsert`, no `JobAttempted`/`QueueBusy` listeners, no domain routes, PostgreSQL not MySQL, no Bootstrap pagination, no morph pivots, no custom contract implementations, OAuth auth so no password-reset flow). Two deliberate config touch-ups:

- `config/sanctum.php`: swapped the deprecated `ValidateCsrfToken` alias for the new `PreventRequestForgery` middleware class (L13's CSRF middleware rename).
- `config/cache.php`: added `'serializable_classes' => false` to match L13's hardened default. Safe because we only cache arrays/scalars (webhook logs, lockdown state, rate-limit buckets, slugs, tokens), never PHP objects.

### Not changed (deploy notes)

- Cache/session key prefixes left on the explicit underscore form. L13's default switches to hyphenated suffixes; adopting that would rename the session cookie and log everyone out, so `SESSION_COOKIE`/`CACHE_PREFIX` stay pinned via existing config.

### Verification

- Full Pest suite green under Laravel 13.11.2: 780 passed, 2129 assertions.
- `composer audit` clean; `php artisan route:list` resolves all 326 routes; Pint clean on changed files.

## May 20th, 2026 - Symfony security patch (8 CVEs)

`composer audit` flagged 8 advisories published today against the Symfony 7.4.8 components Laravel 12 pulls in. Patched ahead of the planned Laravel 13 upgrade since these affect the current production stack and shouldn't wait.

- `composer update "symfony/*" --with-all-dependencies` - no `composer.json` constraint changes, lock-only bump within Laravel 12's allowed ranges. Notably `symfony/http-kernel` 7.4.8 -> 7.4.12, `symfony/yaml` -> 8.0.12, plus console/mailer/mime/routing/translation/string.
- CVEs closed: CVE-2026-45075 (HEAD request bypasses `methods` filter on `#[IsGranted]`/`#[IsCsrfTokenValid]`), CVE-2026-45068 (argument injection in SendmailTransport), CVE-2026-45070 + CVE-2026-45067 (email header / SMTP command injection via Mime), CVE-2026-45065 (UrlGenerator route-requirement bypass -> off-site URL injection), CVE-2026-45304 (symfony/yaml).
- `composer audit` now reports no advisories. Full Pest suite green (780 passed, 2129 assertions).

## May 20th, 2026 - `!followage` and `!accountage` chat commands

A user flagged that StreamElements has these and Overlabels' bot didn't - a real shortcoming for streamers who want chatters to be able to check how long they've followed or how old their Twitch account is, without having to fall back to a second bot. Adding both as proper builtins.

### Helix wrappers

- `TwitchApiService::getChannelFollower($accessToken, $broadcasterId, $followerUserId)` - thin wrapper around `GET helix/channels/followers?broadcaster_id=&user_id=`. Returns a single follower row (or null when the relationship doesn't exist). Pinned by `user_id` so the response is at most one row, no enrichment overhead, no pagination concern.
- `TwitchApiService::getUserByLogin($accessToken, $login)` - resolves a typed chat handle (e.g. `targetuser`) to the full Helix user record. Used when the chatter targets someone other than themselves: `!followage @someone` or `!accountage @someone`.

### Duration formatter

- New `app/Support/HumanDuration.php`: `HumanDuration::between(Carbon $from, Carbon $to, int $maxUnits = 5)` returns strings like `"5 years, 4 months, 12 days, 9 hours, 4 minutes"`. Walks top-down through calendar units (year -> second), skips zero-valued units entirely, plural-correct, returns `"just now"` for a zero span. Calendar units rather than seconds-per-month math means a follow that landed on 2020-02-29 reads "5 years, 2 days" on 2025-03-02 instead of drifting from accumulated 30.44-day approximations.

### `!followage`

- `BotFollowageController` at `POST /api/internal/bot/followage`. Body: `{ channel_login, chatter_id, chatter_login, chatter_display_name, target_login? }`. Resolves the channel owner, optionally resolves a target user via Helix users (app token), then hits `helix/channels/followers` with the **broadcaster's** user token + the existing `moderator:read:followers` scope (no new scope migration needed - we already request it for every opted-in streamer).
- Self-bounce: when the queried user is the broadcaster, returns `"you own this channel, so the follow date isn't all that meaningful"` (or the third-person variant for targeted lookups). Twitch auto-follows broadcasters to themselves on signup, so without this every broadcaster gets a useless follow-date readout.
- Not-following case: `"@TargetUser doesn't follow this channel yet"` (or first-person variant for self).
- Following case: `"@TargetUser has been following for 5 years, 4 months, 12 days"` (or first-person variant).
- Unresolvable target login: `"no twitch user named @nobody_here"`.

### `!accountage`

- `BotAccountageController` at `POST /api/internal/bot/accountage`. Body: `{ chatter_id, chatter_login, chatter_display_name, target_login? }`. No channel_login needed - account age is global Twitch data. Resolves the target via Helix users with the app access token (no broadcaster scope required, account creation dates are public).
- Self: `"your account was created 7 years, 2 months ago"`.
- Target: `"@TargetUser's account was created 4 months ago"`.

### Bot side (overlabels-bot repo)

- Two new builtin handlers: `src/commands/followage.js` and `src/commands/accountage.js`, registered in `commands/handlers.js` Map. Each handler strips a leading `@` off the optional first arg, POSTs to the corresponding endpoint, and speaks the returned `reply` inline (no outbox round-trip, since these are read-only). Silent when `reply` is null - that's the server's signal for "nothing useful to say". Standard `OverlabelsApiError` handling.
- `fetchFollowage()` and `fetchAccountage()` added to `src/overlabelsApi.js` next to the other internal-endpoint helpers.

### Migration

- `2026_05_20_120000_seed_followage_accountage_bot_commands` seeds both commands at `permission_level: 'everyone'` for every `bot_enabled` user. Idempotent via `firstOrCreate`. Streamers who want to lock either down can edit the row from the bot settings UI.

### Tests

- `tests/Unit/HumanDurationTest.php`: zero span, singular nouns, zero-unit-skipping, top-down multi-unit breakdown, max-units cap, reversed-arg swap.
- `tests/Feature/BotFollowageTest.php`: missing channel owner, broadcaster self-query, follow-found, follow-not-found, target_login resolution, target_login no-match, missing internal secret.
- `tests/Feature/BotAccountageTest.php`: self via id, target via login, target no-match, missing internal secret.
- 18 new tests, all green.

### Help docs

- `resources/js/pages/help/bot/Commands.vue` Miscellaneous section now lists both commands with examples + `[@user]` syntax notes. Same time: extended the misc section's template to render `cmd.notes` (it was the only command-list section dropping notes silently).

## May 19th, 2026 - Search, filter, and paginate recent events

`/dashboard/recents` showed a flat list of the 50 most recent events with no way to slice it. With weeks of stream history accumulating, finding "that one Ko-fi from last Tuesday" meant scrolling. Adding proper filters + pagination.

### Backend - unified paginated query

- `DashboardController` now builds a UNION ALL across `twitch_events` and `external_events` (excluding `gps`), normalized to one shape, wrapped in `fromSub()` so cross-table `ORDER BY created_at DESC` works under Laravel's standard `paginate()`. The original `mergeRecentEvents()` helper still backs the dashboard tile (5 events, no filters), so nothing else regressed.
- Four filter dimensions, all optional: `search` (Postgres `ILIKE` against `event_data::text` and `normalized_payload::text` so the full JSON is searchable), `source` (twitch or any external service the user has events from), `event_type` (any type the user has actually received), and `range` (hour / 24h / 7d / 30d / all). Source = `twitch` short-circuits the external query with `1 = 0` and vice versa, so the union still works but contributes zero rows from the other side.
- New `eventFilterFacets()` helper returns the distinct sources + event_types actually present in the user's history. The frontend uses these to populate the dropdowns - no point showing a "Ko-fi" filter to someone who's never received a Ko-fi event.
- `recentActivity` (full page) paginates at 20/page, `recentEvents` (embed) paginates at 25/page. Both call `withQueryString()` so paginator links preserve active filters.

### Recents page (`dashboard/recents.vue`)

- New "Filters Section" matching the visual language of `templates/index.vue`: 4-column grid on `lg`, 2-column on `md`, single column on phones. Search input is debounced 300ms; selects fire on change. URL stays canonical (defaults like `range=all` are stripped from the query string so back/forward navigation doesn't bounce between equivalent forms).
- `watch(props.filters)` resyncs the form when Inertia restores props on browser back/forward - same pattern templates/index.vue uses.
- Pagination component appears at the bottom only when `last_page > 1`. Empty state copy adapts to "no events match your filters" instead of the original "no events yet" since filters can now produce empty results from a non-empty history.

### Embed page (`dashboard/events.vue`)

- Filters live behind a "Filters" toggle button next to "Refresh", closed by default - the priority on the embed view is screen real estate for the actual event list while streaming. Opening it reveals the same 4 controls in a 2-column grid (single column on phones).
- Pagination at the bottom of the list, like every other paginated list in the app. Same `<Pagination>` component, just sized down to fit the narrower max-width.
- Refresh / info button behavior preserved.

## May 19th, 2026 - Per-list, per-action chat permissions for !list

Until today, `!list` was gated by a single moderator+ check in `BotListActionController`, hardcoded against `ListMetaCommand::PERMISSION_LEVEL`. That was fine when the meta-command only existed for destructive operations, but with the new `search` / `searchall` / `random` verbs there's real value in letting viewers run safe lookups while keeping `clear` / `draw` / `pop` mod-only. The single hardcoded gate prevented that. Replacing it with per-action permissions configurable per list.

### Storage shape

- New nullable `jsonb` column `chat_permissions` on `option_sets`. NULL = use service-side defaults (everything moderator+). When non-null, holds a partial map `{action: level}` of overrides only - actions matching the default get dropped at save time so the JSON stays minimal, and an all-defaults submission persists as NULL.
- The full known-action set is defined in `ListActionService::ACTION_DEFAULTS` (12 actions: count, first, last, random, search, searchall, pop, draw, clone, clear, disable, enable). Levels are the standard `BotChatGate::TIER_ORDER` strings (everyone / subscriber / vip / moderator / broadcaster).

### Service gate

- `ListActionService::handleInvocation` gained a fourth parameter `?array $badges = null`. `null` = bypass (dashboard path - the request has already been authorised as the list's owner). An array (even empty) = gate is active and the action's resolved permission level is checked against the chatter's badges via `BotChatGate::hasPermission`.
- The gate runs only for known actions in `ACTION_DEFAULTS` - bare `!list`, slug-only, and unknown-action help text always pass through. Gating help would be confusing ("you can't see what you can't see").
- On denial, the service returns a friendly `@user - 'count' on 'q' is moderator+ only.` reply so viewers understand why their `!list q random` worked but `!list q clear` didn't. The bot still gets a `{fired: true, reply: ...}` JSON response so the reply lands in `bot_chat_outbox`.
- Two helper methods: `resolvePermission(OptionSet, string)` returns the merged level for a single action; `resolveAllPermissions(OptionSet)` returns the complete merged map (used by `ListController::serialize` so the dashboard always sees a full picture without having to know the defaults itself).

### Bot map

- `BotCommandController` now announces `permission_level: 'everyone'` for `list_meta` instead of `moderator`. Necessary - otherwise the bot pre-gates `!list` at the IRC layer and viewer messages never reach our server, defeating any per-action `everyone` overrides. The trade-off is that every `!list` from any chatter hits us now, but `!list` isn't a high-frequency surface so the extra HTTP volume is negligible. Server gate stays authoritative.

### Controllers

- `BotListActionController` - dropped the static `BotChatGate::hasPermission(ListMetaCommand::PERMISSION_LEVEL, $badges)` block; now passes badges straight into the service.
- `ListActionWebController::runAction` - passes `null` badges (bypass) since the dashboard caller has already cleared `authorizeOwnership`.
- `ListController` - serialize includes the resolved permission map; update accepts `chat_permissions` as a focused PATCH (same pattern as `disabled` and the expiry fields). Validates that levels come from `BotChatGate::TIER_ORDER`; defaults are dropped at save time and an all-defaults payload persists as NULL.

### Dashboard UI

- The "Actions" panel on `/dashboard/lists` is now a 2-column grid on `md+` screens. Left column: existing button stack, unchanged. Right column: "Allow viewers in chat" header + four sub-groups (Inspect / Pop/draw / Whole list / State) of checkboxes, one per action, grouped to mirror the left column's structure.
- Binary toggle: checked = `everyone`, unchecked = `moderator`. The underlying storage holds the full level string so a future UI can expose subscriber/vip without a migration.
- Auto-saves on each toggle via the focused `lists.update` PATCH. Optimistic local update + revert on error + brief "Saving…" hint while the request is in-flight. Plain HTML checkboxes (per the existing project convention - Reka's checkboxes have had issues).

### Tests

- `ListActionTest`: 7 new service-level cases (default-action denial, opened-action runs, other-actions-on-same-list-still-gated, dashboard-bypass, unknown-action-help-bypasses-gate, resolveAllPermissions merge). 2 existing cases updated (the old single-gate refusal flipped into a friendly per-action denial; the command-map permission level updated to 'everyone').
- `ListControllerTest`: 3 new PATCH cases (override-with-defaults-dropped, all-defaults-becomes-null, unknown-level-422).
- 146/146 green across all `List*Test` suites.

### Manual UI verification

- Production vite build clean, `vue-tsc` no errors in the file. I have not exercised the checkbox UI in a browser - please verify the toggle save + revert behaviour and the layout on mobile (`md` breakpoint flips to single-column).

## May 19th, 2026 - !list search / searchall for quotes-style lookups

A viewer on stream today tried `!list q search <name>` after adding a quote via a chat-append command, expecting the same search vocabulary they'd had on their previous bot (Wizebot's `!quote search`). The Overlabels meta-command didn't have a `search` verb, so the lookup silently failed and they had to scroll the dashboard. Adding `search` + `searchall` to `ListActionService` so the meta-command speaks that vocabulary too.

### Behaviour

- Syntax: `!list <slug> search <keyword>` (top match) and `!list <slug> searchall <keyword>` (all matches).
- Keyword is everything after the action verb; multi-word keywords join with spaces (`!list q search how are` searches for the literal `"how are"`).
- Case-insensitive substring (`stripos`). Not fuzzy/Levenshtein - the streamer's mental model of "find quotes containing X" is far closer to grep than to spell-correct, and similar-but-wrong matches would just be noise for quote/raffle/copypasta lists.
- Iteration is newest-first (end of the `items` array, which is append-order). So `[1]` is always the most recent match.

### Output shape

```
search   -> First search results for 'jasper' in list 'q', searched from new to old: Hello my name is Jasper and this is my quote.
searchall -> All search results for 'jasper' in 'q': [1]Hello my name is Jasper... [2]Hi Jasper how are... [3]Yo Jasper what's good!
```

`searchall` packs entries in newest-first order until the next entry would push the reply past the existing 400-char `MAX_REPLY_CHARS` cap, then appends `(+N more)` so the viewer knows the list is truncated rather than missing matches.

### What changed

- `app/Services/Lists/ListActionService.php` - added `search` and `searchall` to the `ACTIONS` array, two cases in the `match` dispatch, two new private methods, and updated both help messages (`helpMessage` and `listHelpMessage`) so `!list` and `!list <slug>` advertise the new verbs.
- `tests/Feature/ListActionTest.php` - 8 new cases covering: newest-first top match, multi-word keyword join, no-matches reply, empty-keyword help, empty-list friendly reply, newest-first numbered output, MAX_REPLY_CHARS cap + `(+N more)` suffix, and the two empty/keyword guards for `searchall`. All 39 tests in the file pass.
- No frontend changes - the dashboard's Actions section already routes through the same `runAction` web endpoint, so streamers can hit `search` from the dashboard via the same vocabulary (button UI deferred until someone actually asks for it).

## May 19th, 2026 - Optional success reply on list append

Until today, list-append chat commands were silent on success by design, with the lone chat-facing exception being `args_empty_reply` for the rejection case. That left the streamer with no clean way to confirm an append to the chatter who triggered it - they had to glance at the overlay or trust silence. Added a sibling field, `success_reply`, that resolves the same template language and queues into `bot_chat_outbox` after the append commits.

### What changed

- Migration `2026_05_19_170904_add_success_reply_to_list_appenders` adds a nullable `string(500)` after `args_empty_reply`. NULL preserves the existing silent-by-default behaviour, so no existing appender changes shape.
- `App\Models\ListAppender` - added `success_reply` to `$fillable` and the docblock.
- `App\Services\Lists\ListAppendService::fire()` - after the successful append branch (history written, `last_fired_at` stamped, `ListUpdated` broadcast), resolve `success_reply` against the same `$context` used for `value_template` and write to `bot_chat_outbox` when the resolved string is non-empty. Empty resolved string still no-ops to match the `args_empty_reply` contract. New return shape `['fired' => true, 'value' => ..., 'reply' => ...]` documented in the method's docblock.
- `App\Http\Controllers\ListAppenderController` - validation rule `success_reply => nullable|string|max:500`, persisted on `store` and `update`, threaded through `serialize()`.
- `App\Http\Controllers\Api\Internal\BotListAppenderController` - docblock updated to reflect both outbox-bound replies (success + args-empty).
- `database/factories/ListAppenderFactory` - defaults `success_reply` to null.
- `resources/js/pages/dashboard/lists/index.vue` - extended the `AppenderRow` interface, form state, open/edit/save body, modal field (placed directly above the empty-args reply for natural reading order), and a chat-bubble hint line on each appender card that shows the configured reply when present.
- 16/16 existing `ListAppenderTest` / `ListExpiryTest` / `ListReadTagsTest` cases still green.

## May 19th, 2026 - Remove the boss-gate on door damage in Chat Castle

First Chat Castle stream test ran the night of May 18. Players reported the game felt unresponsive and "stopped registering commands" toward the end. The @todo on `ActionApplier::attack()` had flagged the boss-gate as a suspected cause ("this causes game crashes and I can't pinpoint yet how or why"), but the postmortem in the database doesn't support that theory and the gate had downsides anyway, so removing it.

### What the DB showed about last night

- Game 32 ran cleanly to round 67. `last_resolved_at` advanced normally; `failed_jobs` had zero entries for the window.
- All three rooms played (1-3) had only `regular` zombies. No bosses. Zero `boss_blocked` events in the 36-entry recap. The boss-gate code never executed.
- The 4m 35s of "silence" in room 3 (20:22:53 hp_pickup -> 20:27:28 bomb tile) was the player wandering across the room with no logged state changes - votes were splitting between directions, `!a` attacks were silent no-ops because nothing was in adjacency range, and the bot was silently dropping 37% of commands on a per-channel cooldown (separate fix shipped today in `overlabels-bot@00f6542`).

### What the boss-gate actually was

`ActionApplier::attack()` lines 199-209 filtered `$doorsHit` for closed doors in 1-tile adjacency, then bailed with a `LOG_BOSS_BLOCKED` entry if any boss was alive in the current room. Two problems with keeping it:

1. The rolling ticker is capped at 30 entries (`Game::LOG_TICKER_LIMIT`). A boss with 30 HP and players spamming `!a` while not in melee range of the boss would fill the ticker with `boss_blocked` and push real events out. Combined with the bot cooldown bug this is almost certainly what felt like "the game stopped responding" in earlier sessions.
2. The design beat it was trying to land ("you must kill the boss before you can leave") was already implied by every room placing a boss between the player and the exit. Explicit gating on top added friction without adding clarity.

### What changed

- `app/Services/Gamejam/ActionApplier.php` - deleted the boss-gate block. Doors in adjacency now always take damage when `!a` connects (modulo the existing zombies-in-range short-circuit, which still hits zombies first if any are reachable).
- `app/Models/Game.php` - removed the now-unused `LOG_BOSS_BLOCKED` constant.
- `tests/Feature/GameLogTest.php` - the "boss alive prevents door damage and logs boss_blocked" test inverted into "door takes damage even when a boss is alive elsewhere in the room", same fixture.
- `resources/js/pages/gamejam/live.vue` - dropped the `boss_blocked` cases from the colour switch, the formatter switch, and the ticker-entry CSS.
- 5/5 `GameLogTest` and 21/21 `BotGamejamActionTest` green. `node --check` style lint clean on the Vue file.

## May 17th, 2026 - Server-side TTS via ElevenLabs (Kaylin, voice of Overlabels)

The browser `speechSynthesis` path was producing wildly inconsistent voices across viewer machines (whatever the OS default happened to be), reading the donor's chosen sentence in a flat narration voice that fought the alert's vibe instead of supporting it. Replaced wholesale with ElevenLabs Flash 2.5, server-rendered, with Kaylin as the single brand voice for all Overlabels TTS.

### How the new flow works

1. `AlertExpressionRenderer::render()` (unchanged) resolves the `tts_expression` template to a final sentence at alert dispatch time.
2. `broadcast(new AlertTriggered(...))` fires immediately with a new `alert_id` UUID so the overlay can correlate.
3. If the resolved sentence is non-null, `SynthesizeAlertTts::dispatch($alertId, $twitchId, $text)` is queued. The job calls `TtsService::synthesize()`, which either returns a cached mp3 URL (content-addressed by `sha256(text + voice + model)` under `storage/app/public/tts/`) or hits ElevenLabs, writes the mp3, and returns the URL.
4. On success the job broadcasts `TtsAudioReady` on the same `alerts.{twitch_id}` channel, carrying `{alert_id, audio_url}`.
5. The overlay's new `handleTtsAudioReady` looks up the alert by `alert_id`, calculates `remaining = ttsDelayMs - (now - firedAt)`, and schedules `new Audio(url).play()` after that delay. Audio that arrives after the delay window has already elapsed plays immediately.

### Why queued (and why client-side delay scheduling)

Synchronous synthesis would have held `AlertTriggered` for 700ms-2s while ElevenLabs answered, making alerts feel laggy. Queued synthesis runs in parallel with the alert's SFX, so for typical 1-2s SFX the voice lands at exactly `tts_delay_ms` (zero perceived latency). Worst case the audio arrives slightly after the planned delay - still correct, just delivered when ready. The `alert_id` correlation is what lets the overlay enforce the delay client-side instead of `->delay()`-ing the synthesis job and wasting parallelism.

### Invariants preserved from the old browser-TTS path

- Server resolves the expression before broadcast (tags still never reparse on the client - donor message content cannot inject template tags).
- 500-char cap on the resolved sentence (in `AlertExpressionRenderer`).
- `tts` boolean control mute gate: streamer creates a boolean control with key `tts` and value `0` to suppress all TTS. Now suppresses synthesis entirely (no API spend) since the resolved string is `null` and the job is never dispatched.
- Cancel-prior-utterance on rapid alerts: replaced `speechSynthesis.cancel()` with a singleton `HTMLAudioElement` pattern matching `playAlertSound`.

### Overlays never phone home (still true)

The overlay receives a pre-baked public mp3 URL on the broadcast and plays it with `new Audio()`. Same trust boundary as alert SFX - no API keys in the browser, no telemetry, no synthesis calls from the overlay. ElevenLabs only talks to the queue worker.

### Cost model

ElevenLabs Starter ($5/mo) unlocks Kaylin (a "professional" library voice that free-tier API access forbids). Cache hits cost zero credits, so repeat sentences like "X just followed!" are free after first synthesis. Weekly `tts:cleanup` schedule evicts `tts/*.mp3` older than 7 days; a cache miss just re-synthesizes. No per-user budget enforcement for now (3 users, 1 active); the plan is to gate TTS upgrades behind Ko-fi support tiers when it matters.

### Files

- `app/Services/Tts/TtsService.php`: ElevenLabs wrapper with `synthesize()` + `listVoices()`. Failure logs and returns null - alerts must fire even when ElevenLabs is down.
- `app/Console/Commands/TtsListVoices.php`: `php artisan tts:list-voices [--filter=name]` for discovering voice IDs at setup.
- `app/Jobs/SynthesizeAlertTts.php`: queued, `$tries = 1` (no retries - permanently-malformed input shouldn't burn credits looping).
- `app/Events/TtsAudioReady.php`: new broadcast event, `tts.ready` on `alerts.{twitch_id}`.
- `app/Events/AlertTriggered.php`: `alert_id` is now a required constructor arg + ships in the broadcast payload. `tts_text` removed from the broadcast payload (synthesis happens server-side; overlay receives audio URL instead).
- `resources/js/components/OverlayRenderer.vue`: `speakTts()` replaced by `rememberPendingTts()` + `handleTtsAudioReady()` with the singleton `HTMLAudioElement` pattern.
- `config/services.php`: new `elevenlabs` section reading `ELEVENLABS_API_KEY` / `ELEVENLABS_VOICE_ID` / `ELEVENLABS_MODEL_ID`.
- `routes/console.php`: weekly `tts:cleanup` schedule entry.

## May 16th, 2026 - channel:bot in streamer OAuth + bot sends as app token

Two coordinated changes (one here, one in the overlabels-bot repo) so the shared @overlabels chat bot account qualifies for Twitch's Chat Bot Badge on outgoing messages.

### Why

Per [Twitch's Chatbot Badge and Chat Identity docs](https://dev.twitch.tv/docs/chat/), to display the Chat Bot Badge a bot must (1) call the Send Chat Message API, (2) authenticate that call with an app access token, (3) have `channel:bot` granted by the broadcaster OR moderator status in the channel, and (4) not be the channel's own broadcaster. Overlabels already met (1) and (4); the bot's sends were signed with its user access token rather than an app token, and broadcasters never granted `channel:bot` because we never asked for it.

### What changed here

`TwitchScopeService::REQUIRED_SCOPES` now includes `channel:bot`. The Connect Twitch flow at `routes/web.php:287` reads this constant directly, so new authentications request the scope without any further wiring. Existing users surface in the stale-scope detection that already drives the re-auth nudge - no migration, no forced sign-out.

### What changed in the bot repo

`apiClient.asUser(botUserId, ctx => ctx.chat.sendChatMessage(...))` -> `apiClient.chat.sendChatMessageAsApp(botUserId, broadcasterId, text, options)`. The bot still authenticates its EventSub WebSocket reads with the user token (no change there), but outgoing replies now use Twurple's app-token send path, which `RefreshingAuthProvider` services automatically via the client id/secret it already holds.

### Failure mode during the transition

Connected streamers who haven't re-auth'd (and who haven't manually modded the @overlabels bot account) will see bot replies return 401 from Helix until they refresh scopes. The stale-scope nudge surfaces this. Accepted as a closed-beta-acceptable trade against keeping a user-token fallback path and its branching.

### What this does not unlock

This change qualifies the bot for the Chat Bot Badge only. Appearing under "Chat Bots" in Users in Chat additionally requires Channel Chat Message reads via EventSub Webhooks (not WebSockets) - the bot still reads via WebSockets, and that's a separate, larger refactor not bundled here.

## May 16th, 2026 - RAF-batched time-dependent expression evaluation

With the CSS fast path landed, the lissajous overlay rendered in Chromium without crashing but ran at ~5fps and spammed `Uncaught (in promise) Maximum recursive updates exceeded. This means you have a reactive effect that is mutating its own dependencies and thus recursively triggering itself.` on every frame. The CSS work was no longer the bottleneck - the bottleneck was Vue's effect scheduler refusing to converge.

### Root cause

Time-dependent Expression Controls (anything using `now()` / `now_ms()`) each had their own `watchEffect`. Each watchEffect both **read** `data.value` (via `buildContext`) and **wrote** `data.value` when its result string changed. With 4+ expressions ticking on a shared RAF, each write triggered the other watchEffects to re-run. `now_ms()` had advanced by a few microseconds in between, so `resultToString` produced a different float string, which wrote `data.value` again, which retriggered the other watchEffects... Vue tripped its `MAX_TRIGGER_RECURSION_LIMIT` guard and aborted the flush mid-stream, leaving most of the work undone for that frame.

### Fix

Time-dependent expressions are no longer wired into Vue's reactivity. They're evaluated in a single RAF-driven loop:

- `tickFrame()` builds the data context once, iterates every time-dependent entry in the registry, evaluates against the shared context, collects results into a single patch object, and performs **one** `data.value = { ...data.value, ...patch }` write per frame.
- `registerExpression()` branches: time-dependent expressions are stored in the registry with `stop: null` (no watchEffect); data-dependent expressions still create a watchEffect as before. The `timeTick` ref and its `void timeTick.value` read inside the watchEffect are gone entirely.
- Initial value is seeded synchronously at registration time so the first frame doesn't render with an empty slot.
- The RAF loop self-dispatches with `requestAnimationFrame(tickFrame)` and the existing `timeDependentCount` accounting starts/stops it when the registry transitions to / from zero time-dependent entries.

### Trade-off

A change to a data dependency that a time-dependent expression reads (e.g. `now_ms() * c.speed`) now propagates on the next RAF tick (<=16ms latency) instead of synchronously inside the same microtask. This is the same latency the old code effectively gave - `timeTick` only advanced on RAF anyway - so there's no observable regression.

Data-dependent expressions (no `now*()` call) keep their `watchEffect`. Their writes converge because identical results don't propagate (the `!==` check), and they were never the source of the cascade.

### Verified

10 time-dependent expressions at 60fps in OBS Browser Source / Edge / Helium. No console spam.

## May 16th, 2026 - CSS custom-property fast path for overlay tag updates (Phase 1 surgical patching)

Even after filtering unused expressions, the user's lissajous overlay still ticked the full render pipeline 60 times a second: every data update triggered `compiledCss` to re-run `replaceTagsWithFormatting` across the entire CSS string, then `injectStyle` destroyed and re-created the `<style>` element, then `compiledHtml` re-parsed the entire HTML, then morphdom diffed the rebuilt DOM against the live one. The HTML output is byte-identical every frame for CSS-variable animations - all of that work produces the same DOM. This is Phase 1 of the surgical-patching plan: compile the CSS once at mount, rewrite every `[[[tag]]]` into a `var(--ol-...)` reference, inject the stylesheet exactly once, and write CSS custom properties on the root element for tag value changes instead of replacing the `<style>` element.

### Compiler (`resources/js/utils/tagParser.ts`)

- New `compileCssBindings(source)` returns `{ fastPath: true, css, bindings }` where `css` is the rewritten stylesheet and `bindings` is a `Map<tagKey, Array<{property, pipe, suffix}>>`. Same `[[[key|pipe]]]` syntax, single regex pass.
- **Property naming**: each unique `(key, pipe, suffix)` tuple gets its own property, slug-derived (`c:lissajous_x` + pipe `round:2` + suffix `px` -> `--ol-c-lissajous-x-round-2-px`). The same tag emitted with two different unit suffixes resolves to two distinct properties with the unit pre-baked.
- **Suffix handling is essential, not optional**: the user's CSS writes `[[[c:lissajous_x|round:2]]]px`, which under a naive rewrite becomes `var(--ol-...)px` - **invalid CSS**, because you cannot append a unit to a `var()` reference. The compiler captures any `[a-zA-Z%]+` glued to the right of `]]]` and bakes it into the property's *value* at write time, so `setProperty('--ol-...-px', '50.42px')` produces the unit correctly inside the cascade.
- **Bail conditions** (fall back to slow path):
  - CSS contains `[[[if:`, `[[[elseif:`, or `[[[foreach:` - those select between text segments and can't be reduced to a single `var()` reference.
  - A placeholder is glued to a non-boundary character on its left (e.g. `#[[[hex]]]`). Handling left-side prefixes would require folding them into the property value too; punted to a follow-up.

### Applier (`resources/js/composables/useCssCustomProperties.ts`, new)

- `install(table, locale)` stores the bindings + locale; `applyAll(data)` iterates the table, formats each value with the bound pipe, and writes via `document.documentElement.style.setProperty(property, value + suffix)`.
- Internal `lastWritten` cache short-circuits writes when the formatted value didn't change for that property - a 60Hz expression tick that rounds to the same two decimals as last frame costs zero DOM work.
- Empty formatted value -> property set to empty (suffix dropped), so a missing data slot leaves `var(--ol-...)` falling back through the CSS cascade cleanly instead of writing `'px'` as a property value (which would parse as invalid length and silently nuke the rule).
- `teardown()` removes every property the applier ever wrote from `:root`, called from `onUnmounted`.

### Renderer wiring (`resources/js/components/OverlayRenderer.vue`)

- New state: `cssApplier` (the composable instance), `cssOnFastPath: Ref<boolean>`, `cssCompiledStatic: Ref<string>`.
- `compiledCss` computed short-circuits to `cssCompiledStatic.value` when `cssOnFastPath` is true, so it no longer subscribes to `data.value` at all - the computed never re-runs after mount on the fast path. `watch(compiledCss, ...)` no-ops on fast path.
- Mount-time block (after expression controls are registered, so initial expression values are in `data.value`): `compileCssBindings(css.value)` -> if fast-path, install the table, set `cssOnFastPath = true`, seed every bound property from the current data snapshot.
- New `watch(data, applier.applyAll, { flush: 'post' })` drives property updates on every `data.value` reassignment. On the slow path this is a no-op because the applier has no bindings.

### Verified

User reported the lissajous overlay rendering at 60fps in OBS Browser Source with 10 time-dependent expressions (5 pairs, each 500ms offset), where previously even 4 expressions crashed Chromium-family browsers entirely. CSS custom properties visible in DevTools update at the screen refresh rate; the `<style id="overlay-style">` element is constant across frames.

## May 16th, 2026 - Skip unreferenced Expression Controls on overlay render

User reported an overlay with 4 lissajous-curve Expression Controls (each using `now_ms()`) crashing the Chromium-family browsers - OBS Browser Source, Edge, Helium - while Firefox rendered it fine. Tracing showed all 4 expressions ticked on RAF and cascaded through Vue's reactivity even when only 2 were referenced in the HTML/CSS, because the renderer registered every expression the user owned on the template, regardless of whether the template actually used the value.

- **Backend filter** (`OverlayTemplateController::renderAuthenticated`): expression controls are now collected into a `$expressionsByKey` map (broadcast key -> expression + dependencies) instead of straight into the payload. After the controls/lists loops we build `$referencedControlKeys` from `template_tags` (any tag starting with `c:`) and BFS the dependency closure - an expression ships in the response only if its `c:<broadcastKey>` is referenced in the template, or another shipped expression transitively depends on it. The existing Twitch-tag allowlist widening (`extractTwitchTagReferences` loop) still operates on the filtered list, so `t.<name>` references inside unused expressions no longer widen the data payload either.
- **Why dependency-closure, not direct-reference**: chained expressions like `c.A = c.B * 2; c.B = now_ms() / 1000` where only `c.A` is rendered still need `c.B` registered for `c.A` to resolve. The BFS walks every shipped expression's `config.dependencies` so the transitive set is correct by construction.
- **Known limitation**: alert templates that reference `c:<key>` are not considered here - those tags live on the alert template, not the static one. Rare in practice; flagged in a comment for follow-up if anyone hits it.
- **Verified by the reporter**: leaving the 2 unreferenced controls in the user's account no longer crashes the overlay in OBS Browser Source.

## May 16th, 2026 - Dashboard UI for `list_writer` controls

- Closes the "code works on prod, but you have to tinker" gap from the prod smoke test. The whole pipeline (sidecar -> recompute -> ListWriterAppend -> list growth) has been live and tested end-to-end since this morning; this slice surfaces it to the dashboard so streamers can wire bindings without `php artisan tinker`.
- **Backend** (`OverlayControlController`): new `list_writer` branch in `store` and `update`. Both call a shared `validateListWriterConfig()` private helper that verifies `config.source_control_id` exists in this template OR is user-scoped (matching how Expression Control dependencies resolve), and `config.target_list_id` exists and belongs to the user. Cross-user attempts get rejected with 422.
- **List prop threading**: `OverlayTemplateController::show()` and `::edit()` now query the user's `OptionSet`s and pass them as a new `userLists` Inertia prop (light projection: id, slug, label, items_count, disabled — not the full items array). `templates/show.vue` and `templates/edit.vue` accept the prop and forward to `ControlsManager`, which forwards to `ControlFormModal`.
- **Modal** (`ControlFormModal.vue`): new `<option value="list_writer">` in the type dropdown. New config block (only visible when type=list_writer) with two `<select>`s — source picker reuses the existing `availableWatchControls` computed (same pool the Expression Builder uses, with the template-scoped vs user-scoped dedup), target picker reads the new `userLists` prop. A new `listWriter` ref holds the picker bindings; `buildPayload()` branches on `list_writer` to assemble `config = { source_control_id, target_list_id }` and `value = null`; the watch-on-open handler populates the pickers from an existing control's config when editing or copying. Empty-state hint when the user has no lists yet, pointing at the Lists dashboard.
- **Manager** (`ControlsManager.vue`): `list_writer` added to `TYPE_LABELS` ("List writer") and `TYPE_ORDER` (after expression). The row's `configSummary()` special-cases `list_writer` to show `source label → list label`, looked up via two new helpers `lookupControlLabel()` and `lookupListLabel()` that fall back to `(control #ID)` / `(list #ID)` when the referenced row isn't in scope. The `[[[c:key]]]` snippet button is hidden on `list_writer` rows since they have no renderable value.
- **Tests**: 8 new feature tests in `tests/Feature/ListWriterControlEndpointTest.php` covering POST happy path (counter source + list target), missing source_control_id rejection, cross-user source rejection, cross-user target rejection, user-scoped service control accepted as source, PUT update of both fields, PUT update rejected when new source is foreign, label-only PUT update succeeds without re-validating the config. 737 tests green overall.
- **Not yet eyeballed in a browser** - the Vite build is clean and the existing lint warnings are pre-existing (unused `index` in v-for, untouched by this diff), but I haven't actually clicked through the modal. Streamer should eyeball the picker UX before this is in front of users.

## May 16th, 2026 - Wire expression-engine into dev + Kamal + CI

- Three small edits to make yesterday's sidecar actually run, both locally and on the Linode box.
- **composer dev script**: added `node expression-engine.mjs` as a fifth `concurrently` stream alongside server / queue / vite / reverb. `composer run dev` (the `crd` alias) now starts the sidecar with the rest. Picks up the secret from `.env`.
- **Kamal accessory**: new `expression-engine` accessory in `config/deploy.yml`, same shape as the existing streamlabs/streamelements listeners. Port binding is `127.0.0.1:3010:3010` so it's only reachable from the host (not the public internet); the app container reaches it via Docker DNS (`http://overlabels-expression-engine:3010`). 128m memory limit. `EXPRESSION_ENGINE_HOST=0.0.0.0` inside the container, `EXPRESSION_ENGINE_PORT=3010`.
- **App env**: `EXPRESSION_ENGINE_URL` and `EXPRESSION_ENGINE_TIMEOUT_MS` added to the app's `env.clear` block; `EXPRESSION_ENGINE_SECRET` added to `env.secret`. The Laravel client picks up the URL through `config/services.php`.
- **GitHub Actions**: new "Build & push expression-engine" step in the deploy workflow (same shape as the listener builds, with `cache-from`/`cache-to` scoped to its own cache key). `EXPRESSION_ENGINE_SECRET` added to the Kamal-deploy step's env block and to the `.kamal/secrets` writer loop. Repo secret already set, so no additional action required.

## May 16th, 2026 - Expression engine sidecar + `list_writer` control type

Substantial architecture change: Expression Controls now evaluate server-side, persistently, through a shared-code sidecar. The yesterday-shipped `option_sets.source_control_id` binding got unwound in favour of a first-class control type. Together these unblock the user's flagship use case ("save the result of any control value into a list, including Expression Controls") which yesterday's slice explicitly couldn't do.

### Shared expression engine: one codebase, two runtimes

- New `resources/js/lib/expression-engine/engine.mjs` is the **single source of truth** for Overlabels expression semantics. Pure functions: jsep parsing, AST evaluation, the built-in function library (`latest`, `argmax`, `oldest`, `round`, `sqrt`, `atan2`, `now`, etc.), context building, result stringification. No Vue, no DOM, no I/O.
- `useExpressionEngine.ts` is now a thin Vue wrapper over the shared lib: imports `evaluate` / `buildContext` / `resultToString` / `containsNowCall` and adds the reactive `watchEffect` + RAF ticker for time-dependent expressions. Behaviour unchanged on the overlay side; lint and full Vite build pass.
- This is the load-bearing decision: parity between the overlay and the server side is by construction, not by parallel implementations and parity tests. Adding a function like `argmax` is one PR touching one file; both runtimes pick it up at deploy time.

### Sidecar service

- `expression-engine.mjs` at the repo root, alongside the existing `streamlabs-listener.mjs` and `streamelements-listener.mjs`. Node 22, no new framework dep (Node built-in `http` instead of Express - the surface is two endpoints).
- Bound to `127.0.0.1` by default. `EXPRESSION_ENGINE_SECRET` required at boot; missing secret = hard exit so we can't accidentally run an unauthenticated evaluator.
- Endpoints: `POST /evaluate` (X-Internal-Secret gated, JSON in/out, parse error / payload-too-large / 403 / 500 each have explicit responses), `GET /health` (Kamal liveness probe).
- `Dockerfile.expression-engine` for Kamal accessory deployment; same shape as the other sidecars.
- Graceful SIGTERM shutdown so Kamal restarts don't drop in-flight requests.
- Smoke-tested locally: `c.wins + 1` -> `6`, `latest(...)` picks the most recent timestamped value, parse errors come back as `{ ok: false, error: { code: "parse_error" } }`, missing secret returns 403.

### Laravel client + recompute listener

- `App\Services\Controls\ExpressionEngineClient` is a thin HTTP wrapper over the sidecar. Timeout-bounded (default 2s). On any failure (connection error, non-2xx, malformed response) returns `null`; callers treat null as "skip this update" rather than crashing - the overlay still computes locally, persistence just doesn't refresh until the next dep update succeeds.
- `App\Listeners\RecomputeExpressionControls` subscribes to `ControlValueUpdated`. When any control updates, finds every Expression Control owned by the same user whose `config.dependencies` includes the updated broadcast key, ships each through the sidecar, stores the new value on the row, and dispatches a cascade event so downstream listeners (`list_writer`, alerts, overlay broadcast) see the new value.
- Cascade walk happens inside one handler invocation. Cascade events are flagged with a new `alreadyRecomputed: bool` field on `ControlValueUpdated`; the recompute listener bails when it sees the flag (it already walked the descendants in-process). Other listeners ignore the flag.
- Termination guaranteed by the save-time cycle check (dep graph is a DAG). `MAX_DEPTH = 10` is the defensive backstop in case a row ever leaks past the cycle check.
- Scope limit: data context is built from `overlay_controls` only. Expressions referencing `t.<tag>` (Twitch template-tag values) see those as undefined for now - the real Twitch tag data lookup is a follow-up.

### `list_writer` control type

- New entry in `OverlayControl::TYPES`: `'list_writer'`. Config carries `source_control_id` (the control whose value to record) + `target_list_id` (the list to append to). The `value` column on the row is null - this is a side-effect control, not a renderable one.
- `OverlayControl::isListWriter() / listWriterSourceId() / listWriterTargetId()` accessors keep the storage shape readable from callers.
- `App\Listeners\ListWriterAppend` subscribes to `ControlValueUpdated`, finds list_writers pointing at the updating control (via `whereJsonContains('config->source_control_id', ...)`), and appends. Same FIFO drop logic as yesterday's listener: "keep latest N" intent on cap, not silent refusal.
- Cross-user safety: defence-in-depth check rejects appends to a list owned by a different user (the dashboard will prevent it once the UI ships, but the listener also enforces).

### The unwind: drop `option_sets.source_control_id`

- Yesterday's binding lived briefly on the List. The list_writer control type supersedes it: bindings are Controls now, which means they fit naturally into the existing Control management surface (edit, clone, recipe-manifest-export, etc.) and stop being a one-off field on a different model.
- New migration drops `option_sets.source_control_id` (the column, the unique index, the FK).
- `App\Listeners\AppendControlValueToList` deleted. Its test file deleted. `OptionSet::detectListBindingCycle()` deleted (the cycle vector it guarded against is irrelevant when the binding lives on the control side - the existing Expression Control cycle check covers everything).
- The model-level validator that rejected Expression Control sources is gone. Expression Controls work as `list_writer` sources now (that's the whole point).

### Tests + ops

- 10 new Pest tests in `tests/Feature/ListWriterTest.php`. Happy path with raw counter + service-managed source, FIFO cap, disabled list skip, no-binding no-op, cross-user safety, and four cases exercising the recompute path: Expression Control recomputes through the sidecar and ends up in the bound list, chained `A -> B` cascade through two Expression Controls, sidecar-returns-null safely no-ops, and the `alreadyRecomputed` flag prevents re-walking. 729 tests green overall.
- Uses a `FakeExpressionEngineClient` test double bound in the container so tests don't need the sidecar process running.
- Required new env vars (production): `EXPRESSION_ENGINE_URL` (default `http://127.0.0.1:3010`), `EXPRESSION_ENGINE_SECRET` (must be set), `EXPRESSION_ENGINE_TIMEOUT_MS` (default 2000).
- Kamal accessory wiring not yet in `config/deploy.yml` - that's a follow-up so we can review the deploy shape together before flipping production over.

## May 15th, 2026 - Control-to-List binding: migration + listener + cycle guard (no UI yet)

- First slice of "Lists can subscribe to a Control's value stream." Every time the bound Control fires a `ControlValueUpdated` event, the new value gets appended to its bound List automatically. Closes a real gap - until now Controls had no way to store their value over time, so things like "latest donor across all services" or "every win this stream" died as soon as the underlying control updated.
- **Schema**: new `option_sets.source_control_id` column - nullable FK to `overlay_controls` with `nullOnDelete`, plus a unique constraint so a Control can feed at most one List. The 1-to-1 cardinality keeps the model deterministic; broadcast fan-out is a future feature.
- **Listener `App\Listeners\AppendControlValueToList`** subscribes to `ControlValueUpdated`. Resolves the User from `broadcasterId` (twitch_id), the Control row from `(key, source, overlaySlug)` with the right disambiguation for user-authored keys shared across templates, then appends the event's value to the bound List inside a row-locked transaction. FIFO drop when `max_items` is hit (control-driven appends always succeed, unlike chat appenders which silently refuse on overflow - the streamer's intent with a capped binding is "keep latest N"). Broadcasts `ListUpdated` on success.
- **Cycle guard** `OptionSet::detectListBindingCycle()` is a DFS walking the source control's expression dependencies, following both control-to-control deps AND list-to-source-control deps via a new `c.list.<slug>` extractor. Depth-capped at 5, visit-set to short-circuit revisits. The realistic cycle vectors don't exist yet (see Expression Control limit below) but the guard is in place so the day they DO exist, this isn't a silent landmine.
- **Model-level validator** rejects two failure modes at write time with `InvalidArgumentException`: (a) binding to an Expression Control - because the expression engine (jsep) runs only in the overlay, the server has no computed value to append when the source updates, so it would just store stale data; (b) binding to a control owned by a different user. Catches both bad writes regardless of who's setting the field (controllers, factories, tinker, future UI).
- **The Expression Control limit is the load-bearing v1 constraint.** The user's flagship motivating example was an Expression Control aggregating donor names across services (`latest(c.kofi.latest_donor_name_at, c.kofi.latest_donor_name, ...)`); that exact use case doesn't work yet because the server never sees the computed value. Two paths forward when we revisit: (1) build a server-side mini-evaluator for Expression Controls (jsep equivalent in PHP, finite scope - just enough to recompute on the listener path); (2) ship server-side broadcasts of computed values from the overlay (rejected per the "overlays never phone home" feedback memory). For now, users CAN bind raw Controls and service-managed Controls; the donor-history use case works one-service-at-a-time.
- **12 new Pest tests** in `tests/Feature/ControlToListBindingTest.php` cover happy path (raw and service-managed controls), FIFO cap, disabled-list skip, no-binding no-op, source-deletion nulls the binding, Expression Control rejection, cross-user rejection, DB-level uniqueness, direct cycle detection, no-false-positive on raw controls, transitive cycle through an intermediate expression. 731 tests green overall.
- **No UI surface yet.** The mechanism is fully working end-to-end against the event bus, but binding is currently set programmatically (controller wiring + dashboard checkbox come next). Power users could bind via tinker today.

## May 15th, 2026 - /help/aliases + drop the "deaths" example everywhere

- New <Link to /help/aliases>: dedicated docs page for Bot Aliases, modeled on /help/lists' layout (TOC, sections, things-to-know panel, quick-reference card). Covers concepts (what an alias is, two-layer dispatch), creation paths (dashboard + `!ol alias add`), the placeholder grammar ({1}/{2}/{*}), permission and one-hop rules with a worked "viewer fires `!hardreset` aliased to `!reset`" denial trace, all four options, four full worked examples including the negative-amount trick (`!w -2` -> `!inc wins -2`), and a quick-ref card at the bottom. Linked from /help/bot/commands' "Your own commands" grid (previously pointed at a self-anchor `#ol`) and added to the /help index card grid with a CornerDownRight icon.
- **"Deaths" purged from the user-facing copy.** It had snuck into Controls.vue (six conditional examples, one counter description, one snake_case example), bot/Commands.vue (two of my own examples shipped yesterday), and two stray internal comments (OverlayControl.php docblock, useConditionalTemplates.ts comment). Mood-shifting framing too: the "deaths >= 10 -> 'tilted' / focused" expression became "wins >= 10 -> 'on_fire' / warming_up"; the three-branch conditional ("Struggling tonight..." / "Getting rough." / "Still alive!") became ("On a tear tonight!" / "Building momentum." / "Just getting started."); "this run is falling apart" became "this run is incredible." Two test files (BotInternalApiTest, ExpressionControlTest) renamed their `'deaths'` literals to `'wins'` since the keys are just test data; 75 tests in those files still green, 719 overall.

## May 15th, 2026 - Help page: /help/bot/commands rewritten for the new command surface

- The bot grew a lot in the last 48 hours (Bot Aliases, `!ol` chat-admin meta-command, `!list` integration, four user-defined command families) and the existing /help/bot/commands page only documented the original eight control commands - in big vertical cards that took ~150px each. Rewriting now while the surface is still fresh.
- **Density**: every command renders as a one-line `<details>` row by default, expanding on click to show the example chat trace and any notes. Native HTML disclosure widget with a ChevronRight that rotates on open; no Vue state to manage. Mobile-friendly (the row wraps cleanly), full-width on desktop.
- **Information architecture**: seven sections instead of one giant list - Controls (8), Controls-access switch (2), `!ol` chat-admin (10 subverbs), `!list` meta-command (1, links into /help/lists for the verb vocabulary), Your own commands (overview grid pointing at /help/expressions, /help/bot/commands#ol, /help/lists#appenders, recipes), Miscellaneous (`!ping` plus a pointer to /help/gamejam for the Chat Castle verbs), and Permission tiers. TOC at the top jumps to each.
- **`!ol` section** has its own options-vocabulary panel at the bottom listing all four option keys (cooldown / permission / enabled / hidden), the permission shortforms (mod, sub, vip, bc, all), and the boolean variants (true/false/on/off/yes/no/1/0). Explicit note that aliases respect the target's permission after rewrite, so an alias to `!reset` doesn't escalate.
- **Controls-access warning** kept but compacted - the long version still shows on first visit and dismisses to a one-liner with a "Show details" link. localStorage-backed (`help.bot.commands.controlsWarningAck`). Also clarified that `!ol` and `!list` work regardless of the controls-access switch (they're a different surface).
- **Tier badge palette** reused for visual consistency: green/sky/pink/amber/violet for everyone/sub/vip/mod/broadcaster. Badges shortened (Sub+, VIP+, Mod+) so they don't push the summary off the line on mobile.
- Visual verification still pending - lint and Vue compilation are clean but I haven't rendered it in a browser; the streamer should eyeball before this lands on prod.

## May 15th, 2026 - Test: pin the `!ol cmd add !name` bang-stripping behaviour

- New Pest case in `BotChatAdminTest` covering the realistic "mod types the leading `!` by accident" scenario. Verifies single-bang on `cmd add`, double-bang on `cmd delete`, and bang-on-alias-name all normalise to a clean stored name. Two layers already enforced this (the service's `stripBang()` plus the validator's own ltrim+regex), but the behaviour wasn't pinned by a test - so a future refactor could quietly regress it.

## May 14th, 2026 - `!ol` chat-admin: create commands and aliases without leaving Twitch

- New chat-admin meta-command lets moderators (or the streamer) manage Bot Expressions and Bot Aliases from the Twitch chat without opening the dashboard. Targets near-feature-parity with StreamElements' `!command add/edit/delete/options` flow but namespaced under `!ol` so it doesn't collide with SE, Wizebot, Nightbot or Streamlabs Cloudbot, all of which already own `!command` / `!cmd` / `!commands`. The namespace also leaves room for future Overlabels subverbs (`!ol kit install`, `!ol overlay refresh`, ...) without claiming more top-level command slots.
- **Surface** (full v1 grammar):
  ```
  !ol cmd add    <name> <payload>
  !ol cmd edit   <name> <payload>
  !ol cmd delete <name>
  !ol cmd options <name> <option> <value>
  !ol alias add    <name> <target_template>
  !ol alias edit   <name> <target_template>
  !ol alias delete <name>
  !ol alias options <name> <option> <value>
  !ol list [cmd|alias]
  !ol help [cmd|alias|options]
  ```
  Options are `cooldown` (0-86400s), `permission` (everyone/sub/vip/mod/broadcaster, with short forms accepted), `enabled` (true/false), `hidden` (true/false). Permission shortforms canonicalise: `mod -> moderator`, `sub -> subscriber`, `bc -> broadcaster`, `all -> everyone`.
- **Architecture**: new `POST /api/internal/bot/manage` endpoint sits next to `/list-actions/fire` in the bot's internal API. The bot relays the parsed `(subject, action, name, payload, option, value)` shape; the backend dispatches on `(subject, action)` to one method per verb. Validation runs through the same `BotExpressionValidator` / `BotAliasValidator` services the web settings controllers use, so a chat-side typo can't bypass what the dashboard would have rejected. Reply lines (success messages, validation errors, "no command named X") get queued to `bot_chat_outbox` and spoken by the existing outbox poller. Same shape as the `!list <slug> <action>` flow.
- **Validator extraction**: pulled the inline validation out of `BotExpressionsController::validatePayload()` and `BotAliasesController::validatePayload()` into `App\Services\Bot\BotExpressionValidator` and `BotAliasValidator`. The web controllers now delegate; the new `BotChatAdminService` calls the same methods. Same rules: regex on command names, reserved-word collision check (builtins), duplicate detection per user, self-loop and alias->alias chain blocks for aliases, placeholder syntax check (`{1}`/`{2}`/`{*}` only). 80 existing bot/expression tests still green after the refactor.
- **Permission model**: `!ol` is a builtin command with `permission_level: moderator`. The bot enforces the chat-side gate (mod or broadcaster only); the backend re-checks via `BotChatGate::hasPermission()` for defence in depth (silent block returns `{queued: false, reason: 'gate'}`). The target command's own permission after rewrite still applies, same as plain Bot Aliases - a mod creating `!ol alias add r reset stuff` won't escalate, because `!reset` is broadcaster-only and stays broadcaster-only at runtime.
- **Seed migration** adds `!ol` to `BotCommand::DEFAULTS` (moderator) and backfills the row for every existing bot-enabled user; new sign-ups pick it up via `BotCommand::seedDefaults()`. Idempotent (`firstOrCreate`).
- **Bot-side handler** (`overlabels-bot/src/commands/handlers.js::olAdmin`) parses `!ol <subject> <action> ...` into the structured payload and POSTs to `/api/internal/bot/manage` via new `submitChatAdmin()` API helper. Stays silent on the wire (outbox poller speaks the reply); logs at info level for telemetry. Unknown subjects forward as `subject: help` so the user sees a usage line rather than silence.
- **Tests**: 15 new Pest tests in `tests/Feature/BotChatAdminTest.php` covering add/edit/delete/options for both cmd and alias paths, builtin collision rejection, self-loop rejection on alias, permission canonicalisation, cooldown range check, list output, help line, mod gate (`{queued: false, reason: 'gate'}` for non-mod chatters), unknown-channel handling, and the unauthenticated 403.
- **Bot-side work shipped** alongside in the bot repo - the contract doc at `docs/bot-aliases-bot-contract.md` from yesterday already laid out the dispatch pattern; this commit follows the same shape (silent on success, outbox poller speaks, badges forwarded with the request).

## May 14th, 2026 - Bot aliases: bot-side dispatch shipped, feature is live

- The `overlabels-bot` repo now handles `type: "alias"` entries from the command map. Aliases save in Overlabels and fire end-to-end through chat as of this commit. The earlier Bot Aliases changelog entry (below) shipped the backend + frontend only - this one closes the loop.
- **Backend touchup**: `BotCommandController::index()` now includes `cooldown_seconds` in alias map entries. Other command types either ride server-side cooldown (expressions) or the bot's hardcoded default (builtins), but aliases have no fire endpoint and need to enforce cooldown bot-side, so the value has to travel in the map.
- **Bot-side**: dispatch tree extracted into an inner `processParsedCommand(parsed, event, aliasDepth)` so the alias branch can recursively re-dispatch the rewritten command without duplicating routing logic. `aliasDepth` parameter caps recursion at 1 hop - if a rewrite somehow lands on another alias (stale map data) it's logged and dropped, defending against alias->alias chains in case backend validation drift ever lets one slip through. `event` is passed through unchanged so reply threading still targets the chatter's original message and badge checks still see the original user.
- **Naming collision fixed**: bot had a hardcoded `const ALIASES = new Map([['inc', 'increment'], ['dec', 'decrement']])` shorthand table at the top of `registry.js`. Renamed to `BUILTIN_SHORTHAND` so "alias" unambiguously means the per-user feature throughout the bot codebase. The shorthand still works (and composes nicely - an alias's `target_template` can target `inc` and the shorthand layer expands it to `increment` on re-parse).
- **Cooldown**: new `createDynamicCooldown()` factory that accepts the window at check time instead of at construction time. Aliases use this with their per-row `cooldown_seconds * 1000`; existing builtin cooldown gate is untouched. Broadcaster bypass and "log on block" logic match the existing helper.
- **Substitution**: `expandAliasTemplate(template, args)` does the `{1}`/`{2}`/`{*}` rewrite then `parseCommand('!' + expanded)` re-parses it back into `{ name, args }`. Trim + collapse-whitespace on the expanded string handles users putting awkward spaces in their templates. Smoke-tested six cases including empty args, `{*}` capture, multi-positional, and shorthand chain.

## May 14th, 2026 - Bot aliases: short rewrites that expand to longer commands

- New "Bot aliases" feature alongside Bot Expressions. An alias is a short command (e.g. `!w`) that rewrites to a longer one (e.g. `!increment wins {1}`) before the bot dispatches it. Closes the long-standing "I want a one-letter shortcut for the command I type 40 times a stream" gap without forcing me to author full expressions for every shortcut.
- **Schema**: new `bot_aliases` table mirroring `bot_expressions`, with `target_template` replacing the `expression` text field. Permission default is `moderator` (vs. expressions' `everyone`) because aliases are typically mod tooling, but the dropdown allows any tier - the target command's own permission still gates after rewrite, so even an everyone-permitted alias to `!reset` would fail at the second hop for non-broadcasters. Unique constraint on `(user_id, command)`; index on `(user_id, enabled)`.
- **Substitution syntax**: positional placeholders only. `{1}` `{2}` ... `{N}` substitute the chatter's Nth whitespace-split arg, `{*}` substitutes all remaining args. No `[[[bot:args.0]]]` style - kept deliberately simple because the bot does substitution and re-dispatch entirely client-side. A separate fire endpoint would have meant a Laravel round-trip per alias use for no real benefit.
- **One-hop rule**: aliases can target builtins (`!increment`, `!set`, `!reset`, ...) and the user's own expressions, but **not other aliases**. Backend rejects alias->alias chains at save time with a clear validation error on `target_template`, and the bot is documented to drop chain dispatches if stale map data ever leaks one through. Self-loops also rejected.
- **Command map (`GET /api/internal/bot/commands`)** now emits `type: "alias"` entries that carry `target_template` so the bot can substitute and re-dispatch without calling back to Laravel. Resolution order on collision is `builtin > expression > alias > recipe_trigger > list_append > list_meta` - aliases sit just under expressions because both are user-authored, with expressions winning since they're the more direct match. Backend validation refuses colliding rows at save time, so the order only matters as a tiebreaker for stale data.
- **Settings UI** at `/settings/bot/aliases` with the same shape as `/settings/bot/expressions`: Index lists rows with `!command -> !target_template`, permission/cooldown/enabled badges, and edit/delete actions. Edit page has the command field, the target template field with quick-insert chips for `{1}`/`{2}`/`{3}`/`{*}` plus a "Target a command" expander listing builtins and the user's own expressions for one-click insertion, a live example block showing how a sample callsite resolves, the permission/cooldown row, the two toggles (enabled, hide from `!commands` listing), and an info panel restating the one-hop and badge-context rules.
- **Linked from `/settings/integrations`** as a sibling line under the bot section, paired with the existing "Manage expressions" link. Both surface only when the bot is enabled.
- **Bot-side dispatch lives in `overlabels-bot`** - this commit ships the backend and frontend only. Contract written up at `docs/bot-aliases-bot-contract.md` covering the new map shape, the substitution algorithm, the one-hop and chatter-context invariants, three worked examples, and the optional v2 `last_fired_at` touch endpoint that isn't built yet. Until the bot ships its `type: "alias"` handler, saved aliases sit in the map but don't fire - they're inert, not broken.

## May 14th, 2026 - Account deletion: self-serve + admin "delete_all" now actually deletes

- Three related changes that together fix the "delete leaves a restorable ghost" bug and give streamers a way to nuke their own account without emailing support.
- **Admin `delete_all` strategy is now a hard delete.** `AdminUserController::destroy` previously called `$user->delete()` for every strategy, which under the SoftDeletes trait only stamped `deleted_at`. Result: a user whose content was wiped via `delete_all` still appeared in `/admin/users?include_deleted=true` with a "deleted" badge that read like a role, and could be `restore()`d into a hollow zombie account with login intact but no data. Now `delete_all` routes through the new `UserDeletionService::eraseAccount()` which `forceDelete()`s the row inside a transaction. `delete_content` and `assign_ghost` still soft-delete since their restore semantics remain meaningful (content survives or was reassigned to ghost).
- **New `app/Services/UserDeletionService.php`** centralises the full erase flow: detach kits/templates pivots, delete owned templates and their controls/event-mappings, delete user-scoped controls/tags/categories/integrations, revoke access tokens and eventsub subscriptions, then `forceDelete()`. Used by both admin `delete_all` and the new self-delete endpoint so the two paths can't drift.
- **Self-delete on `/settings/account`.** New `App\Http\Controllers\Settings\AccountController@destroy` bound to `DELETE /settings/account` (`settings.account.destroy`). Requires the request body to contain `confirmation: "DELETE ACCOUNT"` exactly. Logs the user out, invalidates the session, then calls `eraseAccount()`. Returns to `/` with a confirmation flash message.
- **Settings page renamed `/settings/appearance` -> `/settings/account`.** The page had drifted well past "appearance" - it now houses theme controls, locale selection, foreach loop caps, and (new) account deletion. `Appearance.vue` renamed to `Account.vue` via `git mv` so file history follows. `routes/settings.php` keeps a permanent 301 redirect from the old URL so existing bookmarks and OBS-side links still resolve. Sidebar nav, user menu link, command palette entry, and two help-page cross-refs (`Formatting.vue`, `Conditionals.vue`) all updated.
- **Danger-zone UX on the Account page.** Bordered destructive panel at the bottom, bulleted list spelling out exactly what gets deleted (overlays, tokens, integrations, bot commands, custom tags, stream history; new Twitch auth starts from scratch), a textbox the streamer must type `DELETE ACCOUNT` into before the button enables, plus a `window.confirm()` "last chance" gate on submit. Button is disabled until the phrase matches; error messages are inline.
- Audit trail: admin `user.deleted` log entry is now written *before* deletion executes, so `user_name` and `twitch_id` remain captured even when the user row is hard-removed. Self-delete doesn't write to the admin audit log (it's not an admin action), but the deletion is irreversible by design - per the user's call, "no coming back to your data: it's gone."

## May 14th, 2026 - Help page: Lists (the whole picture in one place)

- New `/help/lists` page covering the full Lists feature surface end-to-end. Lists shipped in three slices over the past couple of days plus a privacy/retention follow-up; this is the first time it's been written up in one place where a non-developer streamer can read top to bottom and understand what they can do.
- Structure mirrors the existing `/help/expressions` and `/help/conditionals` pages: TOC, sections, worked examples, "Things to know" panel grid, quick-reference card. AppLayout, violet section links, code blocks in the sidebar palette.
- Thirteen sections cover: what a List is, creating and editing, all read tags (`:first` `:last` `:count` `:empty` `:random` `:sum` `:expires_at` `:countdown` plus the bare JSON), foreach iteration, chat appenders (with the raffle + quote-wall config tables), the `!list` meta-command and its rename story, the action vocabulary split into read / destructive / state groups, snapshots and 30-day retention, the two expiry timers (entry-TTL and whole-list deadline), disable/enable semantics, four worked examples (raffle, FIFO queue, quote wall, donation tally), seven "Things to know" gotchas (lists-are-lists contract, `:random` stability, mod-floor permissions, collision checking, minute sweep cadence, recipe-managed locks, live update protocol), and a quick-reference card with every tag and chat verb.
- Route registered at `/help/lists` -> `help/Lists` Inertia component, slotted alphabetically next to `/help/integration-presets` in the help index card grid with a `ListChecks` icon to keep it distinct from the dashboard `ListIcon`.
- Cross-links into `/help/conditionals`, `/help/formatting`, `/help/expressions`, `/help/reference`, and `/privacy` so the new page sits inside the existing docs graph rather than as an island.

## May 14th, 2026 - Privacy: list snapshot retention sweep + GDPR-friendly policy entry

- Shipping the 30-day snapshot retention sweep that was deferred in slice B/C and surfacing it in the privacy policy so the commitment is real rather than aspirational. Streamers (and their viewers, whose names end up in raffle lists) should be able to point at the policy and trust that snapshots aren't kept forever by default.
- New scheduled task `prune:list-snapshots` in `routes/console.php` running daily: deletes `list_snapshots` rows where `pinned = false AND created_at < now - 30 days`. Pinned snapshots are exempt and stay until the streamer unpins or deletes them. Daily cadence so the 30-day window is honoured to the day rather than to the week (matters because the privacy text now commits to 30 days specifically).
- New Pest test in `ListExpiryTest` covering all three cases at once: old-and-unpinned (swept), old-and-pinned (kept), young-and-unpinned (kept). 703 tests green.
- `Privacy.vue` updated: bumped `lastUpdated` to today, added Lists to "Information You Provide" (so it's clear that values viewers contribute via chat commands - display names, custom messages - are user-provided data we hold), and added a "Lists and snapshots" subsection inside Data Retention covering: every destructive action auto-creates a snapshot, unpinned snapshots auto-delete after 30 days and can't be recovered, pinned snapshots stay until manually removed, deleting a List removes all its snapshots (including pinned ones - the cascade is already in the schema from slice B), deadline-driven expiry produces a snapshot that follows the same 30-day rule.
- Also fixed a pre-existing import bug in Privacy.vue surfaced by the IDE diagnostic: `<Button>` was referenced at the bottom of the page but never imported. Now imported alongside the other shadcn primitives.

## May 14th, 2026 - Lists slice C: entry-TTL age-out + whole-list expiry + countdown tags

- Closes the lists-as-platform-primitive series with auto-expiry on two timescales: per-item age-out (drop entries older than N seconds) and whole-list deadline (snapshot, clear, and disable at a future moment). Both ride the same minute-cadence sweeper. No more "the raffle list still has Tuesday's entries on Friday" or "the giveaway closed three hours ago but is still accepting `!enter`."
- Migration: `option_sets` gains three columns. `entry_ttl_seconds` (nullable int; per-item age-out in seconds, 10s..30d range), `expires_at` (nullable datetime; whole-list deadline), `item_added_at` (jsonb; parallel array of Unix-seconds-per-item, backfilled to the row's `created_at` so the sweeper doesn't immediately sweep the entire pre-feature population on first tick).
- `App\Support\ListItemTimestamps` helper centralises the four timestamp operations the mutators need: `freshFor()` for new lists, `append()` for chat-driven appends, `removeAt()` for draw/pop/single-item removal, `preserveByValue()` for the dashboard's items-textarea save path (items matching by value keep their original timestamp; reordering doesn't reset the entry-TTL clock; renames or new entries get a current stamp). Every mutator that touches `items` now also touches `item_added_at` in lock-step: store, update, append, clear, draw, pop, clone (inherits verbatim so a mid-stream clone preserves time-remaining), snapshot restore (gets fresh stamps so an old snapshot doesn't immediately age out).
- `App\Services\Lists\ListExpirySweeper` runs both sweeps in one tick. Entry-TTL: pulls lists where `entry_ttl_seconds IS NOT NULL AND jsonb_array_length(items) > 0`, filters `item_added_at` against `now - ttl`, broadcasts `ListUpdated` when at least one entry aged out. List-expiry: pulls lists where `expires_at <= now AND disabled_at IS NULL`, snapshots (reason `before_clear`) when there's content to preserve, clears items + item_added_at, sets `disabled_at = expires_at` (auditable as "expired at T", not "disabled by sweep at T+1m"). Disabled lists still get their entry-TTL applied so old data ages out even after the list is disabled.
- Artisan command `lists:sweep-expired` scheduled every minute with `withoutOverlapping()`. Service is idempotent so manual runs during dev are safe. Both sweeps return counts the command surfaces for log readability.
- Renderer integration is two-fold. `[[[c:list:slug:expires_at]]]` ships as a static Unix-seconds tag (empty string when unset). `[[[c:list:slug:countdown]]]` synthesises a timer state with `mode: 'countto'` and `target_datetime: expires_at->toIso8601String()`, so the existing RAF tick machinery already used for `[type=timer]` controls drives a live seconds-until-expiry value. No new client code path - the timer system handles it. Renders as the current seconds-until-expiry (clamped >= 0); pipe formatters like `[[[c:list:slug:countdown|duration:mm:ss]]]` work as expected.
- `OverlayRenderer.vue`'s `handleListUpdated` now patches `:expires_at` and re-seats the synthetic countdown timer when broadcasts carry `expires_at`. Clearing `expires_at` stops the timer tick and zeroes the data slot so a template referencing the countdown doesn't keep showing the last computed value.
- `ListUpdated` event grew two optional fields: `expiresAt` and `disabledAt`. New `ListUpdated::dispatchFor($broadcasterId, $list)` static helper resolves all five fields off the model so callers don't thread them through individually. All eight dispatch sites refactored to use it (ListController create/update, ListActionService broadcast helper + clone, ListAppendService fire, ListActionWebController restoreSnapshot, ListExpirySweeper both paths). Only the delete-signalling dispatch in `ListController::destroy` still uses the raw constructor because it explicitly sends `items: null`.
- `ListController::update` extended with focused-PATCH paths for the two TTL fields. Either `entry_ttl_seconds` or `expires_at` in the payload triggers expiry-only mode (items/label ignored). Clearing `expires_at` on a previously-sweep-disabled list also clears `disabled_at` so "reopen" is a one-input action instead of needing to also click Enable.
- Lists Vue page gets a new Expiry panel between the items textarea and the action row. Two-column layout: per-item age-out (number input + unit picker auto-snapping to the largest divisor on load, so 3600 displays as "1 hour" not "60 minutes" or "3600 seconds") and whole-list deadline (datetime-local picker with live countdown preview that ticks every second so the streamer can sanity-check the date without doing math). Both have explicit Clear buttons. Template tags surfaced under the panel for copy-paste convenience.
- Lists page broadcast listener extended to thread `expires_at` and `disabled_at` through. Undefined-instead-of-null protocol: a broadcast missing the field falls back to the existing value (so a one-off broadcast that doesn't know the TTL state doesn't accidentally clear it), but `null` explicitly clears.
- 20 new Pest tests in `ListExpiryTest` cover the full surface: timestamp sync across all 7 mutators, sweeper entry-TTL age-out (drop / no-op / broadcast), sweeper list-expiry (snapshot + clear + disable / future expires_at not touched / re-sweep idempotent), controller PUT paths (update / clear / out-of-range validation / re-enable on expires_at clear), Inertia serialization, broadcast payload shape with the new fields. Plus the existing `ListUpdated` broadcast shape test in `ListControllerTest` updated to assert the new keys. 702 tests green (was 682).
- Deliberately deferred: scheduled retention sweep for unpinned snapshots >30 days old (the cron job; schema + pin behaviour have been in place since slice B), recipe-level declarative TTL config (recipes today can pre-create a List but can't yet declare `entry_ttl_seconds: 3600` in their manifest), and "extend by N" UI for nudging an existing expires_at without resetting it from scratch.

## May 14th, 2026 - Lists slice B: !list meta-command + action vocabulary + snapshots

- The big half of the lists-as-platform-primitive work. Adds the `!list <slug> <action>` chat surface (one configurable meta-command per user with a fixed action vocabulary), dashboard buttons for the same actions, and an auto-snapshot safety net so destructive actions are undoable.
- Two new tables. `list_meta_commands` (one row per user; configurable command name defaulting to "list"; unique on user_id; permission hardcoded to moderator+ since the action vocabulary is destructive or chat-emitting). `list_snapshots` (list_id FK cascade, items jsonb, reason enum, triggered_by_user_id, pinned bool, created_at; append-only, no updated_at).
- Fixed action vocabulary, one parser:
  - **Read** (no mutation, no snapshot): `count`, `first [N]`, `last [N]`, `random [N]`. N defaults to 1, caps at list size.
  - **Destructive** (auto-snapshot, broadcast ListUpdated): `draw` (random pick + pop, announces winner with 🎰), `clear` (empties), `pop first|last` (head/tail removal; bare `pop` errors with help), `clone <new_slug>` (duplicates).
  - **State**: `disable`, `enable`.
- Self-documenting help threaded through the same parser. Bare `!list` returns the global help message; `!list <slug>` returns per-list help; `!list <slug> unknown_action` lists valid actions; `!list <slug> pop` and `!list <slug> clone` (missing required arg) reply with usage hints inline. Beautifully consistent: any ambiguity surfaces as a helpful reply rather than silent failure.
- `ListActionService` is the single backing service for both chat-driven invocation and dashboard buttons. Parses `<slug> <action> <args>`, validates ownership, dispatches per-action, generates chat-friendly replies, snapshots before destructive ops inside lockForUpdate transactions, broadcasts ListUpdated. Hard cap of 400 chars on chat replies (Twitch chat is 500; slack for @user prefix).
- New internal API endpoint `POST /api/internal/bot/list-actions/fire` (sibling of expressions/recipe-triggers/list-appenders). BotCommandController surfaces opted-in users with `type: 'list_meta'`. Resolution order: builtin > expression > recipe_trigger > list_append > list_meta.
- Bot repo (separate commit) adds the `entry.type === 'list_meta'` dispatch branch in `bot.js`, mirroring the existing pattern. Bot stays silent on response - replies are queued to `bot_chat_outbox` server-side and the outbox poller speaks them.
- Web endpoints (JSON, consumed via axios): action runner (same vocabulary as chat), snapshot CRUD (list/manual/restore/pin/delete), meta-command settings (one per user; collision-checked against builtin/expression/recipe_trigger/list_append commands at save time).
- Lists Vue page extended with three sections:
  - **Meta-command card** at the top: opt into !list, customise command name, toggle enabled.
  - **Actions row** per active list with 9 buttons (Count / First / Last / Random / Clone / Draw / Pop first / Pop last / Clear). Read actions prompt for an optional N; destructive actions confirm; Draw is the primary call-to-action in violet. Results surface as toasts.
  - **Snapshots panel** (collapsible) with reason badge, item count, relative age, pin/restore/delete per row. Manual "Save snapshot" button before risky edits.
- Snapshots are the safety net. Restoring a snapshot creates a `before_restore` snapshot of the current state first so even the restore is undoable. Reason enum: `before_clear` / `before_draw` / `before_pop` / `before_restore` / `manual`.
- 30 new Pest tests cover service-level read/destructive/state actions + error paths (unknown slug, unknown action, bare pop, bare clone, draw on empty, clone slug collision), bot internal API gate/permission/meta_not_found paths, commandMap surface, web action endpoint + foreign-user 404, snapshot list/restore/pin, meta-command save with collision-check. 682 tests green (was 652).
- Deliberately deferred: scheduled retention sweep that deletes unpinned snapshots >30 days old (schema + pin behaviour are in place; the cron job is the missing piece), auto-expire entries + auto-expire lists (slice C, requires per-item timestamps), and the synthetic timer-control integration for list expiry.

## May 14th, 2026 - Lists slice A: derived read tags + disable/enable state

- First slice of the lists-as-platform-primitive work designed yesterday. Adds five derived read tags and a per-list disabled state. No new tables - just one column + tag injection extensions.
- Migration: `option_sets.disabled_at` nullable timestamp. Null = active. Non-null = disabled (silent refuse on chat-appender fires; existing items stay visible; streamer can still curate manually via the dashboard).
- Five new read tags injected by `OverlayTemplateController::renderAuthenticated`:
  - `[[[c:list:slug:first]]]` - items[0] or empty string
  - `[[[c:list:slug:last]]]` - items[count-1] or empty string
  - `[[[c:list:slug:empty]]]` - "1" if empty, "0" otherwise (for [[[if]]] checks)
  - `[[[c:list:slug:random]]]` - one random item, stable per overlay mount (never re-rolls on broadcast - that'd flicker on every append)
  - `[[[c:list:slug:sum]]]` - sums numeric items; whitespace and empty entries skipped as 0; fails loudly with `ERR: list 'slug' has non-numeric item 'abc' at position 4` so the streamer can find and fix the broken row
- Also exposed `[[[c:list:slug:count]]]` as a colon-form alias for the existing `.count` (foreach materialiser still uses the dot form internally; the colon form is the user-facing read).
- `OverlayRenderer.vue`'s `handleListUpdated` recomputes :first, :last, :empty, :sum on every `ListUpdated` broadcast so the tags update live in active overlays. `computeListSum` mirrors the PHP version exactly so initial render and broadcast updates produce identical strings. `handleListDeleted` now sweeps both dot-form and colon-form derived keys from the data store.
- `ListAppendService::fire` short-circuits with `reason: 'list_disabled'` when the target list has `disabled_at` set. Silent - no outbox reply, no chat noise. The streamer disabled the list intentionally and the bot shouldn't apologise about it.
- `ListController::update` accepts an optional `disabled` boolean. When present, it's a focused PATCH-style operation that only toggles `disabled_at` and ignores label/items even if sent. Lets the dashboard fire a one-click toggle without round-tripping the items array. Recipe-locked lists can still be disabled by their owner - locking only prevents item edits, not state changes.
- Lists Vue page gets a status badge (red "Disabled" + PowerOffIcon) next to the existing recipe/locked badges, plus a toggle button in the action row that swaps between "Disable list" and "Enable list" depending on current state.
- 13 new Pest tests in `ListReadTagsTest` cover :first/:last/:empty/:random/:sum (including the loud-failure path for non-numeric content and the integer-no-trailing-zeros formatting), `disabled_at` blocking appender fires, re-enable restoring fires, and the toggle endpoint preserving items when disabled is set. 652 tests green (was 639).
- Locked-in but deferred to slice B: the `!list <slug> <action>` meta-command + the action vocabulary (draw / clear / pop / clone / count / first / last / random N). Those are bigger surface (action-runner service + bot dispatch + snapshot safety net) and ship together as one cohesive unit.

## May 13th, 2026 - Fix: /broadcasting/auth was being shadowed by the catch-all 404 route

- WebSocket channel-auth requests to `POST /broadcasting/auth` were returning HTTP 200 with the `errors.404` HTML view instead of the JSON auth signature Echo expects. Browser tab open on `/dashboard/lists` (and presumably everywhere else using `window.Echo.private(...)` - `useStreamState`, the overlay renderer's dashboard preview, etc) couldn't authorise their channel subscriptions. The visible symptom on the Lists page: no live updates when chat appended to a list via a `!command`, even though the server-side broadcast was firing.
- Root cause: `routes/web.php` ended with `Route::any('{catchall}', [PageController::class, 'notfound'])->where('catchall', '.*')`. That wildcard regex match wins by route-registration order over `/broadcasting/auth` which gets auto-registered later by Laravel's BroadcastServiceProvider. So Echo's POST hit the catch-all, got `view('errors.404')` rendered as text/html with status 200, and Echo saw the HTML response as a failed auth.
- Fix: switched the catch-all to `Route::fallback([PageController::class, 'notfound'])`. `Route::fallback()` is Laravel's canonical "match only when no other route matches" mechanism and yields to any later-registered route regardless of order. Genuinely unknown URLs still hit the same `notfound` controller and render the same 404 page; `/broadcasting/auth` (and any future auto-registered Laravel routes) now reach their real handlers.
- Two new Pest tests in `BroadcastingAuthRouteTest`: hitting `POST /broadcasting/auth` as an authenticated user does NOT return the `errors.404` HTML view; and a genuinely-unknown URL still 404s. 639 tests green (was 637).
- This was a latent bug, not new in this session - any feature relying on private channel subscriptions has been silently failing auth (useStreamState's "live" indicator, the recipe dashboard's expected real-time `picker.landed` updates, etc.). The Lists feature just made it visible because the new `.list.updated` listener is the first surface that's clearly broken when channel auth fails (other consumers either have polling fallbacks or only matter during specific user-triggered events).

## May 13th, 2026 - Lists page: fix phantom-blank-first-item + subscribe to live broadcasts

- Two post-ship fixes after first real prod use surfaced both. (1) Creating an empty list then having a chat appender add to it produced a list with a phantom blank first row. (2) The /dashboard/lists page didn't update when a chat command appended to a list - had to reload the page to see the new item.
- Phantom blank fix in `resources/js/pages/dashboard/lists/index.vue`: the textarea was sending `newItemsText.value.split('\n')` to the server on save. JS's split returns `[""]` (one empty-string item) when called on an empty string, not `[]` - so saving an empty textarea created a list whose first item was already `""`, and `!hi hello` from the first chatter would append AFTER that phantom. Fix is `text === '' ? [] : text.split('\n')`. Same fix on `createList()` and `saveActive()`. Preserves the "lists are lists, don't touch what was typed" contract for any non-empty content. Matches what Streamer.Bot's Write-To-File feature is doing under the hood too - same underlying JS pitfall.
- Live updates fix: the /dashboard/lists page wasn't subscribed to the user's broadcast channel at all. Only `OverlayRenderer.vue` was. Added an Echo subscription in onMounted using the same `alerts.{twitch_id}` pattern as `useStreamState`. Listens for `.list.updated` and `.list.deleted`; patches the local `lists` ref by slug. If the user is currently editing the affected list AND has unsaved changes, the active textarea is left alone (their pending edits win until they save); if they're not dirty, the textarea refreshes with the new items so a chatter's append appears in the view immediately. onUnmounted leaves the channel cleanly.
- No backend changes - the broadcast was already firing from `ListAppendService`. Both fixes are entirely in the Vue page.

## May 13th, 2026 - List Appenders: chat commands that grow Lists (raffle / queue / quotes / praise wall)

- Builds the write side on top of the Lists feature shipped earlier today. Streamers can now wire a chat command like `!raffle` to append the invoker's display name to a List automatically; same shape extends to `!join` queues, `!quote` walls, etc. The read side (Bot Expressions / overlays reading lists) and the write side (chat appending to lists) compose to close the producer/bus/consumer loop for the user-controlled-data path.
- Two new tables. `list_appenders` (user_id, target_list_id FK to option_sets, command, permission_level, cooldown_seconds, value_template, args_empty_reply, dedup_policy, max_size, enabled, last_fired_at, timestamps; unique(user_id, command)). `list_append_history` (list_appender_id FK, target_list_id FK, chatter_id, chatter_login, value, stream_session_id FK nullable, fired_at; append-only, no updated_at). The history table powers dedup + provides an audit trail without requiring a join on every check.
- The value template uses the exact same syntax as Bot Expressions - same `[[[bot:from_user]]]` / `[[[bot:args]]]` / `[[[c:foo:bar]]]` / pipe formatters - because ListAppendService just hands the template to the existing `BotExpressionResolver`. Zero new template language. The same applies to `args_empty_reply` - "Hi @[[[bot:from_user]]] add something after !raffle, like !raffle salami!" resolves chatter context via the same code path.
- `[[[bot:fired_at]]]` added to the shared bot context in `BotExpressionService::buildBotContext` - Unix seconds at dispatch time. Available to both Bot Expressions and List Appenders now. Pipe formatters give it locale-aware formatting for free: `[[[bot:fired_at|date:HH:mm]]]`.
- Three dedup policies: `none` (allow duplicates - quotes, praise walls), `per_chatter` (lifetime per appender - raffles), `per_chatter_per_stream` (resets when stream goes live - game queues, where each viewer should get one play per stream). When stream is offline and `per_chatter_per_stream` is configured, falls back to lifetime `per_chatter` to keep offline tests from accidentally seeding the next stream's queue.
- `max_size` is enforced with a `lockForUpdate` inside the fire transaction so two simultaneous chat fires at slot 99/100 can't both pass the check and over-fill the list. Silent refuse on full (no chat message) - streamer can layer a Bot Expression that reads `:count` for a "raffle is full" announcement if they want one.
- Args-empty handling: when the value template references `[[[bot:args*` AND the chatter supplied no args, the fire bounces with `reason: 'args_empty'`. If the streamer configured an `args_empty_reply` (Bot Expression syntax), it's resolved with chatter context and queued into `bot_chat_outbox` - the bot's existing outbox poller picks it up and speaks it. Bot stays dumb. Leaving `args_empty_reply` null gives silent refusal for streamers who want zero chat noise.
- `BotExpressionResolver` reuse made the service shockingly thin: ~150 lines for canFire + fire + dedup + history-write + broadcast. Same shape as `BotExpressionService` and `RecipeChatTriggerService` so the three are now interchangeable in pattern, just different terminal effects (output to chat / fire a picker / append to a list).
- Internal API: `POST /api/internal/bot/list-appenders/fire`, X-Internal-Secret auth, same payload shape as `/expressions/fire` and `/recipe-triggers/fire`. `BotCommandController::index` now surfaces appenders with `type: 'list_append'` in the commandMap. Resolution order on collision: builtin > expression > recipe_trigger > list_append. ListAppenderController (web side) refuses to save a command that collides with any of the other three surfaces.
- Bot repo `6e647d0..` adds the `entry.type === 'list_append'` branch to `bot.js`, calling a new `fireListAppender(payload)` in `overlabelsApi.js`. Mirrors the recipe_trigger dispatch path exactly. Bot remains silent in chat for the success path; args_empty_reply paths reach the outbox via Overlabels-side code and get spoken by the existing outbox poller.
- UI: per-list "Append commands" section on `/dashboard/lists` (right pane, under the items textarea). One row per appender showing command, permission, dedup, max_size, cooldown, value template, args_empty_reply preview. Add/Edit via Reka Dialog modal with all fields. CRUD goes through `ListAppenderController` returning JSON (consumed by axios) rather than Inertia full-page reloads - feels inline instead of router-bouncy.
- 14 new Pest feature tests in `ListAppenderTest`: append + history write + ListUpdated broadcast, per_chatter dedup, none dedup allows duplicates, max_size silent refuse, args_empty queues outbox reply, args_empty silent when no reply configured, multi-tag value template resolution, fire endpoint happy/channel_not_found/appender_not_found/gate paths, X-Internal-Secret enforcement, commandMap surface + collision resolution. 637 tests green (was 623).
- Deliberately deferred: EventSub-triggered appenders ("every new subscriber goes into a recent_subs list" - the table is generalisable but the source field isn't there yet), other list ops (remove/clear/replace - currently append-only), streamer-facing history view ("who joined the raffle and when" - data's already there in `list_append_history`, no UI yet), and visible "raffle is full" / "you're already in" replies from the appender itself (use Bot Expressions for those).

## May 13th, 2026 - Lists: user-managed OptionSets as a first-class top-level feature

- After reverting the Wheel kit's bad frontend implementation earlier today (commits 7ef9457, d009aec, 1317e39), the actionable insight was: Recipes today don't have a backend rich enough to think about visuals. OptionSets only existed when a recipe install created them - users couldn't author their own. This commit promotes them to first-class. Recipes are still the producer-of-controls layer; what we did is uncouple "lists of values" from "recipes that pick from lists." Lists are a primitive that anyone can create and any consumer can read; recipes are one such consumer.
- The `option_sets` table didn't need a schema change (recipe_instance_id was already nullable). New surface is purely additive: routes, controller, Vue page, broadcast event, renderer injection.
- `ListController` at `app/Http/Controllers/ListController.php` handles index / store / update / destroy under `/dashboard/lists`. User-authored lists (recipe_instance_id = NULL) get full CRUD. Recipe-installed lists also surface here and are editable when their recipe declared `user_editable: true`; locked ones stay visible but are read-only via a 403 on the update path. Min/max items from a recipe manifest are still respected on save when set.
- "Lists are lists" - server-side validation is intentionally minimal. The controller does NOT dedupe, does NOT strip empty lines, does NOT trim whitespace. Only filter applied is NUL-byte stripping per item, purely so the json_encode pipeline can't blow up on a NUL that would never be deliberate input. Empty lines, duplicates, leading/trailing whitespace, and Unicode are preserved exactly. Two existing global middlewares would have silently corrupted this contract - `TrimStrings` and `ConvertEmptyStringsToNull` - so `bootstrap/app.php` now exempts both for requests under `/dashboard/lists*` via callback closures (the Laravel 11+ API).
- New tag namespace `[[[c:list:<slug>]]]` exposed at render time. `OverlayTemplateController::renderAuthenticated` injects each user-owned list as TWO data shapes simultaneously: (1) the full JSON-encoded array under `c:list:<slug>` so direct tag substitution resolves to a JSON string, AND (2) flat indexed scalars `c:list:<slug>.0`, `c:list:<slug>.1`, ... plus `c:list:<slug>.count` so the existing `materialiseIterable` (`useConditionalTemplates.ts:176`) in the foreach machinery picks them up unchanged. Net result: `[[[foreach:c:list:donors as donor]]][[[donor]]][[[endforeach]]]` Just Works against any user list, alongside the existing foreach support for Twitch event payloads, with no parser or materialiser changes.
- Live propagation via new `ListUpdated` broadcast event on the existing `alerts.{twitch_id}` channel. Fires on save with the full items array as payload + `updated_at` (Unix seconds); fires with `items: null` on delete and uses `broadcastAs() = 'list.deleted'` to let the listener distinguish without inspecting payload shape. `OverlayRenderer.vue` registers `.list.updated` and `.list.deleted` listeners alongside the existing `.control.updated`; the update handler patches both the full JSON and the indexed scalars in-place and prunes any tail keys from a shrinking list so stale `.3`, `.4`, ... don't linger in the data store. No overlay reload needed when a streamer edits a list - the next foreach iteration on the next morphdom tick reflects the change.
- Vue page at `resources/js/pages/dashboard/lists/index.vue` is a two-pane layout: left rail lists the user's lists (label, slug, item count, recipe badge if any, lock icon if recipe-locked), right pane edits the active list (label input + raw textarea + save/delete). Textarea splits on `\n` at save time so the user can type empties and duplicates intentionally. Create flow is a collapsible card at the top with slug validation matching the server pattern (`^[a-z][a-z0-9_]{0,49}$`), live-previewed as `[[[c:list:your_slug]]]`. Snippet copy button on the active list puts the tag on the clipboard.
- AppSidebar gains a "Lists" entry under "My stuff" (between Alerts and Kits) using the lucide `ListIcon`. Lists are a primitive recipes happen to use, not a sub-feature of recipes - top-level nav reflects that.
- 15 new Pest tests in `ListControllerTest`: index returns the right shape with `[[[c:list:<slug>]]]` tag baked in, index doesn't leak other users' lists, store preserves empties and duplicates exactly, store strips NUL bytes only, store rejects malformed slugs and duplicate-per-user slugs, update wholesale-replaces items and broadcasts, update refuses locked recipe lists, update allows editable recipe lists, update enforces min/max bounds, update returns 404 on foreign lists, destroy refuses recipe-installed lists, destroy broadcasts a `list.deleted` event, and broadcast payload shape covers both update and delete cases. 623 tests green (was 608).
- Deliberately not in this slice: dynamic lists populated from Twitch/EventSub data (the planned step 3), transformations on lists (filter/sort/slice - step 4), reorder beyond "edit the textarea and resave", per-item metadata (color/weight/etc.). The static-list backbone shipping first means none of those need to fight schema choices for their UI surface later.

## May 13th, 2026 - Recipes step 5: Dice ships as the second first-party recipe

- Per the build order in `Recipes.md`: "you cannot skip step 5. Without a second recipe to validate the manifest shape, the abstraction is just 'Coin Flip with extra steps.'" Dice is that validation. Different shape than Coin Flip (6-face option set vs 2, different command name, different cooldown), zero new code path - same RecipeManifestValidator, same RecipeInstaller, same BridgePickerLandedToControl listener, same BotCommandController surface, same dashboard page. The abstraction held.
- `resources/recipes/dice/manifest.json`: 40 lines, mirrors Coin Flip's shape exactly. 6-face default (1..6) with `min_items: 2` and `max_items: 100` so the streamer can swap in custom faces ("Pizza", "Tacos", "Sushi"...) without breaking the picker contract. `!roll` chat command with a 5s cooldown (tighter than Coin Flip's 10s because rolling dice is meant to be a flurry, not a ceremony). Dashboard button labelled "Roll the die".
- Auto-seeded via FirstPartyRecipesSeeder which globs `resources/recipes/*/manifest.json` - no seeder edit needed to ship a new recipe, just drop the manifest file. End-to-end install verified for user 50 (jasperdiscovers): RecipeInstance row materialised one option_set `d6_faces` with the six faces, one picker `d6_roller`, three controls (`dice:d6:result/result_at/running`), one chat trigger (`!roll`, 5s). 30-roll distribution across the six faces looks healthy (no single face missing). Both Coin Flip (`!flip`, `c:coin_flip:main:*`) and Dice (`!roll`, `c:dice:d6:*`) now appear on `/dashboard/recipes` with their respective Fire buttons.
- Two new Pest tests. `RecipeManifestValidatorTest::it accepts every shipped first-party manifest` is a regression guard that globs `resources/recipes/*/manifest.json` and validates each against the schema - drops one safety net under every future recipe addition so a hand-edited manifest can't reach production via the seeder unnoticed. `RecipeInstallerTest::it installs the Dice recipe with the same shape and a six-face option set` exercises the full install path against the real manifest file, verifying slugs (`d6_faces`, `d6_roller`), broadcastKey (`dice:d6:result`), trigger materialisation (`!roll` with 5s cooldown), and a single fire landing on a real face. 608 tests green (was 606).
- One existing tinker-debug artifact cleaned up: `App\Models\Recipe::truncate()` against the testing DB removed two leftover catalogue rows that survived from the step-3 seed run done outside a test transaction. Recipe tests are DatabaseTransactions-wrapped so each test creates its own catalogue rows; the stale rows were causing a unique-constraint violation when re-runs tried to re-create them. The fix here is purely operational - the seeder is correct, the tests are correct, and prod is untouched.

## May 13th, 2026 - Recipes dashboard page lists installed instances and fires dashboard buttons

- Follows step 4. Now that the install pipeline + bot dispatch + dashboard fire endpoint all work, the dashboard page exists so a streamer can actually *see* their installed recipes and click their buttons without dropping into tinker. Layout intentionally minimal: one card per installed instance, label + recipe + version + instance slug, a Copy-tag button that puts `[[[c:<recipe>:<instance>:result]]]` on the clipboard, and a "Fire" button per dashboard_button trigger that fires the picker and surfaces the latest result inline with a relative timestamp.
- Route: `GET /dashboard/recipes` -> `RecipeInstanceController::index()`, behind the standard `auth.redirect` middleware. The controller eager-loads `recipe` + `pickers`, walks each manifest's triggers[] for dashboard_button entries, and builds a tight payload with just what the page renders. No PHP-side enrichment of last_result_at into a formatted string - that's a frontend formatter so it can recompute "30s ago" on the next interaction without a refresh.
- Page: `resources/js/pages/dashboard/recipes.vue`. Inertia page using AppLayout + Heading + Card + Button + Badge. Local mutable ref mirrors the props payload so a fire updates the result inline without a router reload (the result + timestamp on the row change in place; the toast confirms). Loader2 spinner on the firing button, Play icon when idle. Copy-tag button uses navigator.clipboard with a toast confirmation. Empty state with ChefHat icon when the user has no installs yet. Responsive: single column on mobile, two columns from md up.
- No new tests; the controller payload is exercised end-to-end via the existing fireButton endpoint tests in `RecipeChatTriggerTest`. The page render path was verified by hitting `/dashboard/recipes` (302 -> login for unauthenticated, which is the expected behaviour), and the controller's index() return shape was smoke-tested in tinker for user 50 (jasperdiscovers) - returns the installed Coin Flip instance with one button row.
- Bot repo (separate commit at `C:\Users\jmstu\PhpstormProjects\overlabels-bot`, commit 6e647d0): dispatch path now recognises `entry.type === 'recipe_trigger'` and POSTs to `/api/internal/bot/recipe-triggers/fire`. Bot stays silent in chat by design - recipe trigger result announcement lives in Bot Expressions, optionally bundled by a Kit. Badge normalisation extracted into `buildBadgeList(event)` shared between the expression and recipe-trigger dispatch helpers; behaviour for the expression path is unchanged.

## May 13th, 2026 - Recipes step 4: chat-command + dashboard-button trigger materialisation

- Step 4 wires the producer-side trigger surfaces. Recipe manifests already declared `chat_command` and `dashboard_button` triggers since step 2; step 3's installer wrote primitives and controls but ignored the triggers section. This commit picks those up: a `recipe_chat_triggers` table materialises at install time, an internal API endpoint the bot calls to invoke them, and a web endpoint the (future) dashboard uses for button triggers. The full Coin Flip path - chatter types `!flip`, picker lands, controls update, broadcast hits the overlay - now works end to end without writing a single line of UI.
- New table `recipe_chat_triggers`: recipe_instance_id FK (cascadeOnDelete), denormalised user_id (lets the bot commandMap scan in one shot without joining), picker_id FK, command (without leading `!`, same shape as `bot_commands.command` and `bot_expressions.command`), permission_level, cooldown_seconds, last_fired_at, enabled. Unique(user_id, command) - chat is a user-scoped namespace, two installs of the same recipe with `!flip` would collide. Dashboard_button triggers deliberately don't get a row; they're fired by name through the web endpoint which looks up the picker through `RecipeInstance.primitive_map`.
- New shared helper `App\Support\BotChatGate`: stateless permission + cooldown gate with three static methods (`hasPermission`, `isOffCooldown`, `isBroadcaster`). Extracted because BotExpressionService and the new RecipeChatTriggerService run the exact same gate with the same broadcaster-bypass semantics against the same BotCommand::PERMISSION_LEVELS vocabulary; duplicating 30 lines and a TIER_ORDER constant would have invited drift. `BotExpressionService::canFire` now delegates to it (existing 24 BotExpression tests still pass with no behaviour change).
- `App\Services\Recipes\RecipeChatTriggerService` mirrors BotExpressionService's surface: `canFire(trigger, badges)` for gating, `fire(trigger)` for the side effect. The side effect is calling `Picker::fire()` and stamping last_fired_at - no chat output. Recipe triggers are deliberately silent in chat per the producer/consumer split documented in `Recipes.md`. Kits will be expected to bundle a default Bot Expression alongside the recipe if a streamer wants the result announced.
- `RecipeInstaller` extended in two ways. (1) Pre-flight `assertNoChatCommandCollisions()`: refuses install if any manifest chat_command's name already exists as a BotCommand, BotExpression, or RecipeChatTrigger for the user. Surfaces a clear error message naming which surface is colliding, rather than letting it fail at DB-uniqueness time with an opaque message. (2) Inside the transaction, walks `manifest.triggers[]` and writes a recipe_chat_triggers row per chat_command trigger (dashboard_button is skipped because it needs no row). The trigger's permissions/cooldown_seconds default to "everyone"/0 if the manifest omits them, matching the schema defaults.
- New internal endpoint `POST /api/internal/bot/recipe-triggers/fire` (handled by `BotRecipeTriggerController`). Same auth + payload shape as `/expressions/fire` so the bot can hit it through the existing X-Internal-Secret rail. Returns `{fired: bool, reason?: string, result?: string}` - reason is one of `channel_not_found` / `trigger_not_found` / `gate`. Broadcaster bypasses cooldown to match the existing builtin-command pattern.
- `BotCommandController::index` extended to surface recipe triggers in the per-channel command map with `type: 'recipe_trigger'`. Resolution order on collision is now builtin > expression > recipe_trigger (later loses). Defensive: even if a colliding row sneaks past the installer's pre-flight check, the bot still gets a deterministic single entry per command name.
- New web endpoint `POST /recipes/instances/{instance}/fire-button` (handled by `RecipeInstanceController`). Auth via `auth.redirect`, validates instance ownership against the authenticated user (404 for foreign instances - same shape as the rest of the dashboard CRUD surfaces). Body takes a `picker_ref` matching the manifest's primitives.pickers[ref], and the picker must be wired to a dashboard_button trigger in that manifest - firing a picker that has no button trigger declared is a 404 because that's a confusing surprise to the installer. No Vue UI yet; the endpoint is exercisable via curl / fetch and is what a future Recipes dashboard will POST to.
- 16 new Pest tests in `RecipeChatTriggerTest.php` cover: trigger materialisation from the manifest, command-collision checks against all three other surfaces (BotCommand, BotExpression, RecipeChatTrigger), cascade-delete cleanup, the internal API's six response shapes (success / channel_not_found / trigger_not_found / gate / permission gate / cooldown gate / broadcaster-bypass), the commandMap surface with type=recipe_trigger and builtin-wins-on-collision, the web endpoint's success path, foreign-instance 404, and unwired-picker 404. Plus one new test in `RecipeInstallerTest` documenting the new "second install collides on chat command" behaviour. 606 tests green (was 589).
- Two existing RecipeInstaller tests updated: "two instances under different slugs" and "max_instances_per_user" now use a chat-trigger-stripped variant of Coin Flip since the canonical manifest's `!flip` chat trigger would correctly collide on the second install. The multi-instance semantics still hold for dashboard-button-only recipes; the new "second install collides" test documents the chat-bound case explicitly.
- Deliberately deferred: (1) a Vue dashboard page that lists installed instances + their dashboard buttons. Not yet because there's only one recipe and zero installed instances on the platform - it would be a UI for ten use cases that don't exist yet. (2) Per-user cooldowns (currently per-channel only, matching Bot Expressions' v1 stance). (3) `!commands`-style meta-command surfacing recipe triggers - belongs in Bot Expressions follow-up. (4) Trigger enable/disable toggle UI - the column exists, no surface to flip it.

## May 13th, 2026 - Recipes step 3: RecipeInstaller service + catalogue + PickerLanded-to-controls bridge

- Step 3 of the build order in `resources/help/reference/Recipes.md` is "Build the install flow. A RecipeInstaller service that reads a manifest and writes owned instance rows. Reuse fork/copy slug logic from kits." This lands the bridge between the JSON manifest contract (step 2) and the primitive tables (step 1). Triggers (chat-command routing + dashboard-button endpoint) deliberately deferred to step 4 since they touch the bot routing system and the Vue dashboard surfaces - both deserve their own design pass.
- Two new tables: `recipes` (catalogue: slug, version, manifest jsonb, author info, is_first_party flag, unique(slug, version) so each version is its own installable artifact per the no-WordPress-style-updates decision) and `recipe_instances` (per-user installs: recipe_id FK, user_id, instance_slug, primitive_map jsonb mapping manifest refs to materialised primitive row IDs, unique(user_id, recipe_id, instance_slug) so Johnny can install Coin Flip twice as long as the slugs differ - matches the doc Q4 answer "each instance should have its own specific name, even if Johnny calls both of his Recipes LOLWHEEL").
- Adds nullable `recipe_instance_id` FK (cascadeOnDelete) to `option_sets`, `pickers`, and `overlay_controls`. Cleans up the entire graph on uninstall via cascade - tested: delete the recipe_instance row and every owned primitive + control vanishes. NULL means hand-authored, not recipe-installed, so the existing tinker/power-user flow is unaffected.
- `App\Models\Recipe`: catalogue model with jsonb manifest cast + instances hasMany. `App\Models\RecipeInstance`: per-user install model with relations to optionSets, pickers, overlayControls, recipe, user. primitive_map is the bridge listener's lookup table - `{"option_sets": {"coin": 42}, "pickers": {"flipper": 17}}` translates manifest refs to row IDs without a join.
- `OverlayControl::broadcastKey()` updated to compose the three-segment namespace for recipe-managed controls: `<recipe_slug>:<instance_slug>:<key>` (e.g. `coin_flip:lolwheel:result`). Falls back to the existing `<source>:<key>` shape for service-managed controls and bare `<key>` for template-scoped controls. The existing tag regex already tolerates colons in the tag key so `[[[c:coin_flip:lolwheel:result]]]` resolves through the standard render path - no template-parser changes needed.
- `App\Services\Recipes\RecipeInstaller`: takes (Recipe, User, instance_slug, label?). Validates instance_slug against the snake_case pattern, re-validates the manifest (defensive against catalogue rows hand-edited via tinker), enforces unique(user, recipe, instance_slug) and max_instances_per_user, then transactionally materialises option_sets, pickers, and one overlay_controls row per manifest control_export. Primitive slugs are `<instance_slug>_<manifest_ref>` so two installs never collide. Returns a fresh-loaded RecipeInstance with all relations eager-loaded.
- `App\Listeners\BridgePickerLandedToControl`: auto-discovered listener (no explicit Event::listen registration needed in Laravel 12+; the previous manual registration was a bug that fired the listener twice per event, fixed by removing it). On PickerLanded, walks the manifest's control_exports, finds the export(s) whose `from` path references this picker, writes the new value to the matching overlay_control row, and dispatches ControlValueUpdated with the namespaced broadcast key. No-op for primitive-only pickers (no recipe_instance_id) - the tinker / power-user flow stays intact.
- `database/seeders/FirstPartyRecipesSeeder`: globs `resources/recipes/*/manifest.json`, re-validates each through RecipeManifestValidator, then upserts into the catalogue with is_first_party=true. Idempotent - re-running produces the same rows. Wired into DatabaseSeeder so a fresh `php artisan migrate:fresh --seed` ships with the Coin Flip recipe ready to install. Coin Flip v1 is seeded in dev today.
- 9 new Pest feature tests in `RecipeInstallerTest.php` cover: end-to-end install creates exactly 1 option_set + 1 picker + 3 controls with correct slugs and types, broadcastKey composes to `coin_flip:main:result`, duplicate instance_slug rejected with a clear message, two instances with different slugs coexist, dash-in-slug rejected at the InvalidArgumentException layer, max_instances_per_user enforced, cascade-delete cleans up all 5 owned rows, full PickerLanded -> overlay_controls -> ControlValueUpdated round-trip with the namespaced key on a recipe-owned picker, and the bridge is correctly inert for standalone pickers (no ControlValueUpdated fires when there's no recipe_instance). 589 tests green (was 580).
- Deliberately deferred to step 4 (triggers materialisation): chat_command triggers need to register with the bot's existing command-routing system (overlabels-bot repo + bot_commands routing). dashboard_button triggers need a small API endpoint + Vue button in a dashboard page. Both are separable from the producer-side install flow; the controls bridge proves the install pipeline works without them. The trigger definitions are stored on the catalogue row so step 4 just reads from there.

## May 13th, 2026 - Recipes step 2: manifest JSON Schema + PHP validator + canonical Coin Flip manifest

- Step 2 of the Recipes build order is "Define the Recipe manifest shape. JSON schema for which primitives, how wired, what controls are exported. Version field from day one. Hand-authored, no UI." This lands the formal contract that step 3's RecipeInstaller will read from and that future third-party recipe authors will publish against.
- The doc is explicit that "once recipes are shared, the manifest shape becomes a contract you cannot break" - so the manifest was authored as a real JSON Schema artifact (resources/recipes/recipe-manifest.schema.json, draft-2020-12), not Laravel-style validation rules in PHP. A standalone schema is the right shape for the Claude-auditable-before-publishing story the doc calls for: any third-party can run the same schema against their manifest before publishing, with no Overlabels code path involved.
- Composer dep: opis/json-schema ^2.6. Well-maintained, supports draft-07/2019-09/2020-12, no transitive baggage. Picked over justinrainbow/json-schema (older, draft-04 era) and league/json-schema (less complete).
- Schema encodes the locked decisions from the doc: integer recipe_format_version (const 1 for now; bumps create new installable artifacts, never in-place upgrades), snake_case slug pattern matching the existing control-namespace rules, sectioned primitives object (option_sets + pickers arrays), permission enum mirroring BotCommand::PERMISSION_LEVELS exactly (everyone/subscriber/vip/moderator/broadcaster - 'broadcaster' covers the streamer_only case so we keep one vocabulary across Bot Expressions and Recipe triggers), control_exports as {name, from} with from constrained to pickers.<ref>.{result|result_at|running} (the three picker fields actually supported today; locked by regex so a manifest trying to export a made-up internal field fails at validate-time), and triggers as oneOf chat_command/dashboard_button (EventSub and scheduled deliberately deferred per the doc). Every object uses additionalProperties: false so accidental typos in field names are caught at validate-time rather than silently ignored.
- App\Services\Recipes\RecipeManifestValidator: thin layer over opis with two responsibilities. (1) JSON Schema validation via opis, formatted to a flat list of {pointer, message} using opis's ErrorFormatter with formatErrorMessage so placeholders like {pattern} and {keyword} resolve to actual values in the output. (2) Semantic cross-reference checks that JSON Schema can't express on its own: picker.option_set_ref points at a declared option_set ref, control_export.from references a declared picker, trigger.fires references a declared picker, and refs/export-names are unique within their list. Returns {valid: bool, errors: list<{pointer, message}>} - flat list shape so a future builder UI can render each error next to the offending field without walking a nested tree.
- Canonical Coin Flip manifest lives at resources/recipes/coin_flip/manifest.json. ~40 lines of pure data: one option_set (coin/Heads-Tails), one picker (flipper, reject_if_running), three control_exports (result, result_at, running), two triggers (!flip chat command with 10s cooldown + "Flip the coin" dashboard button). This is the first-party example and the validation surface for step 3 (RecipeInstaller will materialise this manifest into owned option_set + picker + overlay_control rows for the installer).
- 17 new Pest tests in tests/Feature/RecipeManifestValidatorTest.php cover: happy-path Coin Flip acceptance (both validate() and validateFile()), slug rejection (dashes, leading digit), unknown recipe_format_version, unknown top-level properties (catches accidental "renderers" keys from anyone who skim-reads the doc), all four cross-reference checks (undeclared option_set, undeclared picker in control_export, undeclared picker in trigger, all three duplicate-ref shapes), enum violations (permissions, concurrency), unsupported picker field in from, chat command without leading !, and malformed JSON input rejection. 580 tests green (was 563).
- Deliberately deferred from this slice: (1) recipes DB tables - that's step 3's RecipeInstaller; manifests today are JSON files only. (2) Admin-configurable 100-item OptionSet cap and per-user instance cap - will land alongside the install flow. (3) max_instances_per_user enforcement - currently a stored hint; will be enforced when install actually creates instance rows. (4) The Recipes-Manifest-Spec.md doc the primer mentions - the schema file itself is the formal contract now; a human-readable spec doc can be generated from it later.

## May 13th, 2026 - Recipes ground work: OptionSet + Picker primitives land with concurrency-safe fire() and PickerLanded broadcast

- Step 1 of the build order in `resources/help/reference/Recipes.md` is "primitives: Picker, OptionSet, triggers, control writes, emitted events." This commit lands the first two primitives and the broadcast event so the producer side of the controls-as-bus architecture has a foundation to build on. Triggers and the control-write bridge are deferred to subsequent passes once the manifest schema (step 2) tells us what shapes they actually need. No user-facing UI yet - power-user / tinker access only, exactly as the doc specifies.
- Two new migrations under default Postgres connection: `option_sets` (user_id FK, snake_case slug unique per user, jsonb items array, min_items/max_items, user_editable) and `pickers` (user_id, option_set_id FK, slug, consume_on_pick + jsonb consumed_indices for raffle mode, concurrency enum, last_result/last_result_at/is_running for the lazy state-mirroring strategy the recipe install layer will read from later). Slug pattern matches the existing OverlayControl convention (`/^[a-z][a-z0-9_]{0,49}$/`) - dashes explicitly banned because dashes inside `[[[c:...]]]` tag context have bitten the user before (the gps integration was renamed away from `overlabels-gps` for this exact reason).
- `App\Models\OptionSet`: thin Eloquent wrapper, items cast to array, `pickers()` hasMany, `SLUG_PATTERN` constant exposed for future controller-level validation.
- `App\Models\Picker`: holds the `fire()` core. Uses `DB::transaction` + `lockForUpdate` so two simultaneous `!flip` chat commands can't both grab the same picker and race the result. Concurrency modes are constants on the model (`CONCURRENCY_REJECT` / `CONCURRENCY_CANCEL` / `CONCURRENCY_ALLOW`); v1 only honours `reject_if_running` since picks are atomic, but cancel/allow are reserved so animated pickers (the future Wheel) don't need a schema change. `consume_on_pick` appends the picked index to `consumed_indices` and excludes it from future draws until `resetConsumed()` clears the list. Empty pickable set returns null instead of throwing, so consumers can decide whether "nothing left to pick" is an error or expected (raffles deliberately exhaust).
- `App\Events\PickerLanded`: implements `ShouldBroadcast`, broadcasts as `picker.landed` on `alerts.{twitch_id}` - the same channel the existing OverlayRenderer Echo subscription already listens on, so when the renderer eventually wires up picker-event handling it won't need a second channel subscription. Payload carries `picker_id`, `picker_slug`, `result`, and `result_at` (Unix seconds, matching the platform-wide timestamp contract).
- Smoke-tested end-to-end via tinker: 20-fire distribution on a `[Heads, Tails]` set returned 13/7 (RNG behaves), 3-item `consume_on_pick` picker exhausted cleanly and returned null on the 4th call, `resetConsumed()` restored the picker, and `Event::fake` verified `PickerLanded::dispatch()` carries the right `result`, `pickerSlug`, and `broadcasterId`.
- Deliberately deferred from this slice (each is its own design pass): trigger primitive table (4 kinds in the doc, each kind has its own wiring story - chat command needs bot routing, scheduled needs a tick scheduler, dashboard button needs an API endpoint), control-write bridge from picker state to `overlay_controls` rows (recipe install layer's job), and `min_items`/`max_items` enforcement on user-edited items (matters once the install flow validates user edits against manifest bounds). Admin-configurable 100-item OptionSet cap will land alongside the manifest schema rollout.

## May 13th, 2026 - Triggers tab gains filter bar; layout extracted into a shared shell that the Targeting tab now also uses

- With the Twitch/external split fixed (earlier commit today), the Triggers tab now hosts a growing list of toggles - Twitch alone is ~28 event types, plus 7 external services each adding 1-6 more. At ~40+ rows the page already needs a filter input, and every other long-list surface in the app (TemplateTagsList, ControlsManager, ControlPanel) already has the same shape: Search icon + input, a counter line ("X items in Y groups"), an expand/collapse-all toggle, and per-group `Collapsible` cards with chevrons. Copy-paste-and-tweak across four sites is the wrong shape - extracting the shell pays for itself the moment the next list surface needs the same pattern.
- New `FilterableGroupedList.vue` component owns the filter input, filtered counter, expand-all toggle, localStorage-backed expansion state (`expandedStorageKey` prop), and renders each group as a `Collapsible` with the canonical header (chevron + slot icon + label + count badge). Slots for `description`, `header-actions`, `group-icon`, `item`, `footer`, `empty`, and `empty-search` cover the variations between consumers. A `flat` prop suppresses group headers entirely for single-list surfaces like AlertTargetOverlaySelector (only filter + counter + items + footer render). Generic over the item type (`<script setup generic="TItem">`), so consumers get full type-safety on the slot prop.
- `TriggerManager.vue` was rewritten on top of the shell. The per-row UI (toggle + label + key + duration input) moved into a small `TriggerRow.vue` with `v-model:enabled` and `v-model:duration-ms` plus a single `save` emit. Groups are computed as `Twitch` first + one entry per connected external service; each group's badge shows enabled-vs-total ("3/8") so the user sees activity at a glance. Filter searches against event label, event type, service, and the rendered key text, so typing "kofi", "donation", "stream", or "channel.cheer" all surface the right rows. Expansion state persists under the same `triggers_manager_expanded` localStorage key as before, so users who had Twitch collapsed and Ko-fi expanded keep that state.
- `AlertTargetOverlaySelector.vue` was the next-most-tortured tab and got the same treatment. Previously a hand-rolled flat list with bespoke checkbox-row markup and a footer phrased "1 overlay selected / All overlays". Now uses the shell in `flat` mode: same filter bar, same counter ("X overlays" / "Showing X of Y"), same input-border row styling, and a `footer` slot for the selection state with a small Target icon. Filter is real - typing "alert" or part of a slug surfaces matching overlays. The hover/selected state preserves the green-outline visual that signals "this overlay will receive the alert."
- Mutation-safety subtlety worth noting for the next refactor: TriggerRow uses `defineModel` for `enabled` and `durationMs` (not direct prop mutation), so Vue's reactivity is the source of truth and ESLint's `vue/no-mutating-props` rule stays happy. The parent (`TriggerManager`) owns the underlying `Row[]` arrays; the row component just renders them and emits updates back via the two v-models.
- No backend changes - the existing `templates.triggers` PUT controller still drives saves, all 8 `TemplateTriggersTest` cases still pass. Build verified clean; haven't browser-tested the Triggers/Targeting tabs but the surface area is small enough that the unit-level tests + the lint pass + the build success cover the regression risk. User should sanity-check the two tabs end-to-end on next dev session.

## May 13th, 2026 - TriggerManager: external events no longer leak into the Twitch group; BMAC and Overlabels GPS now selectable

- User flagged that the Triggers tab on an alert template was rendering Ko-fi, Streamlabs, Fourthwall, and BMAC events under the Twitch group header. Worse, even if a user had toggled one of those on (e.g. `channel.kofi.subscription`), nothing fired - the external alert pipeline broadcasts under `service:event_type` keys via `ExternalEventTemplateMapping`, so a Twitch-shaped key like `channel.kofi.subscription` could never match an incoming Ko-fi event. Dead labels that took up real estate in the catalogue and confused the mental model of the page.
- Root cause was `EventTemplateMapping::EVENT_TYPES` (the constant that backs the "Twitch" group in `TriggerManager.vue`) carrying 11 stale entries dating back to before the external-integration pipeline existed: `channel.kofi.*`, `streamlabs.*`, `fourthwall.*`, `bmac.*`. The Vue component iterates the map verbatim, so it had no way to tell those weren't actually Twitch events. The fix is structural - the catalogue is now Twitch-only, with an inline docblock pointing readers at `ExternalEventTemplateMapping::SERVICE_EVENT_TYPES` for the external surface so the next dev doesn't accidentally re-introduce the same pollution.
- `ExternalEventTemplateMapping::SERVICE_EVENT_TYPES` was missing two services whose drivers existed and dispatched events daily: `bmac` (6 event types - donation, recurring, extra, membership, wishlist, commission) and `gps` (4 event types - location_update, session_start, session_end, settings_sync). Added both, with labels matching each driver's `getSupportedEventTypes()`. Also re-sorted the map alphabetically by service key and fixed the `gpslogger` label that previously read "GPS Location Update" (now "GPSLogger Location Update") to disambiguate it from the new Overlabels GPS driver.
- No frontend code changes needed - `TriggerManager.vue` already splits Twitch vs external groups correctly using `connectedServices` and `SERVICE_LABELS` (which already had entries for all 7 external services including `gps: 'Overlabels GPS'`). The bug was entirely in the backend catalogue shape.
- Self-healing for existing data: if any prod `event_template_mapping` rows hold a now-removed key like `channel.kofi.donation`, the next save through the Triggers UI deletes them via the controller's existing `whereNotIn($keptTwitchEventTypes)` sweep. Those rows weren't firing anyway, so no manual cleanup is needed.

## May 13th, 2026 - Production PHP upload limits aligned with the Cloudinary endpoint's 10MB validation

- User reported a hard-to-debug 422 on `/kits/create` when pasting a screenshot. Network response was the generic Laravel `{"message":"The image failed to upload.","errors":{"image":["The image failed to upload."]}}`. The wording is the giveaway: that's the implicit `uploaded` rule message, not the `image` rule message. PHP rejected the upload at the SAPI level before Laravel's validator ever ran. Pasted PNGs from a high-res display land in the 3-8MB range, which sails past PHP's stock 2MB ceiling.
- Root cause was the `dunglas/frankenphp:1-php8.4` runtime inheriting the official PHP image's `php.ini-production` defaults (`upload_max_filesize=2M`, `post_max_size=8M`). The Dockerfile copied a Caddyfile and an entrypoint but never wrote any PHP ini overrides, so the prod container ran with limits a third of what the Laravel-side validator expected (`max:10240` = 10MB on `CloudinaryUploadController`). Symptom only showed up under specific conditions (high-DPI clipboard screenshot vs a small drag-dropped JPG), which is why it survived the original Cloudinary backend-routing rollout.
- Fix: new `docker/php-uploads.ini` snippet (`upload_max_filesize=10M`, `post_max_size=12M`) copied into `$PHP_INI_DIR/conf.d/zz-uploads.ini` in the runtime stage. `post_max_size` sits 2MB above the file cap to absorb the multipart envelope and the `kind` form field, so PHP accepts the full 10MB image and Laravel renders any further error (dimensions, mime, abuse) instead of the opaque uploaded-rule message. The `zz-` filename prefix ensures the override loads after anything else in `conf.d/` no matter how PHP orders its scan.
- Frontend tweak in `resources/js/components/ImageDropZone.vue`: error-message fallback chain reordered so `data.errors.image[0]` / `data.errors.kind[0]` are checked before `data.message`. Laravel's 422 envelope always includes the generic "The given data was invalid." (or here, the upload-rule string) as `message`, but the field-specific entry under `errors` is what the user actually needs to see. Before the swap the dropzone surfaced the generic; now it surfaces "Image must be at least 400x400px" or similar verbatim.
- No frontend code changes to `kits/create.vue` or `kits/edit.vue` were needed. Earlier suspicion that they had drifted from `templates/edit.vue`'s upload flow turned out to be a red herring - all three call the shared `ImageDropZone` with the right `kind` prop and have done since the f9a8479 backend-routing refactor.

## May 12th, 2026 - Freesound modal: tags per result + clickable tag chips, sort dropdown

- After the first user got the integration working in prod, three small affordances felt obvious. Tags help disambiguate sounds with similar names ("coin1.wav" with `metal short clean` reads very differently from "coin1.wav" with `pirate treasure deep`). Clickable tags turn the result list into a faceted explorer - click `metal` on a coin sound and you're now searching for metal sounds, no retyping. Sort dropdown lets a user surface short SFX over long ambient clips when looking for an alert SFX.
- `FreesoundClient::SEARCH_FIELDS` now includes `tags`. New `FreesoundClient::ALLOWED_SORTS` constant (`score`, `duration_asc`, `duration_desc`, `created_desc`, `downloads_desc`, `rating_desc`) - the canonical list of values Freesound's Solr index accepts on the `sort` query param. `search()` gained a `$sort` parameter; values outside the allow-list silently fall back to `score` so a malformed input never breaks the API call.
- `FreesoundController::search` validates the optional `sort` against `Rule::in(FreesoundClient::ALLOWED_SORTS)` (returns 422 on an unknown value - any client trying to inject a `sort=evil_injection` hits validation). Re-shape now includes `tags` (defensively coerced to `array_values()` of the array since Freesound occasionally returns `null` or non-list shapes for sound records with no tags).
- `BrowseFreesoundModal.vue`: new sort `<select>` next to the search input (6 options, plain HTML select - per the no-Reka-checkboxes preference plain HTML is reliable here too). Tag chips render as a row under each result's metadata (max 6 per result, hard-capped so a sound with 30 tags doesn't blow up the row height). Each tag chip is a button - clicking it sets the query to the tag text and re-runs search, so the modal becomes a faceted browse interface. Hover state uses violet tint to make the clickability obvious. Sort changes auto-re-run the current query (only if results are already shown - empty modals don't auto-search).
- 2 new feature tests in `FreesoundLibraryTest.php`: tags array flows through to the response, and unknown sort values are rejected with 422. 563 tests green (was 561).

## May 12th, 2026 - Freesound polish: URL-format license handling, auto-set TTS delay to sound length, visible close UX on the modal

- Four bugs surfaced by first real use of the Freesound integration earlier today, all fixed in one pass. The research-agent report on the API claimed `license` came back as a name-string (e.g. `"Creative Commons 0"`, `"Attribution"`), but the live API actually returns license URLs (`http://creativecommons.org/publicdomain/zero/1.0/`, `https://creativecommons.org/licenses/by/4.0/`). The validator and label formatter were both written against the wrong shape, so every save failed with "Sound has a non-commercial license" even on valid CC0/CC-BY sounds, and the modal rendered the raw URL inline next to each result.
- `FreesoundController::isCommercialSafeLicense` rewritten to check URL forms first (`publicdomain/zero` → CC0, `/licenses/by/` → CC-BY) and explicitly reject `/by-nc`, `/by-nd`, `/by-sa`, `noncommercial`, and `sampling` modifiers *before* the by-check so a URL like `/licenses/by-nc/4.0/` cannot sneak through. Name-string variants kept as fallback in case the API surface ever flips back. 3 new feature tests lock in URL-format coverage: CC0 URL accepted, CC-BY URL accepted, CC-BY-NC URL rejected. 561 tests green (was 558).
- `licenseShort()` in both `BrowseFreesoundModal.vue` and `templates/edit.vue` now maps URL-format licenses to short badge labels (CC0 / CC-BY / CC-BY-NC / CC-BY-SA / CC-BY-ND / Sampling+). The fallback returns an opaque "CC" badge instead of the raw URL when an unrecognised license-shaped URL comes through, so search results never leak a `http://...creativecommons.org/...` string into the UI.
- TTS delay auto-set on sound selection. When the user picks a sound via the search modal (`onFreesoundSaved`) or clicks "Use for this alert" on a library card (`useLibrarySound`), `form.tts_delay_ms` is set to `Math.ceil(duration_seconds * 1000)`. The assumption is: if the user is choosing this sound, its length is the right reference for the TTS delay - voice starts right after the SFX. Users can still hand-tune the value after. Clearing the alert sound (via the inline Clear button or by removing the source library entry) also resets the delay to 0 in the same gesture.
- Modal close UX: the previous version relied entirely on `Modal.vue`'s default × button (which is small, top-right, and visually competes with whatever the modal slot puts in its own header). Added an explicit `X` icon button in the BrowseFreesoundModal's header next to the title plus a visible "Close" button in the footer next to the pagination. Library-count indicator moved from a separate right-side block into the descriptive paragraph (was visually overlapping with the inherited × button anyway). Three working ways to close: the new header X, the footer Close button, or ESC.
- The `FREESOUND_CLIENT_ID` / `FREESOUND_CLIENT_SECRET` env vars remain unused (reserved for a future OAuth path); the integration runs entirely on `FREESOUND_API_KEY` static-token auth. Search filter still uses Solr name-string syntax (`license:"Creative Commons 0" OR license:"Attribution"`) - the index is keyed off name strings even though the response gives URLs, so the filter still does its job.

## May 12th, 2026 - Freesound integration: in-app search, per-user library, attribution surface, Sound tab redesign

- The right way to close the loop on "where do alert sounds come from" turned out to be much simpler than the original plan: a research pass on the Freesound API ToS surfaced that **re-hosting their content on a third-party CDN is explicitly prohibited**, and commercial API use requires a separately negotiated agreement with UPF. That kills the "mirror sounds to Cloudinary" approach the user originally outlined. The clean alternative: hotlink Freesound's own public `preview-hq-mp3` URLs (no auth needed, served from their CDN, 128kbps MP3 which is overkill for an alert SFX). We store only metadata locally - never audio bytes. No transcoding, no storage, no quota system at the byte level, no DMCA exposure.
- Architecture is "hotlink-only" end to end. `FREESOUND_API_KEY` in `.env` is the static API key (one app key shared across all users - no OAuth flow because we don't hit `/sounds/{id}/download/` which is the only OAuth-gated endpoint). `App\Services\Freesound\FreesoundClient` is a 100-line HTTP wrapper around two endpoints: `/apiv2/search/text/` and `/apiv2/sounds/{id}/`. The license filter `license:"Creative Commons 0" OR license:"Attribution"` is appended server-side so **NonCommercial-licensed sounds physically cannot reach the user's search results** - the frontend has no path to surface them.
- New `user_freesound_sounds` table: `user_id`, `freesound_id`, `name`, `author`, `license`, `preview_url`, `duration`, `freesound_url`, timestamps. Unique on `(user_id, freesound_id)` so re-saving the same sound is idempotent. Hard cap of 10 saved sounds per user enforced in `FreesoundController::save` - over the cap we 422 with a clear message ("Your sound library is full..."). The cap is a product lever for a future paid tier ("Free: 10 sounds. Pro: unlimited library + custom uploads"). Storage cost of 10 metadata rows per user is effectively zero, so the cap is positioning, not economics.
- `FreesoundController` is three thin endpoints: `GET /freesound/search` (proxies the API, re-shapes hits down to the 7 fields the modal actually renders), `POST /freesound/library` (re-fetches the sound from Freesound at save time so the client only controls the ID - everything else comes from the canonical record), `DELETE /freesound/library/{sound}` (per-user-owned). The save endpoint runs `isCommercialSafeLicense()` as defence in depth even though the search filter already excludes NC sounds; an unauthenticated direct `POST` with a smuggled `freesound_id` for a NC sound is rejected before it lands in the DB.
- Sound tab redesign was the user's other ask. Previous layout was a `max-w-3xl` wall of paragraph-sized explainer text with three inputs jammed between them - no hierarchy, hard to scan. New layout is a 3-column grid: left 2/3 holds two stacked sections (**Alert sound**: URL input + Browse Freesound button + attribution box when the sound is from the library, then **Text-to-speech**: expression textarea + delay input + tags + a `<details>` "How do I mute TTS?" disclosure), right 1/3 holds the **Your sounds** library card-stack. Section headers (`text-base font-semibold`) give scannable structure; the wall-of-text explainers collapsed into single hint lines or details-summary disclosures.
- `BrowseFreesoundModal.vue` is the search surface: search input → results list with a singleton audio audition (clicking play on a new result auto-stops the previous one, same singleton-cancel pattern the overlay renderer uses for alert playback), license badges, "Use & Save" button per result. Save adds to library, sets `form.alert_sound_url` to the preview URL, closes the modal. Pagination via prev/next buttons for queries with >15 hits.
- The Sound tab library card shows each saved sound with a play/pause audition button, name + author + license badge + duration, and either a "Use for this alert" button or an "In use for this alert" indicator (with a violet ring around the card) depending on whether `form.alert_sound_url` matches its `preview_url`. The trash icon removes from library; if the removed sound was the one being used by the alert, `alert_sound_url` is cleared in the same gesture.
- Attribution box appears in the Alert Sound section when the current URL matches a library entry. Shows name + author + CC0/CC-BY badge + "Freesound" link to the canonical sound page. This is required for CC-BY sounds (the standard CC practice is sound name + author + Freesound URL + license, all four visible somewhere the viewer can reach), polite for CC0. Right now this only renders in the editor - whether to also surface attribution on the overlay/public-preview page itself is a follow-up.
- 9 new feature tests in `tests/Feature/FreesoundLibraryTest.php`: search proxy injects the CC0+CC-BY filter; search requires auth; save/idempotency/cap/license-rejection paths; destroy enforces per-user ownership (404 for foreign users); missing API key returns 502. `Http::fake(fn () => ...)` was preferred over URL-pattern fakes because the closure form matches any URL regardless of how Laravel encodes spaces (`%20` vs `+`), which is the kind of detail that wastes 20 minutes to debug. 558 tests green (was 549).
- ToS-safe by design. We display Freesound CDN URLs in the user's overlay; the user's viewers stream the audio directly from Freesound. We never store the bytes. The API itself is read-only-from-Overlabels's-perspective. The CC license filter ensures every sound that lands in a user's library can be redistributed (CC0) or attributed-and-redistributed (CC-BY). The user's `FREESOUND_CLIENT_ID` / `FREESOUND_CLIENT_SECRET` are still in `.env` but unused - kept for a possible future OAuth path if we ever want per-user rate limits or original-file downloads.
- Deliberately deferred: per-overlay attribution display (currently only shown in the editor, but CC-BY arguably requires viewer-visible attribution too - a tiny "Sound by X via Freesound" line in the overlay corner is a v2 if anyone asks), custom user-uploaded sounds (would put the DMCA story we deferred earlier into a paid tier), library import/export between users, sound previews in the templates index (we'd need to fetch all the user's library rows on more pages), and a global "browse a curated overlabels-picked set" page (the CC0 corner of Freesound is large enough that streamer-driven search beats a curated list). The integration is intentionally small - one API surface, two endpoints, one feature, no creeping scope.

## May 12th, 2026 - Alert sounds get a first-class field, singleton playback, mount-time preload, and a hard ban on raw `<audio>` in template HTML

- The first user dropping a literal `<audio src="https://jasper.monster/...mp3" autoplay></audio>` into an alert body discovered the disaster mode the hard way: replaying the trigger 20x in a row stacked 20 concurrent audio elements, each independently playing their own copy of the sound. The browser doesn't know they're meant to be exclusive - every dispatch creates a fresh element from the template HTML, all play concurrently, the wash is unlistenable. TTS got a singleton path back when we shipped it; alert sounds got nothing. Fixing that now is the right move because audio-in-alert is the next user's deal-breaker too.
- New `alert_sound_url` column on `overlay_templates` (varchar(2048), nullable). Validated as `nullable|url|max:2048` on store/update. Sits next to `tts_expression` and `tts_delay_ms` because they all belong to the same "what makes noise when this alert fires" surface. No file upload - users point at audio they host themselves on Cloudflare R2 / Backblaze B2 / GitHub Pages / a personal CDN. Overlabels does not host audio: the legal/copyright/hosting-cost combo for streamer-uploaded sounds (think 50 Cent tracks, copyrighted myinstants clips) is exactly the kind of liability a hobby project doesn't take on.
- `AlertTriggered` gained `?string $alertSoundUrl = null` (constructor + `broadcastWith()` field `alert_sound_url`). All 3 dispatch sites pass `$template->alert_sound_url`. The field exists on the broadcast as a fallback only - normally the overlay reads the URL from the mount-time preload map, but a brand-new alert template created mid-session that isn't in that map yet falls back to the broadcast value (cold cache, no preload speedup, but it plays).
- Mount-time preload was the user-discovered ~1s latency win and it's the part of this change that actually matters in production. New `alert_sound_preload` map (`{ alert_template_slug: url }`) ships in the authenticated overlay endpoint's JSON, built the same way `alert_css_preload` already was: for every alert template the user owns that could fire on this static overlay (no targeting = fires everywhere, or explicitly targets this overlay), include the sound URL. On mount, `OverlayRenderer::installAudioPreloadLinks` deduplicates origins and emits one `<link rel="preconnect" crossorigin="anonymous" href="$origin">` per unique audio host, plus one `<link rel="preload" as="audio" href="$url">` per file. By the time the first alert fires the browser has already done the TCP+TLS handshake AND fetched the audio bytes, so playback is effectively instant.
- Singleton audio player in `OverlayRenderer.vue`: a single module-scoped `alertSoundPlayer: HTMLAudioElement | null` ref. `playAlertSound(url)` pauses + resets the previous player before creating a new `Audio()` and calling `.play()`. The cancel-on-new mechanism is the same shape we landed for TTS earlier today; same reasoning (rapid-fire alerts shouldn't pile up overlapping copies of their sounds). `play()`'s promise is caught so the browser autoplay-policy rejection that hits in regular tabs (not OBS browser sources) logs once instead of crashing the renderer.
- `HtmlSanitizationService` now strips `<audio>`, `<video>`, and bare `<source>` tags from template HTML/CSS/head fields on save, alongside the existing strips of `<script>`, `<iframe>`, `<form>`, `<button>`, etc. The reasoning matches: raw `<audio>` in template HTML is structurally incapable of behaving correctly (no singleton, no preload, no cancellation, no playback control), so the only supported route is the new `alert_sound_url` field. Existing alerts that already have `<audio>` tags continue to render until next save, at which point the sanitizer strips them. This is a small breaking change relative to the v0.1.0 prototype; documented here so anyone reading later doesn't think their alert lost audio mysteriously.
- Editor surface: the existing "TTS" tab on `templates/edit.vue` is renamed to "Sound" and now hosts three fields in order: alert sound URL (most prominent, with placeholder + explainer covering BYOH hosting and the autoplay-stack reasoning), TTS Expression (existing), TTS Delay (existing). The tab icon stays `Volume2` because it already covered the surface. Hint text under the URL field calls out the preload mechanism so users understand why the first play is fast.
- 4 new feature tests in `AlertTtsExpressionTest.php`: alert_sound_url ships through `ExternalEventController::replay`, defaults to null when the column is unset, surfaces correctly in `broadcastWith()`, and `HtmlSanitizationService::sanitize()` strips `<audio>` / `<video>` / `<source>` from input HTML. 549 tests green (was 545).
- Deliberately deferred: curated CC0/royalty-free sound library hosted by Overlabels (Mixkit/Pixabay sourced, dropdown in the editor for the "I just want SOMETHING to play" case - planned as v2 because shipping BYOH first unblocks the current user today), per-alert volume control (need to see if anyone asks before adding the surface), a global "alert sounds enabled" boolean control mirroring the `tts` gate (skipped because muting via OBS source-level audio is already universal in practice), and any kind of host whitelist or content-type sniffing (trust the user, let the browser fail visibly).

## May 12th, 2026 - Per-alert TTS playback delay so the alert sound can play before the voice

- The first cut of TTS spoke the rendered string the moment the broadcast arrived, which collided with the alert sound/animation every streamer's templates already include. Standard alert design is "noise first, voice second" - and as long as we don't give people a way to push the voice back, every alert is going to sound like two things shouting at once. Fix: a per-alert-template delay value, configurable on the same TTS tab that already holds the expression.
- New `tts_delay_ms` column on `overlay_templates` (unsigned int, default 0, capped at 60000 by the controller validator). Sits next to `tts_expression` because they're authored together and conceptually inseparable - a delay without an expression is meaningless, and an expression without a delay is just the v1 behaviour we already had. Cast to integer on the model so `null` from the DB never sneaks into the JS layer as a string.
- `AlertTriggered` gained an `int $ttsDelayMs = 0` constructor param (clamped to `max(0, value)` in the constructor so a freak negative can never reach the wire) and a `tts_delay_ms` key in `broadcastWith()`. All 3 dispatch sites - `TwitchEventSubController::renderEventAlert`, `ExternalAlertService::dispatch`, `ExternalEventController::replay` - pass `$template->tts_delay_ms ?? 0`. The constructor param defaults to 0 so existing test fixtures that don't care about it keep compiling.
- Overlay side: `OverlayRenderer::speakTts(text, delayMs)` now schedules speech via `setTimeout` when `delayMs > 0`. The pending timer is held in a module-scoped `ttsPendingTimer` ref so a new alert arriving during the delay window cancels the previous alert's not-yet-spoken voice - otherwise a fast double-fire would queue up two delayed `speak()` calls with no way to interrupt the first. `clearTimeout` on the pending timer + `speechSynthesis.cancel()` runs unconditionally before scheduling, which collapses cancel-pending + cancel-in-flight + schedule-new into one path.
- Editor UI: a number input "Wait before speaking (ms)" sits below the TTS Expression textarea on the same tab. Step is 100, min 0, max 60000, body copy explains the "match the length of your alert sound" intent. Form binding uses `v-model.number` so the value reaches the backend as an integer, not a string.
- 4 new feature tests: ships the value through `ExternalEventController::replay`, defaults to 0 when the column is absent, exposes the field through `broadcastWith()`, and the negative-input clamp. 545 tests green (was 540).
- Deliberately deferred: a per-user "TTS delay multiplier" global setting (so the streamer can globally slow down or speed up TTS without editing each template), per-mapping override (different delay for the same template depending on which event triggered it), and a "test play this expression with the delay" button in the editor. None of those are worth building before we see whether the per-template delay is actually the right level of control.

## May 12th, 2026 - TTS gate accepts a `tts` boolean control on any of the user's overlays, not just user-scoped

- The first cut of the TTS gate (earlier today) only matched `OverlayControl` rows with `overlay_template_id IS NULL` (user-scoped). But the UI hint I shipped told streamers to add the boolean from the Controls tab on a static overlay, which creates a *template-scoped* control - so toggling it never gated TTS. Caught by the first user to actually try it.
- Loosened `AlertExpressionRenderer::isGatedOff()` to a single existence query: `OverlayControl::where(user_id, $user->id)->where('key', 'tts')->where('type', 'boolean')->where('value', '0')->exists()`. Any boolean `tts` control owned by the user that's set to off mutes TTS - it doesn't matter which overlay it lives on. Mental model lines up with what streamers expect: one switch, anywhere, mutes TTS.
- Updated the TTS-tab explainer copy in `templates/edit.vue` to match: "Add a boolean control with the key `tts` to any of your overlays. When the control is off, TTS is skipped for *all* your alerts." Removed the misleading "user-scoped" jargon.
- New test in `AlertTtsExpressionTest`: a `tts=0` boolean control with a non-null `overlay_template_id` (i.e. attached to a specific overlay) now also gates correctly. Existing user-scoped test still passes - same path, different `overlay_template_id` value.

## May 12th, 2026 - TTS Expressions: per-alert-template spoken-line composer, gated by a reserved `tts` boolean control

- New "TTS Expression" field on every alert template - a short templated string the server renders at dispatch time and ships in the `AlertTriggered` broadcast payload as `tts_text`. The overlay calls `SpeechSynthesisUtterance` with that string when an alert arrives, so streamers can finally have their resub / cheer / donation alerts speak a tailored line out loud without bolting on a third-party TTS service. Originally sketched as a "TTS Control" in the issue thread, but the right shape turned out to be: rendered string in the payload (so it can also be reused as plain text by any consumer), authored with the same Bot-Expressions-shaped editor, gated by a single boolean.
- New `tts_expression` text column on `overlay_templates` (nullable, only meaningful when `type='alert'`). Validated on `OverlayTemplateController::store/update` as `nullable|string|max:2000`. The HtmlSanitizationService allowlist deliberately does *not* include this field - tags inside `[[[...]]]` are inputs to the resolver, never raw HTML, and the renderer's single-pass guarantee keeps user content from being interpreted as template syntax.
- New `App\Services\Expressions\AlertExpressionRenderer`. Same single-pass regex (`/\[\[\[([\w.:\-]+)(?:\|([\w.:\- ]+))?]]]/`) as `BotExpressionResolver`, same "tags never reparse" day-one rule, but the lookup table is the flat `$templateData` payload that's already built for the alert. `event.user_name`, `event.streak_months`, bare Twitch tags like `user_name`, and `c:key` / `c:service:key` control references all work identically to how they resolve in the alert HTML. Output is capped at 500 characters; empty/whitespace-only results render as `null` so the broadcast doesn't ship junk.
- Pipe-formatter logic was duplicated between the resolver-to-build and the resolver-just-built, so I extracted it once into `App\Services\Expressions\ExpressionFormatter::apply(string $value, string $pipe, string $locale)`. `BotExpressionResolver` now delegates; `AlertExpressionRenderer` uses the same helper. All 8 formatters (`round`, `number`, `currency`, `date`, `uppercase`, `lowercase`, `distance`, `duration`) are shared. The 24 existing bot-expression tests are still green, so the refactor is transparent to existing callers.
- `AlertTriggered` event gained a `?string $ttsText` constructor param and a `tts_text` key in `broadcastWith()`. All three dispatch sites pass it through after rendering: `TwitchEventSubController::renderEventAlert`, `ExternalAlertService::dispatch`, and `ExternalEventController::replay`. The constructor parameter is optional (defaults to `null`), so any call site that doesn't care - tests, future internal callers - keeps working without changes.
- Gating uses a reserved key convention: if the user owns a user-scoped (overlay_template_id null) boolean control with key `tts` and its value is "0", the renderer short-circuits and returns `null`. Absence of the control means TTS is on - the default for any new user. There's no special tag the streamer needs to put in the alert HTML; one switch in the controls panel mutes all of their alerts globally. Documented in the editor's TTS tab so the muting workflow is discoverable.
- Editor surface lives on `templates/edit.vue` as a new "TTS" tab (alert-only, Volume2 icon, 9th tab after Triggers/Targeting). Multi-line textarea bound to `form.tts_expression` (max 2000 chars), inline hint listing the existing extracted `template_tags` as clickable chips (click to append `[[[tag]]]` to the expression), and a small explainer card on how to mute via the `tts` control. No live preview pane for v1 - the expression shares the same tag set as the alert HTML so authors can iterate by editing the HTML body and replaying a stored event.
- Overlay-side wiring in `OverlayRenderer.vue::handleAlertTriggered` is intentionally tiny: read `alertData.tts_text`, bail if not a non-empty string or `window.speechSynthesis` is missing, otherwise cancel any in-flight utterance and `speak(new SpeechSynthesisUtterance(text))`. Cancel-on-new matters because back-to-back alerts (raid + sub-train) would otherwise queue a backlog of speech the streamer can't catch up with. Errors are caught and logged to console - TTS failures should never crash the overlay.
- 6 new feature tests in `tests/Feature/AlertTtsExpressionTest.php`: rendered string ships when expression is set; `tts_text` is null when expression is null; null when expression resolves to empty (e.g. unknown tag); suppressed when the `tts` boolean control is "0"; permitted when "1"; `broadcastWith()` exposes the field positionally. Tests drive the `ExternalEventController::replay` path because it's the shortest route to a real broadcast without faking webhook signing.
- Deliberately deferred: per-alert-type TTS expressions (currently one per template - if you have one alert template serving two event types via two mappings, both will say the same thing), a live preview pane on the TTS tab, a voice picker (currently uses the browser's default SpeechSynthesisUtterance voice), and conditional / if-else syntax inside the expression. All four are follow-ups gated on real usage feedback rather than guesses.

## May 11th, 2026 - "Powered by Overlabels" watermark on every public-facing screenshot, via Cloudinary URL transformation

- Earlier today the public preview started showing the screenshot as both the on-page hero and the OG share image. Both are valuable surfaces for branding, so we now stamp a "Powered by Overlabels" watermark on every public-facing rendering of the screenshot. The transformation lives in the delivery URL only - the original asset stays clean - so owners on the edit screen continue to see their raw upload and never get confused about what they actually saved.
- New `CloudinaryUploadService::brandedUrl(?string $url): ?string` helper injects an `l_powered-by-overlabels_z4l5ne,w_200,o_90,g_south_east,x_24,y_24` segment right after `/image/upload/` in the delivery URL. Cloudinary composites + caches the result on their CDN, so subsequent hits are free. The helper passes through unchanged when given null or a non-Cloudinary URL, which keeps the function safe to call unconditionally.
- `OverlayTemplateController::servePublic` runs the screenshot URL through `brandedUrl()` in two places: the `screenshot_url` prop on the Inertia page (which feeds both the left column hero and the fullscreen dialog) and the `og:image` meta tag (which is what every shared link will surface). Both surfaces now carry the watermark; both keep their full-resolution + correct-aspect-ratio behaviour because we're not resizing the source - just compositing on top.
- The watermark itself is a 300x80 transparent violet PNG uploaded by the user to public_id `powered-by-overlabels_z4l5ne`. We render it at 200px wide (about 2/3 of source), 90% opacity to soften it slightly against busy backgrounds, anchored south-east with a 24px margin. The values are constants in the service so they're trivial to tune.
- Free-tier math: Cloudinary's free plan allows image overlays as a core transformation (not a paid feature). The 25-credit/month cap covers ~25k transformations or 25 GB CDN delivery; for a hobbyist tool that's plenty, and once a particular (screenshot + transformation) URL has been hit, every subsequent hit is served from CDN cache at zero credit cost.
- Untouched on purpose: `templates/edit` Screenshot tab (raw URL - owner-facing), `templates/show` (still raw - might brand later if visitor-shareable). The watermark surfaces only where a not-yet-Overlabels user might first encounter Overlabels.

## May 11th, 2026 - Per-overlay OpenGraph + Twitter card metadata on the public preview, with the screenshot as the share image

- Until now every Overlabels URL shared the same hardcoded OG tags from `resources/views/app.blade.php` - same title, same description, same brand image. Linking a public overlay preview in Discord/Twitter/Slack showed the generic "A live overlay DSL for Twitch streamers" card every time, which is useless when the whole point of the public preview is to make a *specific* overlay shareable. The screenshot we now require at creation is the obvious thing to put on the card.
- Inertia's client-side `<Head>` component can't help here because social-media scrapers don't execute JS. The fix is server-side: the controller builds an `$og` array and shares it into the root blade view via `view()->share('og', [...])`, and `app.blade.php` now merges that with a defaults dictionary (`array_merge($ogDefaults, $og ?? [])`). Every existing page keeps its existing OG tags because the defaults are identical to the previous static values - the override only kicks in when a controller opts in.
- `OverlayTemplateController::servePublic` populates the per-overlay payload: `og:title` reads "{name} - Overlabels {overlay|event alert} by {owner}", `og:description` is the overlay's own description or a synthesised "A Twitch {type} by {owner} on Overlabels..." sentence when the field is empty, `og:url` is the canonical `route('overlay.public', $slug)`, `og:image` is the Cloudinary screenshot URL when present and falls back to `https://res.cloudinary.com/dy185omzf/image/upload/v1771771091/ogimage_fepcyf.jpg` for legacy overlays without one. `twitter:card` stays `summary_large_image` either way, and `og:image:alt` switches between screenshot-specific and brand-generic copy depending on which image is in play.
- Tidy-ups in the blade: added a previously-missing `<meta name="description">` for non-OG SEO, dropped the static `og:image:width` / `og:image:height` since per-overlay screenshots have unknown dimensions (modern scrapers fetch the image to read its size anyway), and replaced the em-dashes in the brand defaults with hyphens per the project's copy rule. Any future route that wants per-page social cards now just calls `view()->share('og', [...])` and inherits the same merge behaviour.

## May 11th, 2026 - Public preview ditches the live-render iframe in favour of a raw HEAD/BODY/CSS source viewer

- The first cut of the redesign earlier today put a scaled-down iframe of the unparsed overlay in the right column. Same problem in a fancier wrapper: a browser still interprets the markup, so anything that isn't a clean DOM (template tags inside text nodes, unclosed elements during a typo, custom web components without their loader, CSS that targets the OBS viewport) renders as "half-assed interpretation of a browser" instead of the source the visitor actually wants to read. Public previews should show the code, not pretend to be a stream.
- Replaced the iframe with the exact tabbed-source pattern from `templates/show.vue`: vertical HEAD/BODY/CSS tab strip on the left, `<pre><code>` panel on the right, color-coded icons (pink/cyan/lime), `bg-[#f8f8f8]` light / `bg-[#160e21]` dark for the active panel. Empty sections render `// no HEAD content` etc. Reads identically to the owner's own show page so the visitor and the owner see the same shape of preview, just without CodeMirror highlighting (which is overkill for a read-only public surface and gets messy with embedded blade-ish tag syntax).
- Removed the now-unused infrastructure: `servePublicEmbed` controller method, the `/overlay/{slug}/public/embed` route, the `embedUrl` prop on the Inertia page, the `$showBar` flag on `overlay/render.blade.php` (reverted to its original `@if(!$isParsed)`), and the `ResizeObserver` + `transform: scale(...)` iframe-scaling logic in the Vue component. The `overlay.render` blade view itself has no `view()` callsites left and is effectively orphaned, but left in the tree for now in case any out-of-repo tooling renders it - candidate for a follow-up sweep.

## May 11th, 2026 - Public overlay preview is now a 2-column screenshot + live render page, and screenshots are mandatory at creation

- The old `/overlay/{slug}/public` route just rendered the unparsed template HTML with a floating bottom bar. As more and more overlay logic moves into Controls (whose values only exist at runtime for the authenticated owner), the public preview was rendering garbled half-built nonsense - the `[[[c:...]]]` tags can't resolve without a logged-in session, so a visitor's first impression of an overlay was a broken mess. The fix is structural: pair a static screenshot of how the overlay looks with the unparsed live render of what it is, side by side, and require the screenshot up-front so every newly minted public overlay has a visual ground truth.
- Screenshots are now required by `OverlayTemplateController::store()` via `required|url|max:2048` validation, and the new URL is claimed via `CloudinaryUploadService::claim()` just like the existing `updateScreenshot` flow does (orphan-sweeper leaves it alone). Update is unchanged - owners can still swap or remove a screenshot on the edit page, which means legacy overlays without one keep working and the public preview falls back to a placeholder card.
- New `Screenshot` tab on `templates/create.vue` as the 4th tab after Meta/Code/Tags, wiring `ImageDropZone` directly to a `form.screenshot_url` field (not `TemplateScreenshot.vue`, which PUTs to a separate route - the create flow needs the value bundled into the store request). The two Create buttons (header + footer) stay disabled until the screenshot URL is set, plus a small violet hint "Add a screenshot to enable Create" sits next to the footer button. Submitting without one is also caught client-side: the form auto-switches to the Screenshot tab and throws a warning toast, then the server still rejects with 422 as a backstop.
- New `/overlay/{slug}/public` page is an Inertia page (`resources/js/pages/overlay/public-preview.vue`) deliberately rendered outside `AppLayout` - no sidebar, no breadcrumbs, no AppHeader. The full viewport belongs to the preview. A slim top strip carries the Overlabels logo (links to `/`) and, for authed users, a `Dashboard` link on the right; everything below is two equal columns capped at `max-w-[1800px]` so the screenshot and the live render get real estate to breathe.
- Left column is the screenshot in `aspect-video`. Right column is an iframe pointing at the new `/overlay/{slug}/public/embed` route, which serves the existing `overlay.render.blade.php` view with a new `showBar` flag set to `false`. The blade view's olb-bar block was gated on `@if(!$isParsed && ($showBar ?? true))` so every other callsite of that view keeps its bar; only the embed route loses it. The iframe is rendered at native 1920x1080 and CSS-scaled via a `ResizeObserver`-tracked `transform: scale(width/1920)` so the preview matches what an OBS browser source would actually look like, just shrunk to fit the panel.
- Chrome buttons collapsed into one `Copy...` dropdown. The trigger flashes a `Copied HEAD!` / `Copied BODY!` / `Copied CSS!` / `Copied Complete template!` confirmation for 1.5s after a click, so the "did anything happen?" question stays answered without a toast component. The `Complete template` item builds a self-contained `<!DOCTYPE html>` document with the CSS wrapped in `<style>` and the head field inlined - the kind of thing you'd paste into a new file and open in a browser. The `Screenshot` button (full-screen modal) and the fork-to-account `Copy` / `Log in to copy` button stay separate from the snippet dropdown.
- Style follows the brief: `bg-sidebar` / `bg-card` for surfaces, `border-sidebar-border` for divisions, `text-violet-400` for accents, `text-foreground` for body copy, no `rounded-*` anywhere in the new chrome. Owner avatar, name, type badge, view count, and fork count sit in the header strip; the description renders as a separate card below the two columns if set.
- Backward compatibility: the existing `/overlay/{slug}/public/screenshot` route (which renders the screenshot full-page) is untouched in case anything external still links to it. The `overlay.public` route name didn't change, so every existing link / share button across the app keeps working - they just resolve to the new page now. Templates that pre-date this change and have `screenshot_url = null` render the left column as a placeholder ("No screenshot yet") while the right column still shows the live render, so the page is never blank for legacy overlays.

## May 10th, 2026 - Bot Expressions builder UI: list page, create/edit form with live preview

- The companion UI for the Bot Expressions backend that landed earlier today. Lives at `settings/bot/expressions`, reachable from a "Manage expressions" link in the bot section of `/settings/integrations` (only visible when `bot_enabled` is true). The link is gated on the bot toggle because expressions can be authored without the bot on, but they cannot fire - so the index page also surfaces an amber banner when the bot is off, pointing the streamer back to the toggle.
- New `Settings\BotExpressionsController` with `index`, `create`, `edit`, `store`, `update`, `destroy`, `preview`. Validation rejects builtin collisions (with the user-facing reason "'!set' is a built-in bot command and can't be reused as an expression"), duplicate commands per user, and bad command formats (regex `^!?[a-zA-Z0-9_-]{1,30}$`). Per-user ownership is enforced on edit/update/delete via `abort_unless($expression->user_id === $user->id, 404)` rather than route-binding policies, matching the pattern used elsewhere in the settings controllers.
- The `preview` endpoint runs `BotExpressionResolver::resolve()` in dry-run mode against a stub bot context (`from_user: CoolChatter`, `args: sample arg one two`, etc.) and returns `{resolved, length}`. Twitch tags resolve to empty in preview because dry-run skips the Helix fetch - the UI explains this so the user knows what they're looking at.
- Index page (`resources/js/pages/settings/bot/expressions/Index.vue`) lists each expression as a card with the command in monospace, an enabled/disabled badge, the permission tier, optional cooldown, the literal expression string, and last-fired timestamp. Empty state with a `MessageSquare` icon and a hint to create one. Edit and delete buttons per row; delete uses a native `confirm()` for the v1 because the action is reversible-ish (the user can re-create) and not worth a Dialog component yet.
- Edit page (`resources/js/pages/settings/bot/expressions/Edit.vue`) handles both create and edit. Form: command + expression (textarea) + permission select + cooldown number input + enabled checkbox + hidden_from_commands checkbox. Plain HTML checkboxes per the no-Reka-checkbox preference. Body copy uses `text-foreground` and `text-foreground/70` per the body-copy color preference. Every clickable element wears `cursor-pointer`.
- **Live preview pane** sits between the textarea and the secondary controls. Watches the `expression` form field; debounced 250ms; POSTs to the preview endpoint; shows the resolved string with a character counter (red when over 500). Loading state is `Resolving...`, empty resolves render as `(empty)` italics. The pane explains the `CoolChatter` stub and the Twitch-resolves-to-empty behaviour inline so streamers don't think the preview is broken.
- Tag-discovery helper is a `<details>` collapsible. Three sections: bot-context tags (closed enum, fixed list), the user's available controls (sourced from `OverlayControl` rows for that user, deduplicated and sorted), and a static hint about pipe formatters. Each tag is a button - click inserts `[[[tag]]]` at the end of the textarea. No drag-and-drop, no autocomplete in the textarea itself for v1; that's a follow-up if it turns out to be the bottleneck.
- Build is clean: `npm run build` compiles both new pages (`Index-CaxxLdoP.js`, `Edit-KlG9tYNj.js`). `php artisan route:list` shows all 7 new routes registered. `npm run lint` flags 18 errors but all of them are in pre-existing files, none in the new ones. `php artisan test` still 534 green.
- What's deliberately missing in v1: server-side validation that `c:` references in the expression actually resolve to existing controls (currently the resolver returns empty for missing controls, but the form will save them). Tag autocomplete inside the textarea (the helper buttons fill the gap). A "test fire this expression as me" button (the live preview covers most of what that would do). All three are follow-ups that depend on usage feedback.
- Bot-side dispatch branch in the separate `overlabels-bot` repo is still pending. Until that lands, expressions save and resolve correctly server-side, but no chatter typing `!whatever` will see anything happen - the existing bot ignores command names that aren't in its registered handlers.

## May 10th, 2026 - Bot Expressions: server-side backend for user-authored chat commands that read from the controls layer

- Added the entire backend for Bot Expressions, the feature first sketched in [issue #104](https://github.com/jasperfrontend/overlabels/issues/104). A streamer can now define a chat command (`!distance`, `!shoutout`, etc.) whose response is a templated string that reads from `OverlayControl` values, Twitch Helix data, and per-invocation chatter context. The bot speaks the resolved string. Bot stays dumb: it does not parse tags, does not enforce permissions, does not know about controls. All resolution and gating runs server-side before the row hits `bot_chat_outbox`.
- Mental model: Bot Expressions sit on the **read** side of the controls layer, alongside Alerts and Overlay Templates. Recipes (planned) sit on the write side. The architecture diagram lives in `resources/help/reference/Bot-Expressions.md` and `resources/help/reference/Recipes.md`.
- New `bot_expressions` table: `user_id`, `command`, `permission_level`, `cooldown_seconds`, `expression`, `enabled`, `hidden_from_commands`, `last_fired_at`. Unique on `(user_id, command)`. Per-channel cooldowns only for v1, enforced via `last_fired_at` + broadcaster bypass. Per-user cooldowns are a v2 add when demand surfaces.
- Tag syntax inside `[[[...]]]` matches overlay templates exactly, plus a new `bot:` namespace for invocation context. `[[[c:goal_km]]]` for own controls, `[[[c:kofi:donations_received]]]` for service-managed controls (resolved via `broadcastKey`), bare names like `[[[followers_total]]]` for Twitch Helix data, `[[[bot:from_user]]]` / `[[[bot:args.0]]]` for chatter context. Authors copy-paste freely between overlay templates and Bot Expressions.
- `BotExpressionResolver` is the new server-side template resolver in `app/Services/Bot/`. Net-new code: until now all tag substitution lived in the frontend `tagParser.ts` / `OverlayRenderer.vue`. Same regex shape, same single-pass-by-design rule (substituted values are never re-scanned for tags), same null-over-placeholder rule. Output is hard-capped at 500 characters to stay under Twitch's chat message limit. Pipe formatters supported: `round`, `number`, `currency`, `date`, `uppercase`, `lowercase`, `distance` (m -> km/m/mi/ft), `duration` (dd:hh:mm:ss patterns). Locale comes from `User::preference('locale')`.
- `BotExpressionService` orchestrates: `canFire()` runs enabled + permission tier ladder + cooldown, `fire()` resolves and writes the outbox row, stamps `last_fired_at`. `buildBotContext()` flattens the bot's POST payload into the resolver-friendly shape including indexed `args.0`, `args.1`, ... tokens.
- New internal endpoint `POST /api/internal/bot/expressions/fire`. Bot POSTs chatter context (login, display name, badges, args), Overlabels resolves and queues. Silent-on-block policy: failures return 200 OK with `queued: false` and a reason, no chat reply, so the bot doesn't spam "you're on cooldown" noise. Sits behind the existing `bot.internal` middleware + `throttle:bot-internal` bucket - no new auth surface.
- Extended `BotCommandController::index` to merge `BotExpression` rows into the per-user command map with `type: "expression"`. Existing builtins are now tagged `type: "builtin"`. The bot uses `type` to route: hardcoded JS handler for builtins, POST to the fire endpoint for expressions. On collision (a user authored an expression with the same command as a builtin), builtin wins both at the merge layer and at validate-on-save time. JSON shape change is additive, so an old bot deploy reading the response will still work - it'll just ignore the new field.
- Permission enum reuses `BotCommand::PERMISSION_LEVELS` exactly: `everyone` / `subscriber` / `vip` / `moderator` / `broadcaster`. Tier ladder: a `vip`-badged chatter passes a `subscriber`-tier expression, a `moderator` passes a `vip`-tier, etc. Resolution uses the `badges` array the bot includes in the fire POST.
- 24 new feature tests in `tests/Feature/BotExpressionsApiTest.php` covering auth, lookup, gating (disabled / wrong permission / on cooldown), broadcaster bypass, resolution of all three tag families, single-pass safety, dry-run, length cap, sync-endpoint merge behaviour, and collision policy. One existing assertion in `BotInternalApiTest.php` updated to include the new `type` field. Full suite green: 534 passed.
- Three new reference docs in `resources/help/reference/`: `Bot-Expressions.md` (formal spec, manifest now serves as a build contract), `Recipes.md` (the producer-side primer that frames the controls-layer-as-bus architecture), and `Wheel of Fortune.md` (the first concrete Recipe / Kit candidate). These are design docs for the next chunks of work, not user-facing help.
- Not in this commit (deferred to follow-ups): the Inertia builder UI under `settings/bot/expressions` (substantial frontend chunk - own session), the bot-side dispatch branch in the separate `overlabels-bot` repo (tiny `bot.js` change to recognise `type: "expression"` and POST to fire), and the auto-generated tag-discovery reference page. The Laravel-side contract is concrete and tested, so those follow-ups can land independently.

## May 9th, 2026 - Fix: donation-seed and test-mode reset now target `total_received` instead of `donations_received`, and the duplicated logic moves to `ExternalControlService`

- The donation-goal seed across all five integration controllers (Ko-fi, BMAC, Fourthwall, StreamElements, StreamLabs) was writing the user's "starting amount" into the `donations_received` counter instead of the `total_received` running amount. A streamer entering "30" because they'd already raised €30 before connecting Overlabels was seeding "30 separate donations have happened" - which is nonsense and made downstream donation-goal templates render zero progress until the first real donation arrived. Same bug existed in the test-mode toggle: turning test mode off was resetting `donations_received` to the seed value and `total_received` to 0, exactly inverted. Both paths now write the seed to `total_received` and reset `donations_received` to 0 like every other counter.
- The seed flow and test-mode reset flow were duplicated, near-verbatim, across all five integration controllers. The bug existed in 10 places. Lifted both into `ExternalControlService::seedTotalReceived()` and `ExternalControlService::resetServiceManagedControls()`. Each integration controller's test-mode block and seed action are now 3-line calls into the service. Per-integration files retain everything that's actually per-integration: credential shapes, OAuth/JWT/secret flows, settings keys, connect/disconnect.
- `resetServiceManagedControls(User, source, ?seedValue)` walks every service-managed control for that source, resets `total_received` to the saved seed (or `0` if no seed), resets all other counter/number controls to `0`, resets text controls to `''`, and broadcasts `ControlValueUpdated` per change. The seed parameter is `?string`, with `null` meaning "no seed - reset to 0 like everything else."
- `seedTotalReceived(User, source, value)` updates every `total_received` row for that user+source. The seed setting key in `ExternalIntegration::settings` (`donations_seed_value`) stays as-is - it's an opaque label and renaming would force a JSON migration of existing rows for zero behavior change.
- Tests in `tests/Feature/ExternalControlTest.php` updated: three tests renamed from "donations_received" to "total_received" with the control type matched to the production `number` type. Added a new test `disabling test mode resets donations_received to 0 even when seed is set` that locks in the bug fix - if anyone re-introduces seeding for `donations_received`, this fails immediately. Full suite green: 510 passed, 1402 assertions.
- One existing user has a `c.kofi.total_received + c.streamelements.donations_received` Expression Control written as a workaround for this bug (the `total_received` half was always 0 because nothing wrote to it, so the expression was effectively just the count). After this fix it can become `c.kofi.total_received + c.streamelements.total_received` and behave as a real running cross-service total.

## May 7th, 2026 - Stream Sessions page redesign: hero tiles, grouped detail blocks, and the stream's title pulled into the header

- Reworked `resources/js/pages/dashboard/stream-sessions.vue` from a flat list of muted-text rows into a card per stream with real visual hierarchy. Five concrete moves:
  1. **Header carries the stream's identity.** Latest title and category are now pulled from `title_history` and rendered inline next to the date and duration, so you see "Sat May 2 - 9h 41m - *New KSP project time? Going Interstellar!* in Kerbal Space Program" without expanding anything. The "Stream title" sidebar block (which duplicated the latest title in expanded view) is gone.
  2. **Hero tiles are real heroes.** Each of the six metric tiles (Follows, New subs, Resubs, Gift subs, Raids in, Redemptions) gets a 2px colored top border and matching icon color in its own palette: violet for follows, amber for new subs, cyan for resubs, rose for gift subs, red for raids, emerald for redemptions. Numbers jumped from `text-lg` to `text-3xl font-bold`. Secondary lines (tier breakdown, gifter count, viewer total, points spent) now use `text-foreground/80` instead of `text-muted-foreground` to match the body-copy preference.
  3. **Expanded details are grouped into three labeled blocks** with colored top-borders that double as section markers: red `Audience` (title changes + incoming raids), amber `Monetization` (cheers + resub messages + redemptions-by-reward table), cyan `Engagement` (goals + polls + hype trains). Each block hides itself if it has no content, so a low-activity stream doesn't render empty rectangles.
  4. **Resub messages compacted to one row each.** Name, tier badge, months/streak, and the message itself are all on a single flex row (with sm: breakpoint into a two-row stack on narrow viewports), down from the previous three-line block per message. Truncates the message tail rather than wrapping when it's long.
  5. **"All event counts" debug grid demoted.** No longer renders as its own page section. Folded into a single `<details>` element at the bottom of the expanded view alongside "Capture window" - one disclosure for "Debug · capture window and event counts." The headline tiles already cover what a streamer wants to know; this surface stays available for diagnostics without dominating the page.
- Pluralization fixes worked in along the way: "from 1 gifters" -> "from 1 gifter", "1 viewers" -> "1 viewer", "1 cumulative months" -> "1 cumulative month". Tier breakdown now hides zero tiers ("T1 4" instead of "T1 4 · T2 0 · T3 0").
- All `text-muted-foreground` usages in this file replaced with `text-foreground` or `text-foreground/70`-`/80` per the body-copy preference. Toggle button wears `cursor-pointer` per the clickable-cursor rule.
- No backend changes - the controller's response shape is unchanged. This is pure Vue/Tailwind. The same data renders, just in a way that doesn't read like a JSON dump.

## May 7th, 2026 - Stream Sessions headline tiles no longer multiply by the number of distinct event types

- Fixed `StreamSessionController::loadHeadlineAggregates`: the headline aggregate query was joining the per-event row stream against a `LATERAL` subquery that returned one row per distinct `event_type` in the session window. The cartesian product multiplied every `COUNT(*) FILTER (...)` and `SUM(...) FILTER (...)` by the number of distinct event types - so a stream with 6 active event types reported 6 raids instead of 1, 6 redemptions instead of 1, 18 follows instead of 3, and so on. The "Show details" tables (which read from separate queries) were correct, which is what made the discrepancy visible.
- Fix splits the query: `event_counts` is now computed in its own `ec` CTE, joined 1:1 to the outer aggregation. The per-event row stream and the per-event-type aggregate no longer multiply each other. `event_counts` is added to `GROUP BY` since it's no longer aggregated in the outer SELECT.

## May 7th, 2026 - Live map WebSocket handler matches un-prefixed GPS keys

- Fixed `resources/js/map/composables/useMapWebSocket.ts`: the live map composable was still switching on `gps_lat`/`gps_lng`/`gps_speed`/`gps_bearing`/`gps_tracking` even though commit 678681c had stripped the `gps_` prefix from all GPS control keys. Every `map.position` event arrived (so `connected` flipped true) but every `case` fell through, leaving `position` null and the marker frozen. After hard refresh the initial `/api/map/{slug}/position` fetch painted the marker once but no live updates ever arrived. Cases now match the un-prefixed `lat`/`lng`/`speed`/`bearing`/`tracking` keys the backend dispatches. Stale `gps_tracking` references in nearby comments updated to match.

## May 7th, 2026 - Multi-zone safe zones for the Overlabels GPS integration

- The mobile app now manages multiple safe zones, so the `settings_sync` wire format moved from the single-zone `safe_zone_lat`/`safe_zone_lng`/`safe_zone_radius` form fields to one `safe_zones` form field whose value is a JSON-encoded array of `{id, lat, lng, radius}` objects. Empty array `[]` clears all zones, field absent means "no change to zones." Single-level JSON, single decode - the form field carries the JSON string verbatim.
- `ExternalWebhookController::handleSettingsSync` decodes the JSON, validates each zone (id non-empty string up to 64 chars, lat in `[-90, 90]`, lng in `[-180, 180]`, radius numeric and clamped to `[1, 50000]` meters), and stores the cleaned list under `settings.safe_zones`. Bad rows are dropped silently; good rows are kept. List is capped at 10 - extras are truncated with a log line. Malformed JSON or wrong shape: log warning and no-op rather than 500.
- Legacy `safe_zone_lat`/`safe_zone_lng`/`safe_zone_radius` keys are stripped from `settings` on every `settings_sync` regardless of the new payload, so existing rows clean themselves up the next time the app syncs. The legacy single-zone parsing branch is gone.
- `GpsIntegrationController::show` now hydrates the settings page with `safe_zones` (always an array, defaults to `[]`) instead of the old single `safe_zone` object. The HTTP `clearSafeZone` endpoint and its `/settings/integrations/overlabels-mobile/clear-safe-zone` route are removed - safe-zone management is app-side only, and a "panic clear" button on the website would be the wrong direction for a feature whose entire purpose is location privacy.
- Settings page (`gps.vue`) renders the configured zones as a read-only list with lat/lng/radius per row. The "Clear safe zone" button, the `clearSafeZone()` handler, and the unused `router` import are gone. Empty-state copy points the user back to the app.

- Added `/help/for-designers` (`resources/js/pages/help/ForDesigners.vue`), a tactical pre-flight checklist for designers working on Twitch overlays in Overlabels. Pinned to the help index next to "For Creators" with a `Palette` icon - the two pages now form a pair at the top: one explains what the tool fundamentally is, the other explains what to actually deliver. Nine sections: the two surfaces (static vs alert), the unknown-background problem, variable-length content with a worst-case content table, fluid layout vs pixel-perfect, animation-lives-in-CSS (with Lottie support flagged), what to deliver (seven concrete deliverable cards: Figma file with multiple states per surface, color tokens, typography spec with web-license check, asset exports as SVG/PNG/WebP, animation references with timing notes, the live-data field list), what NOT to deliver, working with the implementer, and deep-dives cross-links.
- Tone is balanced between gaming and IRL streaming throughout. Background section names actual scenes (sunlit market, overblown horizon on a cycling shot, kitchen counter, dungeon) rather than defaulting to "gameplay". Worst-case content table includes "Travel & Outdoors" alongside game titles, and the four-mock-background test demands sunlit-overblown IRL plus night-time IRL plus dark game plus pure white starting-soon - because IRL streamers walk through every lighting condition in a single broadcast.
- Closing "Bottom line" callout is the page's load-bearing summary: a violet-bordered box right before the deep-dives section that translates the whole spec into one practical paragraph. Names the actual count ("59 presets across 8 integrations") with a link to `/help/integration-presets`, splits static (persistent state that mutates in real time, has to handle short/long and small/huge) from alerts (one-shot animations with the same flex requirement), uses concrete worst-cases (`XWXWXWXWXWXWXWXWXWXWXW` as the next subscriber, $0.50 vs $5,000 as the next Ko-fi donation), and closes with "Design once, survive everything the audience throws at it." The early drafts framed this abstractly ("understand the system you're actually building") - the final version is pure practical: here's the count, here are the worst cases, design for those.
- "The constraint is the feature" link from /help/for-creators#constraint is referenced from the Animation section, so a designer landing on this page can immediately follow the rationale for why CSS-only is the constraint they're designing against. Lottie note pulled forward from project memory: the lottie.host upload UI was absorbed into lottiefiles.com, so new uploads go through there or tiiny.host. Existing lottie.host URLs still work.

## May 6th, 2026 - Docs: "Overlabels for Creators" - long-form description of what the tool actually is, doubling as a recruiting page for creative-coder collaborators

- Added `/help/for-creators` (`resources/js/pages/help/ForCreators.vue`), an 8-section page that reframes Overlabels from "we make Twitch overlays with HTML and CSS" to "a reactive value graph that happens to render to a browser source". Sections: the thesis, the four primitives (Controls, Sources, expression engine, output), what this means for creators with six concrete recipe ideas (donation chest, subathon tint, GPS cyclist bend, vote-bar wobble, multi-service latest-donor, Lissajous on now()), a comparison table vs Streamlabs/StreamElements/OBS-plugins/plain-browser-source, "The constraint is the feature" (the no-JS section), honest gaps, an open call for paid collaborators, and a deep-dives index.
- "The constraint is the feature" is the load-bearing rhetorical move. Frames the no-JavaScript / no-script / no-iframe / no-on*-handlers / no-javascript:-URLs / tags-parse-once / URL-fragment-token-no-phone-home invariants as a deliberate security and shareability decision rather than a missing feature. Walks through the threat model if templates could run JS (hidden fetch loops exfiling tokens, phishing iframes, telemetry listeners) and lands on the recruiting hook: "your recipe is safe to ship - the streamer who copies it doesn't need a security review". The whole copy-someone-else's-clever-overlay loop only works because there's no escape hatch.
- Pinned to the top of the help index card grid (above the existing Reference card) with a `Lightbulb` icon. Route registered at `/help/for-creators`. Cross-links every other help page from the deep-dives section so this becomes the natural entry point for anyone hearing about Overlabels for the first time.
- Caveat caught and corrected mid-draft: an earlier pass said "you reference a Control in your overlay HTML, CSS, or JS" and "an HTML/CSS/JS template", neither of which is true (templates are sanitized server-side, JS is stripped). Fixed both, and that catch is what motivated promoting the no-JS rule from a footnote in the Output primitive to its own dedicated thesis section.
- Editorial: "Looking for collaborators" section spells out the actual gig (paid recipe co-author book, Shadertoy-style entries each tying a value source + expression + CSS rule to a screenshot/gif), the bar ("would another creative coder find this clever", not "useful to the median streamer"), and the email (`jasper@emailjasper.com`) with what to include (portfolio, starting effect, rate). "Honest gaps" section lists what's not yet possible (audio analysis, MIDI, direct EventSub-in-expressions, persistent expression state, multi-overlay sync) so a creative coder evaluating the surface can see the walls before committing.

## May 6th, 2026 - Docs: dedicated Expression Controls help page, with a full Haversine walkthrough, plus a slimmed ExpressionBuilder that points to it

- Added `/help/expressions` (`resources/js/pages/help/Expressions.vue`) - a complete reference for the expression engine, written like a tutorial rather than an API dump. Sections: what an Expression Control is, syntax basics, how to reference your own / service / Twitch controls, the full function catalog (Trig, Rounding & utility, Multi-argument math, Label selectors, Constants) rendered as proper tables instead of accordion soup, a worked example that builds the **Haversine great-circle distance formula** entirely in chained Expression Controls (`dLat` -> `dLng` -> `haversine_a` -> `distance_to_dest` -> `progress_pct`), then "things to know" callouts for live re-evaluation, GPS ping cadence, radian conversion, the two-arg `atan2` gotcha, service-preset references, and the EventSub-not-yet-direct caveat. Closes with a quick reference card you can mentally screenshot. Cross-links the Math Engine page and the Integration Presets reference.
- The Haversine walkthrough is the headline because it's a genuine "no server, no JS in the overlay" achievement: a streamer cycling toward a destination gets a live km-remaining readout and a character that slides across the overlay proportional to journey progress, all from five chained Expression Controls feeding off `c.gps.lat` / `c.gps.lng`. Every GPS ping recomputes the chain. The page walks through each step with the actual control key, the actual formula, and the actual overlay HTML/CSS that consumes the result.
- Wired into the help index (`resources/js/pages/help/Index.vue`) with a `FunctionSquare` icon card alongside the existing Controls / Math Engine cards. Route registered at `/help/expressions` (`routes/web.php`) using the same Inertia::render pattern as the rest of the help section.
- Slimmed `ExpressionBuilder.vue` from ~530 lines to ~420 by deleting the in-place help dialog (the three-tab Accordion covering Basics / Functions / Examples) and offloading every word of documentation to the new help page. Replaced the `Help` button with two compact controls in the Expression card header: a **Functions** button that opens a small `max-w-md` dialog with click-to-copy function chips grouped by category (Label selectors, Multi-argument, Rounding and utility, Trig, GLSL helpers, Time, Constants), and a **Full reference ->** link that opens `/help/expressions` in a new tab. The functions dialog stays open across copies so users can grab a few in sequence; each chip flashes "Copied!" for 1.5s on click.
- Editorial: every em dash from the source markdown was rewritten to ` - ` (project rule, no em dashes in user-facing copy), the en dash in "2-60 seconds" was hyphenated, and the `→` arrow in the round-returns-string callout became `->` in plain text. Function names and code snippets are otherwise verbatim.
- Side cleanup: dropped `Dialog`/`DialogContent`/`Accordion`/`AccordionItem`/`HelpCircle` imports from ExpressionBuilder, removed `helpOpen` / `HELP_TAB_KEY` / `helpTab` localStorage state, removed `exampleCopied` / `copyExampleCode`. The component now does one thing - build expressions - and points anywhere documentation needs to live elsewhere.

## May 6th, 2026 - Feat: searchable reference page for every integration preset, plus three small UX fixes around the Add Control modal

- The "Stream Controls" Combobox in `ControlFormModal.vue` keeps growing with each new integration - around 50 presets across Twitch, Ko-fi, Streamlabs, StreamElements, Fourthwall, BMAC, Overlabels GPS, and GPSLogger. Useful as a power-user search once you know the key, useless for discovery. Added a dedicated reference page at `/help/integration-presets` that renders every preset grouped per service: lucide icon, short description of what the integration emits, and a table of `Label / type / [[[c:source:key]]]` rows. Top-of-page fuzzy search reuses the existing `fuzzyMatch` + `presetHaystack` so typing "donation", "kofi", or "speed" filters every section live; sections with zero matches collapse out. Links from the help index (new `Plug` icon card) and from inside the modal (small "Browse all presets ->" link in the Stream Controls header that opens in a new tab so the in-progress form isn't lost).
- Each preset's tag is rendered as an `<a href="#" @click.prevent>` wrapping a `<code>` element rather than a `<button>`. Buttons' text is excluded from Ctrl+A in Firefox, so anyone wanting to grab the whole page (e.g. paste into an LLM or share with a friend) was getting empty selections where every tag should have been. Anchor + code is keyboard-focusable, click-to-copy still works (1.5s "Copied!" feedback), and Ctrl+A now grabs every tag along with its label.
- Fix: the QR code on `/settings/integrations/overlabels-mobile` (the GPS settings page) no longer requires a manual page refresh after clicking Connect. The QR generation lived in `onMounted`, which fires once at component mount; after the connect POST returned with a fresh `deep_link` in props, Inertia merged the new prop in but the mount hook never re-ran, so `qrDataUrl` stayed `null` until the user reloaded. Replaced with a `watch(() => props.integration.deep_link, ...)` with `immediate: true`, which covers both the first mount and the regenerate-token flow (the deep link changes there too).
- Fix: the Add Control modal now lands focus on the "Give your control a name" label input instead of the Stream Controls Combobox. Reka's DialogContent forwards an `openAutoFocus` event - intercepting it with `event.preventDefault()` and a `nextTick(() => labelInputRef.focus())` redirects focus to the input that 90% of users actually want to type into first. Power users who hit the dropdown by tab still get there in one keypress.

## May 5th, 2026 - Feat: per-session OG image with the actual route silhouette

- Public session-map URLs (`/map/{slug}/{sessionId}`) now generate a custom 1200x630 OG/Twitter card showing the route's actual polyline next to streamer name + date and four big stat tiles (Distance, Duration, Max Speed, Pings). Pasting a session link into Discord, Twitter, etc. now produces a preview that immediately tells the viewer what the link is - shape of the ride, how long, how fast, how many pings - without opening it. Verified against TicanUK's 4007-ping prod session: 217 simplified points, dashed grid panel + glowing purple route, mph-formatted stats. Matches the dashboard card vocabulary so the share preview feels like the same product.
- Reused the existing `OgImageService` pipeline (resvg renders SVG -> PNG, sha256-keyed disk cache under `public/og/`). Added `urlForGpsSession()` and a new SVG Blade `og.gps-session`. Bumped `TEMPLATE_VERSION` to 3 so previously cached OGs invalidate on next render. resvg's native `<polyline>` support means no headless browser, no map-tile fetch, no Cloudinary roundtrip - just SQL -> simplified coords -> SVG -> PNG, all in-process.
- Added `GpsSessionAggregator::coordinatesFor()` returning ordered + simplified `[lng, lat]` pairs (same RouteSimplifier path the public geojson endpoint uses, so the OG silhouette is geometrically identical to what the user sees on the page). The OG renderer projects them with an equirectangular projection scaled by `cos(midLat)` so short routes look correct at any latitude, with `meet`-style aspect-preserving fit into the panel.
- `MapController::session()` calls the aggregator + OG service synchronously and threads `ogImagePath`/`ogDescription` to the blade. Rebuilt `resources/views/map/session.blade.php` to emit full `og:`/`twitter:` meta - title, description, image (1200x630, image/png), site_name, canonical url. OG generation is best-effort: if the session has no aggregate or resvg fails, the page falls back to the default `/ogimage.png` and the human-facing map keeps rendering normally.
- The `og:description` is composed at controller-level from real session data ("8.5 mi - 3h 35m - 4,007 GPS pings. Live route shared via Overlabels.") so the preview text isn't generic. Speed-unit aware (km/mi suffix follows the integration setting).
- `OgImageService::renderOrCached()` now accepts a template name (defaults to the existing `og.help-reference`) so the help/reference paths are unchanged. The hash key incorporates the template name so the two surfaces can't collide on disk.

## May 4th, 2026 - Feat: stats card on the public session-map view

- The public saved-session URL `/map/{slug}/{sessionId}` rendered the route polyline beautifully but with zero context - a viewer who landed on a shared link had no way to see how long the ride was, how far, or how fast. The same data was already living in the GPS Sessions dashboard card (duration, distance, avg/max speed, elevation min-max, battery start-end, ping count); the public view just wasn't asking for it.
- Extracted `GpsSessionController::aggregateSessions` and `computeDistances` into a new `App\Services\GpsSessionAggregator`. The dashboard now calls `forUser($userId)`; the new public endpoint calls `forSession($userId, $sessionId)`. Same shape, same SQL, one source of truth so the two surfaces can't drift.
- Added `GET /api/map/{slug}/{sessionId}/meta` (route in `routes/api.php`, `throttle:60,1`, no Sanctum). Gated on `map_sharing_enabled` like the existing geojson endpoint. Returns `{ session, speed_unit, locale }` so the unauthenticated frontend can render the streamer's locale rather than defaulting to `en-US`.
- `useSessionDataFormatter` gained a `localeOverride?: Ref<string | null | undefined>` option that takes precedence over `usePage().auth.user.locale`. The dashboard keeps using the authed user's locale as before; `SessionMap.vue` passes the streamer's locale through the new override. No call sites broken.
- `SessionMap.vue` now fires the geojson and meta fetches in parallel and renders an absolute-positioned card in the top-left corner over the leaflet stage. Card uses the same icon set, layout, and formatters as the dashboard row (Clock/Duration, MapPin/Distance, Gauge/Speed, Mountain/Elevation, Battery, Radio/Pings) so the two views feel like the same component. `z-index: 500` puts it above leaflet's default panes (~400). Backdrop-blur + dark glass to keep the route visible underneath. Mobile breakpoint at 640px keeps it readable on phones.
- Meta fetch is treated as decorative: if it fails or returns 404, the map still renders. The card just isn't drawn.

## May 4th, 2026 - Fix: live map `/map/{slug}` now seeds the trail with the in-progress session's history

- Symptom: a friend was streaming with the Overlabels GPS app and logged 4002 pings cleanly into the backend, but anyone opening his public live map URL mid-stream saw a blank map that started following him only from the moment they joined - none of the route already travelled was visible. Visiting the matching saved-session URL afterward showed the full route, so logging and storage were fine; the live page just never asked for the history.
- Root cause: `LiveMap.vue`'s `onMounted` only called `/api/map/{slug}/position`, which returns a single ping (the most recent location). It seeded `trail.value` with that one point and from there the polyline only grew from WebSocket arrivals. The full-session geojson endpoint that `SessionMap.vue` uses (`/api/map/{slug}/{sessionId}/geojson`, served by `GpsSessionMapController::buildGeoJson` with `RouteSimplifier` for sessions over 100 points) was never called because the live page didn't know the active `session_id`.
- Fix: `currentPosition` now also returns `session_id` from the latest ping's payload. `LiveMap.vue`'s `onMounted` then makes a second fetch to the existing public geojson endpoint and seeds `trail.value` with the LineString coordinates (server-side simplification keeps a 4002-point session well under any frontend cap). Added `seededTrailLength` so the live-trail growth cap (`MAX_LIVE_TRAIL_GROWTH = 200`) applies on top of the seeded history rather than trimming it; `goOffline()` resets it so the next session re-seeds cleanly. Falls back to the previous single-point seed if the geojson fetch fails or returns no LineString.

## May 4th, 2026 - Refactor: rename `overlabels-mobile` service slug to `gps`, drop `gps_` prefix from control keys

- The Overlabels mobile integration's internal slug was `overlabels-mobile`. Three pain points piled up: (1) hyphens aren't valid JS identifiers, so jsep parsed `c.overlabels-mobile.gps_lat` as the subtraction `c.overlabels - mobile.gps_lat`, returning `0` in the live preview and "Referenced control 'overlabels' not found in scope" on save; (2) the same hyphen forced bracket notation `c["overlabels-mobile"].gps_lat` everywhere; (3) keys stuttered: `c.overlabels-mobile.gps_speed`, `[[[c:overlabels-mobile:gps_speed|speed:kmh]]]`. Marketing slug invading developer experience.
- Renamed the internal slug to `gps`. The webhook URL `/api/webhooks/overlabels-mobile/{token}` and the settings page URL `/settings/integrations/overlabels-mobile/*` are NOT touched - decoupled at the controller boundary via a `URL_SLUG_ALIASES` map in `ExternalWebhookController` and a `url_slug` field served on the integrations index. The mobile app on the streamer's phone keeps working unchanged.
- Stripped the redundant `gps_` prefix from all 13 provisioned control keys: `gps_speed` -> `speed`, `gps_lat` -> `lat`, `gps_lng` -> `lng`, `gps_distance` -> `distance`, `gps_bearing` -> `bearing`, `gps_accuracy` -> `accuracy`, `gps_battery` -> `battery`, `gps_charging` -> `charging`, `gps_tracking` -> `tracking`, plus the four `gps_session_*` keys to their unprefixed `session_*` forms. Template syntax now reads `[[[c:gps:speed|speed:kmh]]]` instead of `[[[c:gps:gps_speed|speed:kmh]]]`. Expressions read `c.gps.session_max_speed` instead of `c["overlabels-mobile"].gps_session_max_speed`.
- Renamed file/class: `OverlabelsMobileServiceDriver` -> `GpsServiceDriver`, `OverlabelsMobileIntegrationController` -> `GpsIntegrationController`, `OverlabelsMobileWebhookTest` -> `GpsWebhookTest`, Vue page `integrations/overlabels-mobile.vue` -> `integrations/gps.vue` (URL stays). Frontend preset constants: `OVERLABELS_MOBILE_PRESETS` -> `GPS_PRESETS`, and the existing `GPS_PRESETS` (which actually backed gpslogger) -> `GPSLOGGER_PRESETS` to free up the cleaner name. `RESERVED_KEYS` updated.
- Data migration `2026_05_04_000000_rename_overlabels_mobile_to_gps`: moves `external_integrations.service`, `external_events.service`, and `overlay_controls.source` from `'overlabels-mobile'` to `'gps'`; renames `overlay_controls.key` per the prefix-strip map; rewrites every expression-control's `config.expression` (collapsing bracket notation `c["overlabels-mobile"].gps_X` to `c.gps.newkey`) and `config.dependencies` array; rewrites every `[[[c:overlabels-mobile:gps_X]]]` reference in `overlay_templates.html/css/js/head` (preserving any `|formatter` pipe). Includes a safety check that aborts if any user-defined control has key=`gps` (would collide with the new namespace) and a full reverse migration. Local dev: zero rows touched (no overlabels-mobile data); prod will need `php artisan migrate --force` after deploy.

## May 4th, 2026 - Fix: GPS speed control now stores raw m/s (templates format with the speed pipe)

- Symptom: setting the integration `speed_unit` to mph and using `[[[c:overlabels-mobile:gps_speed|speed:mph]]]` produced wildly wrong numbers, while a bare `[[[c:overlabels-mobile:gps_speed]]]` looked fine. Three layers were each trying to own the unit conversion: the driver pre-converted m/s -> km/h on ingest, `beforeControlUpdates()` re-converted km/h -> mph if the integration setting demanded it, and the pipe formatter then ran a third conversion against an already-converted stored value. The setting and the pipe were fighting each other.
- The `gps_session_max_speed` and `gps_session_avg_speed` controls already followed the right pattern - store raw m/s, format at template-time. `gps_speed` was the legacy holdout, predating the pipe formatter system. Brought it in line: both `OverlabelsMobileServiceDriver` and `GpsLoggerServiceDriver` now store the inbound `speed` value as raw m/s (rounded to 4 decimals) with no conversion. Stripped the `* 3.6` from `getControlUpdates()` and the `kmh / 1.609344` mph re-conversion from `beforeControlUpdates()` on both drivers.
- `external_integrations.settings.speed_unit` is now a display hint for the GPS Sessions dashboard only - it does not influence any stored control value. Updated the integration settings copy on both `gpslogger.vue` and `overlabels-mobile.vue` to spell out that overlay templates pick their unit per tag with `|speed:kmh` or `|speed:mph`. Auto-provisioned label updated to `GPS Speed (m/s)` to match the session-aggregate label convention.
- Breaking change for existing templates: a bare `[[[c:overlabels-mobile:gps_speed]]]` (or `gpslogger`) now renders raw m/s instead of the user's preferred unit. Bit the bullet because Overlabels' first user is the only person currently using these controls and we're on his template fixing it directly. Updated the contract doc at `docs/gpslogger-backend-contract.md` to match. Tests in `OverlabelsMobileWebhookTest` and `GpsLoggerWebhookTest` updated; the old "converts speed to mph when speed_unit is mph" tests now assert `speed_unit` does NOT affect stored value. Side fix: stale control-count assertion in the mobile connect test (was hardcoded to 8, driver auto-provisions 13) replaced with a reflective `count(...->getAutoProvisionedControls())`.

## May 4th, 2026 - Fix: visual feedback on Save changes for the Overlabels GPS settings page

- Hitting Save changes on `/settings/integrations/overlabels-mobile` had zero visual feedback - so easy to miss that the user (impersonating his friend's account during a debugging session for the speed-unit issue above) thought the speed_unit setting was being saved when it wasn't. Wired up `RekaToast` (the same pattern used in `dashboard/gps-sessions.vue`, `templates/show.vue`, etc.) with success/error toasts on the Inertia form's `onSuccess` / `onError` callbacks. The button's existing `:disabled="form.processing"` already covers the in-flight cue; the toast covers the outcome.

## May 4th, 2026 - Fix: cascade overlay-controls on template delete (was throwing 500)

- Deleting a template in prod started returning 500. Root cause: `overlay_controls.overlay_template_id` was `nullOnDelete`, so a delete tried to demote the template's controls to `template_id=NULL` instead of removing them. The partial unique index `overlay_controls_user_source_key_unique (user_id, source, key) WHERE overlay_template_id IS NULL` then collided with the user-scoped service-managed row already at `(user, source, key, template_id=NULL)` from auto-provision at integration-connect time, throwing `SQLSTATE 23505`. The whole transaction rolled back so the user saw a delete that "didn't work" with no client-visible reason - error log only had `"message": "Server Error"`.
- Pre-existing latent bug, not caused by today's fork-wizard fix - but the fork fix would have made it more frequent, since forking a template that uses StreamElements presets now actually creates the per-template service-managed rows that hit this collision. Diagnosed by grepping the prod web container's docker logs for the 500: stack trace pointed straight at `OverlayTemplateController::destroy` line 259 hitting the unique index inside the FK demotion.
- Fix: switch the FK to `cascadeOnDelete`. When you delete a template, all its controls (plain or per-template service-managed) go with it. User-scoped rows (`template_id=NULL` from auto-provision) are unaffected because the FK only cascades on rows whose `overlay_template_id` matches the deleted row. Migration also sweeps any pre-existing orphan rows (`template_id=NULL AND source=NULL`) since they're invisible to every UI - `$template->controls()` only loads by `template_id`, ControlsManager and the service group views only see what comes through that. There's no meaningful surface where a template-less, service-less row was ever readable, so they were just dead data from prior `nullOnDelete` runs.
- Prod state before migration: 136 orphan rows, all belonging to a single user, all `source_managed=false`, types ranging across text/number/expression/etc. - all residue from past template deletions that succeeded before the user added any service-managed presets to those templates. Prod state after: 0 orphans, FK confirmed CASCADE via `pg_constraint.confdeltype = 'c'`. Migration ran via the prod web container with `php artisan migrate --force` after scp'ing the file in; the file is committed to git so the next Kamal deploy is a no-op for this migration.

## May 4th, 2026 - Fix: route non-AJAX fork entry points through the import wizard

- Companion fix to today's earlier wizard-source bug. The wizard only opened on the one fork path that uses `axios.post` (the dropdown on a template's show/edit page). The other four entry points - the Copy button on the public overlay preview at `/overlay/{slug}/public`, plus the Copy/Fork dropdown items in `TemplateCard.vue`, `TemplateList.vue`, and `TemplateTable.vue` - all submitted as either a plain HTML form or `router.post`, neither of which sets `Accept: application/json`. The controller's `wantsJson()` check went false, the redirect-to-templates-index branch fired, and the user landed on a forked template with zero controls and no chance to import them. The wizard's whole job was being skipped on most of the entry points it was built for.
- Could have rewritten each consumer to use `axios.post` and emit upward, but that's four call sites plus a blade template with no Vue context, and the wizard component lives on the show page anyway. Cleaner to make the controller redirect to `templates.show` (instead of `templates.index`) on non-JSON requests and flash the wizard payload onto the session. The show page picks it up through Inertia's shared `flash` prop and opens the wizard from `onMounted`. One server-side change, one new flash key, one onMounted hook - the four consumers don't need to know anything changed.
- `OverlayTemplateController::fork()` now builds the wizard payload (`source_controls`, `has_controls`, `required_services`, `connected_services`, `template`) once and either returns it as JSON or attaches it via `->with('fork_wizard', $payload)` on the redirect. `HandleInertiaRequests::share()` exposes `flash.fork_wizard`. `useTemplateActions` gets a new `openWizardFromPayload(data)` helper - the existing AJAX path now calls it too, so both code paths converge on the same wizard-state setter. The show page reads the flash on mount and calls the helper if present.
- Net result: clicking Copy on a public overlay preview, on a template card, in a list row, or in a table dropdown now all land you on the new template's show page with the import wizard already open. The dropdown path on a template you already own still uses the JSON path, so its UX is unchanged. Pre-existing test failures (`OverlabelsMobileWebhookTest`, `ApplyActionTest`, `ExpressionControlTest`) confirmed unrelated by stashing this commit and re-running.

- Copying a public overlay through the fork wizard was silently downgrading every service-managed control (Ko-fi, StreamLabs, StreamElements, Fourthwall, BMAC presets) into a plain template control. The wizard correctly told the user "this template uses StreamElements - you already have it connected", but on Import Selected the new copy ended up with `donations_received`, `latest_donor_name`, etc. as orphan controls with no source, no "Managed by StreamElements" badge, and no membership in the StreamElements group in ControlsManager. The user could then add a fresh `donations_received` preset on top of the orphan because the dedup checks `(template_id, key, source)` and the imported row's source was null.
- Three places in the round-trip dropped `source` and `source_managed`. `OverlayTemplate::fork()` selected only six columns into `_sourceControls`, so the wizard payload never carried them. `ForkImportWizard.vue` mapped only those same fields into wizard rows and back out to `/controls/import`. `OverlayControlController::importForkedControls()` had no validation rules for the two columns and `OverlayControl::createForTemplate()` didn't accept them either, so even if the frontend had sent them they would have been silently discarded.
- Threaded both columns end-to-end. `fork()` now selects `description`, `source`, and `source_managed` alongside the original six. The wizard's `SourceControl` and `WizardRow` interfaces gained the two fields and the watch that initialises `rows` carries them through verbatim. The submit map sends them back. Validation rules added on the controller (`nullable|string|max:50`, `nullable|boolean`). `createForTemplate()` derives `source_managed` from whether `source` is set, defaulting to `true` when present so a malformed payload cannot create a `source != null, source_managed = false` row.
- UX touch-up on the wizard table: added a Source column showing the service badge for service-managed presets, and the Key input is locked when `row.source` is set (preset keys are part of the service contract; renaming `donations_received` to `donations_received_2` would orphan it from the broadcast pipeline). Description now also rides through, since `createForTemplate()` already supported it but the wizard was throwing it away.
- Net result: forking a template that uses StreamElements now produces a copy where the StreamElements controls keep their group, their "Managed by" badge, and their broadcast key (`c:streamelements:donations_received`). The duplicate-add path is closed because dedup matches on `(template_id, key, source)` exactly as it did before. Bug 2 - the public-render Copy button bypassing the wizard entirely - is a separate fix, coming next.

## May 4th, 2026 - Feature: extend expression engine with sqrt and full inverse-trig surface

- Triggered by a friend cycling toward TwitchCon: his overlay needs a character to move across screen proportional to the great-circle distance from his live GPS to the destination. Haversine is the standard formula, and every part of it was already expressible as a chain of Controls (arithmetic, `sin`, `cos`, `PI`) except the final `R * 2 * atan2(sqrt(a), sqrt(1-a))` step. `sqrt` and `atan2` were the only two missing pieces.
- Added `sqrt`, `tan`, `asin`, `acos`, `atan`, and `atan2` to the jsep evaluator's `FUNCTIONS` map and `SUPPORTED_FUNCTIONS` set in `useExpressionEngine.ts`. The proposal originally framed this as adding `Math.sqrt`/`Math.atan2`, but Overlabels expressions use bare names (`sin(x)`, not `Math.sin(x)`) - and `abs`/`round`/`floor`/`ceil` from the proposal's "confirm" column were already there. Real gap was just the trig + sqrt surface.
- `sqrt(x)` returns 0 for negative input rather than NaN, matching the engine's existing "swallow div-by-zero, return 0" convention so a malformed control upstream can't propagate NaN through every dependent expression. `atan2(y, x)` is a two-arg call - the resolver already handled multi-arg functions (`max`, `clamp`, `mod`), so no resolver changes needed.
- Help docs updated: ExpressionBuilder modal pill row now lists the full trig set with a one-line note on what each does, the Math.vue cheatsheet table gains rows for `tan`/`asin`/`acos`/`atan`/`atan2`/`sqrt`, and the "No exponentiation operator" amber callout no longer claims `sqrt` and `tan` are unavailable - just `log` and `exp` (which remain out of scope).
- Backend stays untouched: expressions only evaluate in the frontend (`useExpressionEngine.ts`), backend just stores the string and tracks dependencies for the cascade. Verified existing Pest tests in `ExpressionControlTest.php` still pass; one pre-existing failure on main ("expression with no references should 422") was confirmed not introduced by this change.
- Net result: the full haversine formula is expressible as a portable Controls chain - destination coordinates stay in Overlabels (a streamer-configured value, not something the GPS app should own), no per-overlay JS workarounds, works for any future GPS overlay without code changes.

## May 3rd, 2026 - Security: hide Twitch ID behind a Sqids-encoded slug on the public map page

- Public map URLs were `/map/{twitch_id}`, exposing the streamer's numeric Twitch ID to anyone with the share link. The same ID also leaked through `/api/map/{twitch_id}/position`, the `.../geojson` endpoint, and the `map.{twitch_id}` Reverb channel name visible in DevTools. With a Twitch ID a chatter could URL-hack to any other registered streamer's `/map/...` URL to probe whether they were on the platform.
- Added `App\Services\MapSlugService` wrapping `sqids/sqids` (`composer require sqids/sqids` -> 0.5.0). Encodes a Twitch ID into an opaque short slug (default min length 8) using a custom alphabet pinned in `config/services.php` under `map_slug.alphabet`. Pure CPU, reversible, deterministic - zero DB or cache hits per request, which matters because a popular streamer's map page can have hundreds of viewers polling `/api/map/{slug}/position` every 5s in delay mode. Alphabet is overridable via `MAP_SLUG_ALPHABET` env var; if it ever changes, every previously shared map URL stops resolving (committed default keeps prod stable).
- All public surfaces switched to the slug:
    - Routes: `GET /map/{slug}` and `GET /map/{slug}/{sessionId}` in `web.php`; `GET /api/map/{slug}/position` and `GET /api/map/{slug}/{sessionId}/geojson` in `api.php`.
    - Controllers: `MapController::live`/`session` and `GpsSessionMapController::currentPosition`/`publicSessionGeoJson` decode the slug via `MapSlugService::decode()` and 404 on a malformed/non-resolving slug before any DB query, then look up the user by `twitch_id` exactly as before.
    - Broadcast: `MapPositionBroadcast` now carries `$slug` instead of `$twitchId` and broadcasts on `map.{slug}`. `ExternalControlService::applyUpdates` encodes the slug once per call (only when map sharing is enabled) and reuses it across the per-control loop.
    - Frontend: `window.__MAP__` ships `slug` instead of `twitchId`. `LiveMap.vue`, `SessionMap.vue`, `useMapWebSocket.ts`, and `map/app.ts` rename the prop. `OverlabelsMobileIntegrationController` builds the share URL from the encoded slug. The GPS Sessions dashboard exposes `mapSlug` instead of `twitchId` and the "Open full view" button links to `/map/${mapSlug}/${session.session_id}`.
- Net result: a chatter holding a `/map/...` URL sees only `/map/Yz7X9k2m`, the WebSocket frames carry `map.Yz7X9k2m`, and the API endpoints follow the same shape. Twitch ID never appears in any client-visible string. Slug is not security-grade (sqids are reversible), but it removes the obvious "12345 is a Twitch ID" pattern and prevents URL-hacking to other streamers by guessing.

## May 3rd, 2026 - Admin: extend donation seed override to all donation-style integrations

- The admin user-detail page (`/admin/users/{id}`) had a Ko-fi-only "Received Count" card so admins could correct a user's `donations_received` seed after the one-time user-facing lock fired. With four more donation services live (StreamLabs, StreamElements, Fourthwall, BMAC) that override only worked for one of them.
- Generalized: `AdminUserController::updateKofiSeed` is now `updateIntegrationSeed($id, $service)`, gated by a `SEEDABLE_SERVICES` constant (`kofi`, `streamlabs`, `streamelements`, `fourthwall`, `bmac`). 404s for any other service. Updates the `OverlayControl` row(s) where `source = $service` and `key = donations_received` and writes `donations_seed_set` + `donations_seed_value` into the integration's `settings` so the user-facing settings page sees the new value too.
- Route `admin.users.kofi-seed` (POST `/admin/users/{user}/kofi-seed`) replaced by `admin.users.integration-seed` (POST `/admin/users/{user}/integration-seed/{service}`) with a regex constraint locking `{service}` to the five seedable services.
- `show()` now ships an `integrationSeeds` array (one entry per seedable service) instead of three Ko-fi-specific props. Each entry carries `{service, label, connected, seed_set, seed_value}`.
- Vue page renders one card with a per-service row inside it. Each row owns its own `useForm` so loading state, validation errors, and the input value are isolated. Only services where the user actually has an integration row are shown.
- Audit log key: `user.kofi_seed_updated` becomes `user.integration_seed_updated`, with `service` added to the metadata payload alongside `initial_count`.

## May 3rd, 2026 - Fix: split BMAC connect flow so webhook URL is generated before secret

- BMAC reveals the webhook secret only after you paste an Overlabels URL into BMAC's webhook page, so requiring the secret on first save was a chicken-and-egg dead end.
- Save now accepts a nullable `webhook_secret`. First submit creates the integration row with `enabled=false` and no credentials so the webhook URL appears, user goes to BMAC and gets the secret, comes back, second submit stores the secret and flips `enabled=true`.
- UI follows the three-state shape: not connected -> setup steps + "Generate webhook URL" button. Connected without secret -> URL appears, secret input shown, button reads "Save webhook secret". Connected with secret -> "Save changes" + placeholder reads "(secret saved - enter new to replace)" and the input clears on every successful submit via Inertia's onSuccess hook.

## May 3rd, 2026 - Feature: Buy Me a Coffee (BMAC) integration

- Added BMAC as the fifth donation-style integration alongside Ko-fi, StreamLabs, StreamElements, and Fourthwall. Connects via webhook with HMAC-SHA256 verification: user pastes the secret from `studio.buymeacoffee.com/webhooks/<id>` into Settings -> Integrations -> Buy Me a Coffee, Overlabels generates the webhook URL (`/api/webhooks/bmac/{token}`), user pastes the URL back into BMAC. Every event signed with `x-signature-sha256` of the raw body using the shared secret.
- Six positive event types covered: `donation.created`, `commission_order.created`, `extra_purchase.created`, `membership.started`, `recurring_donation.started`, `wishlist_payment.created`. The eight refund/cancel/update events return `{status: ignored}` 200 (per BMAC's "always 2xx or we disable the webhook" rule). All six positive events bump `donations_received` and add `data.amount` to `total_received` so a single counter tracks all BMAC support.
- `latest_donor_name`, `latest_donation_amount`, `latest_donation_message`, `latest_donation_currency`, `total_received`, plus a new `latest_support_type` (Supporter / Commission / Extra / Membership / Subscription / Wishlist) so streamers can distinguish a coffee from a commission in alert templates without a chain of `[[[if:event.type = ...]]]` blocks. Same six-key contract as Ko-fi/StreamLabs/StreamElements/Fourthwall, so swapping `c:kofi:` for `c:bmac:` (or running them through `latest()`/`oldest()`) just works.
- Privacy first: `supporter_email`, `shipping_address` (top-level and nested under `commission`), and `total_amount_charged` are stripped from the stored `raw_payload` in the driver before it reaches the controller. Email is captured into a new `private_metadata` column on `external_events` (encrypted JSON cast `encrypted:array`) plus a `supporter_email_hash` indexed sha256 column for future "unique supporters" analytics, both populated via two new optional readonly props on `NormalizedExternalEvent`. When supporters mark `note_hidden = true` (memberships and monthly support), the support_note is blanked out everywhere downstream rather than rendered as a placeholder string.
- Test mode mirrors Ko-fi: enabling appends a `_test_<microtime>` suffix to the dedup messageId so BMAC's "Send Test" button can be fired repeatedly. Disabling test mode resets all source-managed BMAC controls to defaults (with `donations_received` falling back to the seed value if set). Starting-count seeding works exactly like Ko-fi - one-time only via the BMAC settings page.
- Frontend: new `bmac.vue` settings page (clone of `kofi.vue`, retitled "Webhook secret" + 6 BMAC event-type checkboxes), `BMAC_PRESETS` exported from `controlPresets.ts`, ControlFormModal surfaces them when `connectedServices.includes('bmac')` on static templates, `SERVICE_LABELS` and the integrations index button allowlist updated. Help/Conditionals page gets a Buy Me a Coffee family with four cards (controls / common tags / payment-flavor / recurring) and its own "How It Works" panel explaining HMAC verification, the privacy posture, and shared-alert-template strategy.
- Tests: 19 unit tests in `BMACServiceDriverTest` covering all six event shapes, signature accept/reject paths, PII stripping, messageId stability, and `note_hidden` semantics across BMAC's mixed string/bool flag types. 10 feature tests in `BMACWebhookTest` covering the end-to-end webhook pipeline (HMAC, dedup, test-mode bypass, control updates, refund-event ignoring, raw-payload sanitization assertions). All 29 pass; full suite stays green except for three pre-existing failures unrelated to this change.

## May 2nd, 2026 - Security: gate WebSocket fanout behind private channels and overlay-token auth

- Pentest of the overlay URL+fragment model surfaced two leaks. Anyone holding one overlay URL+token (e.g. a screen-share, a chat link, a screenshot) could derive `data.user_twitch_id` from the render response and then subscribe to the public `alerts.{user_twitch_id}` Reverb channel. That channel carried `alert.triggered`, `control.updated`, `stream.status`, and `template.updated` events in real time - including donor names, donation amounts, and live `overlabels-mobile:gps_lat`/`gps_lng` updates for streamers using map sharing. Separately, the global `twitch-events` Pusher channel was a platform-wide firehose: any unauthenticated client connected to Reverb received every Overlabels user's Twitch events.
- Channels are now private. `AlertTriggered`, `ControlValueUpdated`, `StreamStatusChanged`, `TemplateUpdated`, and `EventSubSetupCompleted` all use `PrivateChannel` instead of `Channel`. `TwitchEventReceived` now broadcasts on a per-user `private-twitch-events.{twitchId}` channel instead of the platform-wide `twitch-events`; its constructor takes a required `broadcasterId`, and `TwitchEventSubController` skips the broadcast when the broadcaster id is unknown rather than fanning out to a guessable channel. `routes/channels.php` registers auth callbacks that allow only the channel's owning streamer (logged-in dashboard) to subscribe.
- Overlays are session-less browser sources, so the standard `/broadcasting/auth` route can't sign their subscriptions. Added `POST /api/overlay/broadcasting/auth` (`OverlayBroadcastingAuthController`) that takes `{slug, token, socket_id, channel_name}`, validates the `OverlayAccessToken` via the same `findByToken` path as `/api/overlay/render` (active flag + expiry + IP allowlist all enforced), and signs the Pusher-protocol auth response only for the two channels overlays legitimately need: `private-alerts.<owner_twitch_id>` and `private-twitch-events.<owner_twitch_id>`. Anything else (different user, gamejam, presence) returns 403 before any signature is produced. Eight Pest tests cover owner success, cross-user denial, channel-allowlist denial, invalid/short/non-hex/deactivated tokens.
- `resources/js/overlay/app.js` now configures Echo with a custom `authorizer` that POSTs to the new endpoint with the URL-fragment token. `OverlayRenderer.vue`, `useStreamState.ts`, and `useEventSub.ts` switch from `Echo.channel(...)` to `Echo.private(...)`. `useEventSub` now takes a `twitchId` arg so each subscriber lands on its broadcaster's private channel.
- The public `/map/{twitchId}` page (opt-in via `map_sharing_enabled`) was the one place subscribing to alerts on purpose - to receive live GPS. Replaced that with a dedicated `MapPositionBroadcast` event on a public `map.{twitchId}` channel that ships only `gps_lat`/`gps_lng`/`gps_speed`/`gps_bearing`/`gps_tracking`. `ExternalControlService::applyUpdates` dispatches it alongside `ControlValueUpdated` only for `overlabels-mobile` controls and only when the user has map sharing enabled, so opted-in viewers still get realtime updates without leaking donations or donor names. `useMapWebSocket.ts` listens on the new public channel.
- Gamejam channels (`gamejam.{twitchId}`) stay public - the `/gamejam/live/{login}` page is intentionally viewer-facing and the snapshot is meant to be public game state.

## May 2nd, 2026 - Fix: expression controls now drive animations at monitor refresh rate

- Reported as overlay weirdness: a static template using two expression controls (`160 * sin(2 * PI * now_ms() / 5000)` and a cosine sibling) animated an image in a Lissajous orbit, but visibly chugged at ~4fps. Adding a separate random-mode control (16ms interval) and saving it a second time made the animation suddenly silky. The "save twice" part was the giveaway that two bugs were stacked.
- Root cause #1: `useExpressionEngine.ts` advanced its time-dependent heartbeat with `setInterval(..., 250)`, which capped any `now()`/`now_ms()`-driven animation at 4fps regardless of formula resolution. Swapped the heartbeat for a `requestAnimationFrame` loop so time-dependent expressions re-evaluate at the display refresh rate (typically 60Hz, with the browser auto-throttling backgrounded tabs - OBS browser sources stay foregrounded so animations keep running). The random-control "fix" was working only because the random tick mutated `data.value`'s identity every ~100ms, which retripped the expression's `watchEffect` as a side effect.
- Root cause #2: `OverlayRenderer.vue`'s initial mount destructured `random_controls` as `{ min, max }` and dropped the `interval` field, so a page refresh fell back to the 1000ms default in `startRandomTick`. Now passes `rc.interval` through.
- Root cause #3: `OverlayControlController::store()` never broadcast `ControlValueUpdated`, only `update()` did - which is why creating a random or expression control on a live overlay required a second save to take effect. `store()` now broadcasts on create for both expression (with the formula) and random (with min/max/interval) types, matching `update()`'s shape. No more "save twice to make it work."
- Net effect: expression controls using `now_ms()` now render at monitor speed without needing a sidecar random control as a frame ticker, and freshly created controls activate on the open overlay immediately.

## May 2nd, 2026 - Fix: split bot internal throttle so gamejam votes can't 429 the whole bot

- Symptom from yesterday's first chat-castle playtest: a few seconds into a busy round the backend started returning 429 to every bot request, and chat saw `something went wrong` for every command for the rest of the round. Cause: the entire `/api/internal/bot/*` route group sat behind a single `throttle:30,1` (30 req/min, keyed by IP) bucket. The bot polls `/outbox` every 2s = 30/min on its own, so the bucket was always full at idle - one extra `!join` was enough to tip it over. With 50+ players each voting per round, the bucket evaporated instantly. Laravel's default 429 body is `{"message": "Too Many Attempts."}` (no `reply` or `error` key), so the bot's `dispatchGameAction` fell through to its literal `'something went wrong'` fallback for every command.
- Fix splits the inner group into two named buckets, defined in `AppServiceProvider::boot`. `bot-internal` is 600/min by IP and covers token reads, channels, commands, controls, outbox, and settings - 20x the old budget but still bounded. `bot-gamejam-action` is 1200/min keyed per `{login}` route param so a flood in one channel can't starve another, and it carries a `Limit::response()` callback that records the 429 event before returning the JSON.
- `routes/api.php` restructured: the outer prefix group now only carries `bot.internal` middleware and the `withoutMiddleware` opt-outs; two inner `Route::middleware('throttle:bot-internal')` / `Route::middleware('throttle:bot-gamejam-action')` groups split the endpoints. Same auth/CSRF posture, just two buckets instead of one.
- 429 events are recorded into a small `app/Services/Bot/RateLimitLog` cache list (last 50 events, 24h TTL, no DB) and surfaced on `/gamejam/admin` as a "Recent rate-limited (429) requests" section above the debug toggle. Each row shows timestamp + scope + channel login, so the next time a stream throttles you can see it immediately and raise the cap. Chat-side response is intentionally unchanged: `something went wrong` stays in chat, all the diagnostic detail lives in the admin panel.
